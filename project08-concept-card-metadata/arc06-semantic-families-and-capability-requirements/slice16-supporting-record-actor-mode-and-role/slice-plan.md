---
project: project08-concept-card-metadata
arc: arc06-semantic-families-and-capability-requirements
slice: slice16-supporting-record-actor-mode-and-role
status: closed
depends-on: [slice15-provenance-roles-runs-and-shared-references]
version: "1.2"
---

# Actor Mode And Role In Six Supporting Record Kinds

Interpret exactly twelve remaining `actor.mode`/`actor.role` pairs across
memory-admission, preservation-decision, relationship-edge, source-locator,
source-support and validation-result. Compare Slice15's four-kind findings
without inheriting them. This is contextual semantic inventory, not a schema,
role vocabulary, principal registry or migration policy.

## Authority And Assignment

The Operator selected CDC + CRC + CC with Expedited Mode. Arc06
`cdc-directive01.md` approves this exact twelve-pair boundary after Slice15
acceptance and fresh CRC readiness. Slice15 is CRC-closed at `a97ab97b`.
CRC owns this routine open set and later independent slice review; CDC owns
structural changes and arc/project composition. CC's initial assignment is
`cc-prompt.md` and its output remains proposed-done until CRC verification.

### Assignment History

| Prompt | Issued | Predecessor | Reason | Disposition |
| --- | --- | --- | --- | --- |
| cc-prompt.md | 2026-09-17 | CRC-closed Slice15 and arc directive01 | Twelve supporting-kind mode/role pairs with native census, cross-group comparison and pinned replay | Returned at `974dd26a`; CRC R1-R3 in `crc-verification.md`; superseded for correction |
| cc-prompt-iteration01.md | 2026-09-18 | Initial CC packet and CRC R1-R3 | Bind declared evidence authority, validate every range and return literal no-match result | Returned at `9c8ea3a2`; independently CRC-accepted with recipe `713b8860` |

## Exact Scope And Ownership

~~~json
[
  ["actor.mode","memory-admission"],
  ["actor.mode","preservation-decision"],
  ["actor.mode","relationship-edge"],
  ["actor.mode","source-locator"],
  ["actor.mode","source-support"],
  ["actor.mode","validation-result"],
  ["actor.role","memory-admission"],
  ["actor.role","preservation-decision"],
  ["actor.role","relationship-edge"],
  ["actor.role","source-locator"],
  ["actor.role","source-support"],
  ["actor.role","validation-result"]
]
~~~

At opening: 555 frozen full / 208 independently accepted / 347 remaining /
12 assigned / 335 outside. Assignment is not acceptance. The eight Slice15
pairs are already accepted and must not be counted again. The frozen
transition, accepted registries and source skills remain read-only.
Slice17 retains run/preparation/method/shared-reference and remaining CQ
provenance interfaces, subject to CDC sizing; other Arc06 family owners remain
unchanged. P-15 schema/spec discussion and all real-use/UAT gates stay open.

## CRC Pre-Opening Reconnaissance

At authoring, planning `a97ab97b9ac1e8c7583d3c0453ad277a95a8594f` and
source `ce3f77103eff5e07b3533a03c65f158684fc1039` were clean. Frozen
inventory SHA-256 is
`afc1985f1998da0d1df0bdc5800aada9842e77873271fc65f3ae190c0e057b1b`;
post-Slice15 current coverage SHA-256 is
`897bfb603b1c1c5d6b661de811c20553c0db351ec52e19cdcf59145d2bfa57ed`.
CRC checked 555 unique accepted-plus-remaining pairs, 208/347/0/347, and
exactly twelve remaining mode/role pairs: the set above with no accepted
overlap. The initial source-to-current diff has no concept-card or
document-extraction changes.

The frozen inventory has twelve selected parsed object records: two
memory-admission, one preservation-decision, two relationship-edge, one
source-locator, five source-support and one validation-result. Two synthetic
records (admission and edge) have no actor parent. Six templates have actor
objects with null `id`, `mode`, `role`. Four Arc07 pilot supports have
`actor: {id: codex-cc, mode: agent-direct, role: extractor}`. All other
selected mode/role states are absent, null or these strings. Three malformed
rich-rerun files are outside parsed mappings; 15 no-frontmatter files are a
separate availability state. The 2,054 legacy untyped parsed mappings have
no actor parent. These are bounded observations, not vocabulary or
requiredness rules.

CRC read the six complete templates, synthetic admission/edge examples, a
complete pilot support body, and Slice15's accepted registry/report. The
templates show distinct concerns: admission decision authority and operator
acceptance, preservation disposition, edge endpoint roles, locator resolution,
assertion-to-span support, and structural validation identity. None is an
`actor.role` substitute. The populated pilot support actor is support-record
provenance; it does not make the supported claim's actor or source author
identical. The source record-field reference groups actor/provenance without
declaring a closed schema. Operator/extraction/evidence/graph/validation/
admission guidance keeps run, worker, support, result and reliance boundaries
separate. Slice15's card `agent-direct`/`extractor` is a comparison witness,
not a rule to copy into these kinds.

## CC Output And Verification

CC may edit exactly six files in this slice: `artifacts/semantic-membership.json`,
`artifacts/semantic-evidence.md`, `artifacts/validation-evidence.md`,
`artifacts/handoff.md`, `ledger.md`, and `closing-report.md`. No source,
schema, helper/parser, runtime, package, extraction, graph or memory work is
authorized. CC must reproduce the exact set and native census, assign
member-specific meanings with both evidence layers, test wrong value/state
and real-error controls, and provide a pinned, fail-closed replay with all
registered hashes/ranges and scoped preservation. CRC independently reviews
before any pair moves to accepted coverage.

## Prompt Author Readiness

- **Source-grounded:** current coverage, frozen inventory, six full templates,
  two synthetic bodies, one populated support body and accepted Slice15
  comparison were inspected; hashes and counts are CRC preparation, not CC
  evidence.
- **Design-complete:** twelve pairs and outside owners are fixed by CDC.
  Observed `agent-direct`/`extractor` is not an enum, actor identity,
  verification role or admission authority.
- **Guideline-applied:** source operator workflow, evidence lifecycle,
  relationship, validation, preservation and admission guidance justify
  record-local interpretation and distinct result/decision fields.
- **Executable:** `cc-prompt.md` gives output paths, registry shape, jq/native
  diagnostic sketches, route expectations, reading manifest and explicit
  commit/replay sequence.
- **Falsifiable:** predeclared 12 = 2/1/2/1/5/1, 2 absent/6 null/4 populated,
  2,054 legacy, three YAML/15 no-frontmatter; wrong mode/role, child swap,
  absent-as-null, support-to-claim actor, wrong hash/exclusion/reference and
  missing-input controls must reject.
- **Coherent/portable:** approved directive, accepted Slice15 and current
  assignment are separate; reading is bounded for implementation/recovery.
  This readiness is author self-check, not CC delivery or CRC acceptance.

## Version History

- 1.2 (2026-09-18): CRC independently accepts the Iteration01 repair and
  closes all six rows. Exactly twelve bounded mode/role pairs enter accepted
  coverage once; Slice17 remains held for CDC sizing.
- 1.1 (2026-09-18): CRC review keeps Slice16 open and issues Iteration01 for
  registered-authority, all-range and no-match replay corrections. The
  twelve-pair boundary and outside ownership are unchanged.
- 1.0 (2026-09-17): CRC opens the CDC-approved twelve-pair supporting-kind
  unit after Slice15 closure and fresh native/source reconnaissance. No pair
  is accepted by assignment; Slice17 and all outside work remain owned.
