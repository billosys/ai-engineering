# CC Assignment: Slice14 CRC Findings (Iteration 02)

Project: `project08-concept-card-metadata`.
Arc: `arc06-semantic-families-and-capability-requirements`.
Slice: `slice14-provenance-context-and-reference-semantics`.
This prompt: `cc-prompt-iteration02.md`.
Predecessor: preserved `cc-prompt-iteration01.md`, executed at
`6003975321334a0ad554d92663836317b71bd6ce` with follow-up
`ef61a57d636a04deabb9918be80c89ac04b49f14`. This is the first executed
refinement pass; the earlier iteration01 was a routing replacement. Return to
the independent CRC through the Operator. CDC retains design/escalation and
arc/project composition authority.

Work from the canonical planning checkout:
`/Users/oubiwann/lab/billosys/ai-engineering/.worktrees/planning`.
The source checkout is read-only. Read the current project/arc/slice plans and
ledgers, this prompt, `crc-verification.md`, the six-file CC packet, and the
relevant collaboration-framework/project-management/work-verification routes.
The same twelve actor/actor.id pairs, six output files, native inputs, row
criteria and scope fences govern. No schema, source, runtime, package,
extraction, memory, coverage acceptance or Slice15 work is authorized.

## Required Corrections

1. **R1, S14-3:** Correct the `actor.id`/`source-support` membership's false
   "four template/null records" count. There are four populated pilot supports
   and one template/null. Recheck all twelve member-specific meanings against
   the derived census and raw context. Do not infer a global actor principal or
   authority from equal labels.
2. **R2, S14-5:** Repair committed replay so the *executed* literal Bash code
   comes from the explicit `REPLAY_COMMIT`, or an exact fail-closed comparison
   proves that live code equals that committed recipe before execution. Merely
   loading a committed file into an unused variable is insufficient. Show a
   valid separate recipe/contribution revision and a wrong/missing endpoint
   rejection. Keep `CC_COMMIT` as the registry/contribution endpoint.
3. **R3, S14-5:** Remove the global source-HEAD equality lock. Compare
   registered bytes and relevant paths to the pinned source evidence, report
   opening/current commits and hashes, and stop on material changes. Retain
   cleanliness and preservation checks. Make the predicate visibly tolerate
   an unrelated HEAD advance; an isolated fixture may exercise that case, but
   do not alter the source checkout to manufacture it.
4. **R4, S14-2/S14-5:** Derive the exact three rich-rerun YAML-error exclusions
   from pinned frozen-inventory error cells and compare their paths/count to
   the authored exclusions. Keep no-frontmatter, parse-error, parent-absent and
   child-null states separate. Show a wrong-exclusion negative control.

## Acceptance And Return

Re-run the full literal route, all 42 evidence hashes, exact 12-pair and
188/367/12/355 accounting, raw/native census, diagnostics/controls, both
evidence layers, preservation, JSON and whitespace. Record failed/unrun
attempts as well as successful reruns. Keep `crc-verification.md` untouched;
the CC packet remains proposed-done until CRC independently accepts it.
Update the existing CC `closing-report.md` and `ledger.md` with this exact
assignment and the revised row walk. Preserve all issued prompts and prior
CC evidence rather than rewriting history. Use only the same six CC output
paths, changing only those needed for the repair:

- `artifacts/semantic-membership.json`
- `artifacts/semantic-evidence.md`
- `artifacts/validation-evidence.md`
- `artifacts/handoff.md`
- `ledger.md`
- `closing-report.md`

Stage with explicit file names, inspect the staged/unstaged/named-new union,
run `git diff --cached --check`, and commit with `git commit --only` listing
the exact changed files and the required `Co-authored-by: Codex
<noreply@openai.com>` and `Co-authored-by: Billo AI
<ai-engineering@billo.systems>` trailers. Re-run against the committed
endpoint and report commit, exact paths, outcomes and unresolved limits to CRC
through the Operator. Do not write reviewer verification or claim acceptance.
