---
project: project08-concept-card-metadata
arc: arc06-semantic-families-and-capability-requirements
slice: slice18-run-scope-workers-and-outputs
status: open
depends-on: [slice17-run-preparation-method-and-reference-provenance]
version: "1.0"
---

# Run Scope, Workers, And Outputs

Interpret exactly 22 `extraction-run` scope, worker and output pairs. Preserve
the native mapping/sequence/absent distinctions, the two different worker-count
surfaces, and the gap between intended and actual scope. This is contextual
semantic inventory, not a normative run schema, delegation policy, worker-role
vocabulary, output-reference migration or completed extraction trace.

## Authority And Assignment

The Operator selected CDC + CRC + CC with Expedited Mode. Arc06
`cdc-directive02.md` approves exact Set B as Slice18 after Slice17 acceptance
and fresh CRC readiness. Slice17 is CRC-closed at this opening after its
Iteration01 repair; exact Set A's eighteen pairs enter accepted coverage once.
CRC owns this routine open set and later independent review; CDC owns structural
changes and arc/project composition. CC's initial assignment is `cc-prompt.md`;
its return remains proposed-done until CRC verification.

### Assignment History

| Prompt | Issued | Predecessor | Reason | Disposition |
| --- | --- | --- | --- | --- |
| cc-prompt.md | 2026-09-19 | CRC-closed Slice17 and arc directive02 | Exact Set B with mapping/sequence/absence, worker-count, worker-output and target-resolution comparisons | Current CC assignment; pending execution |

## Exact Scope And Ownership

~~~json
[
  ["actual_coverage","extraction-run"],
  ["agent_scope","extraction-run"],
  ["intended_outputs","extraction-run"],
  ["intended_scope","extraction-run"],
  ["output_refs","extraction-run"],
  ["output_refs.cards","extraction-run"],
  ["output_refs.claims","extraction-run"],
  ["output_refs.cqs","extraction-run"],
  ["output_refs.edges","extraction-run"],
  ["output_refs.locators","extraction-run"],
  ["output_refs.source_support","extraction-run"],
  ["output_refs[]","extraction-run"],
  ["output_refs[].id","extraction-run"],
  ["output_refs[].record_type","extraction-run"],
  ["output_refs[].revision","extraction-run"],
  ["parallel_worker_count","extraction-run"],
  ["worker_outputs","extraction-run"],
  ["worker_scope","extraction-run"],
  ["worker_scope.mode","extraction-run"],
  ["worker_scope.roles","extraction-run"],
  ["worker_scope.roles[]","extraction-run"],
  ["worker_scope.worker_count","extraction-run"]
]
~~~

At opening: 555 frozen full / 238 independently accepted / 317 remaining /
22 assigned / 295 outside. Assignment is not acceptance. Slice19-21 retain
their exact unopened Sets C-E; the 248-pair complement retains directive02's
later-family owners. Frozen transition, accepted registries and source skills
are read-only. P-15 and all real-use/UAT gates stay open.

## CRC Pre-Opening Reconnaissance

At authoring, planning `eb7b606baac634f0a3beb3db5d7dcfea26c986e3` and
source `ce3f77103eff5e07b3533a03c65f158684fc1039` were clean. CRC reproduced
Set B from preserved escalation authority `dee3052c`, verified 22 unique pairs
inside the post-Slice17 317-pair remainder, no accepted overlap, and 295 outside.
The frozen inventory remains SHA-256
`afc1985f1998da0d1df0bdc5800aada9842e77873271fc65f3ae190c0e057b1b`.

The native population remains exactly three parsed extraction-run records:
one template and two synthetic examples. The template has null
`actual_coverage`, `agent_scope`, `intended_outputs`, `intended_scope` and
`parallel_worker_count`; empty `worker_outputs`; and mapping-valued
`output_refs` whose six categorized children are empty lists. It has no
`worker_scope` parent. The trace example omits the four scope roots,
`parallel_worker_count` and `worker_outputs`; it has a populated `worker_scope`
mapping with one role and `worker_count: 1`, plus sequence-valued `output_refs`
with two typed ID/revision entries. The parallel recipe omits scope roots,
`parallel_worker_count`, `worker_outputs` and `output_refs`; it has populated
`worker_scope` with two roles and `worker_count: 2`.

CRC read all three complete records and their bodies, current operator/extraction
and field-group guidance, the agent-coordination delegation/integration guidance,
historical v3.2 assignment sections, Slice17's final handoff/review and Slice03's
replay contract. Current guidance says intended and actual scope/output differ,
parallel work must retain each worker's inputs, coverage and outputs, and worker
agreement is not verification. The template defines `parallel_worker_count` as
**additional workers**; neither synthetic `worker_scope.worker_count` field is
documented that way. The parallel synthetic body recommends five roles for
large work but records two workers and coordinator integration in the same
context. These observations do not establish one count model or a closed role
vocabulary.

The source record set exposes two output-reference shapes. Exact inventory
lookup by `id`, `record_type` and `revision` resolves the trace's concept-card
reference to `examples/claim-backed-card.md`. No standalone claim record with
the trace's claim ID/revision is present; appearances as embedded/reference
text are not target resolution. Neither output reference carries a path. This
mixed match/no-match result is a bounded source observation, not validation,
verification, admission or proof that the outputs came from a real run.

The most important current tension is deliberate input to this slice: the
template body asks for worker outputs when parallel work occurs, while the
synthetic parallel recipe records two workers but has no `worker_outputs` key.
The slice must preserve that mismatch and identify its decision owner; it must
not repair source, backfill records or infer that worker scope substitutes for
worker output provenance.

## Applied Guidance And Evidence Method

| Source | Applied decision | Observable proof |
| --- | --- | --- |
| `concept-cards` operator/extraction guides | Intended scope/output, actual coverage/output and worker provenance are separate run concerns | Member meanings and diagnostics do not collapse intent into observation or worker scope into output provenance |
| Extraction-run template and two synthetic examples | Mapping, sequence and absence are native conflicting/alternative observed surfaces, not one normalized schema | Full 3 x 22 matrix records parent-unexpected shape states and exact values |
| Record-field groups and source target witnesses | References retain ID/revision/type and path when needed; empty attachments are not successful review | Target matrix distinguishes one exact match, one successful no-match and tool errors |
| Agent-coordination guides | Contributor seats, lookup workers and extraction-run worker provenance are not automatically the same role system | Meanings avoid importing CC/CRC/CDC authority or a universal worker taxonomy |
| Historical v3.2 assignment sections | Fixed five-agent planning is process history and comparison evidence only | Report labels historical rule separately from current template/example observations |
| Shared evidence/replay contract and prompt-authoring guide | Mechanical identity, semantic warrant and operation outcomes stay separate | Authority-bound hashes/ranges plus independent expectations and real negative controls |

## CC Output And Verification

CC may edit exactly six files in this slice: `artifacts/semantic-membership.json`,
`artifacts/semantic-evidence.md`, `artifacts/validation-evidence.md`,
`artifacts/handoff.md`, `ledger.md`, and `closing-report.md`. No source, schema,
helper/parser, runtime, package, extraction, graph, memory or coverage edit is
authorized. CC must reproduce the exact set/population, give every member a
bounded evidence-backed meaning and consequences, preserve all output/count/
worker tensions, and supply a pinned fail-closed replay. CRC independently
reviews before any pair enters accepted coverage.

## Prompt Author Readiness

- **Source-grounded:** exact coverage, all three full native records, current
  worker/output guidance, target witnesses, historical assignment sections and
  predecessor limits were inspected.
- **Design-complete:** the 22-pair boundary is fixed. Mapping versus sequence,
  additional-worker versus observed worker count, worker-scope versus worker-
  output provenance and intent versus actual coverage stay distinct. Evidence-
  dependent meanings remain findings; normative reconciliation remains P-15/
  Arc02 or a named source-guidance repair.
- **Guideline-applied:** source and agent-coordination guidance determines the
  run-local/delegation/verification boundaries without importing contributor
  seats into extraction records.
- **Executable:** `cc-prompt.md` supplies categorized reading, exact data
  projections, a state model, target lookups, registry/report shapes, ordered
  steps and a six-file fence.
- **Falsifiable:** controls force mapping/sequence/absence, empty/absent,
  additional/total-looking counts, missing worker outputs, wrong element type,
  wrong target kind/revision and no-match/error behavior to disagree.
- **Coherent/portable:** Bash, jq, Git and hashes suffice; the reading packet is
  bounded and leaves semantic/replay repair headroom. This author self-review
  is not CC evidence or independent acceptance.

## Exit Criteria

All seven rows are CC-attested with exact evidence; the six-file contribution
commits with required trailers; precommit and distinct-endpoint replay pass;
unresolved policy questions retain named owners; and CRC independently accepts
the semantics and replay. Slice19 stays unopened until that acceptance and its
own fresh readiness pass.

## Version History

- 1.0 (2026-09-19): CRC opens directive02 Set B after independent Slice17
  closure and fresh exact-set, witness, guidance, target-resolution and prompt-
  author readiness. Assigns 22 pairs at 238/317/22/295 without semantic or
  normative acceptance.
