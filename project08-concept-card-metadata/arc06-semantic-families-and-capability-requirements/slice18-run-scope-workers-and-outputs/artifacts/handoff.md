# Slice18 handoff

Status: CC proposed-done. This packet is bounded evidence for Set B's 22
extraction-run scope, worker and output pairs. It does not authorize a schema,
requiredness, worker-count convention, role vocabulary, output migration,
source repair, real extraction, runtime, graph, memory admission, UAT or
coverage acceptance.

## Boundary and evidence

The exact Set B boundary is 22 unique pairs at opening accounting 555 full /
238 independently accepted / 317 remaining / 22 assigned / 295 outside.
Assignment is not acceptance. The frozen inventory has 2,124 rows, exactly
three parsed extraction-run witnesses, three YAML parse errors and fifteen
no-opening-frontmatter rows. The Complete Musician/Erlang comparison covers
2,054 parsed records and finds zero of the eight Set B root fields.

The complete state/value matrix, source-cited intake readback and field
consequences are in `artifacts/semantic-evidence.md`. The member registry
and authority-bound hashes/ranges are in `artifacts/semantic-membership.json`.
The literal native route and controls are in
`artifacts/validation-evidence.md`.

## Bounded findings

| Family | Finding | Decision owner / limit |
| --- | --- | --- |
| Intent and actual coverage | `intended_scope`, `intended_outputs` and `actual_coverage` are null only in the template and absent in both synthetic witnesses; no populated native scope/coverage witness exists. | Arc02/P-15 and repeated real-run work; do not infer a schema or completeness rule. |
| Agent scope | `agent_scope` is a null template placeholder and absent in both examples. | Arc02/P-15; it is not `worker_scope` or a contributor-seat record. |
| Output shape | Template `output_refs` is a categorized mapping with six empty lists; the trace is a two-entry typed sequence; the parallel example omits the root. | Arc02/P-15; do not normalize mapping and sequence or infer category semantics. |
| Output targets | The trace concept-card tuple matches exactly one inventory record; the claim tuple is a successful no-match. | Exact target lookup is structural only; it is not production, support, verification or admission. |
| Additional count | Template guidance describes `parallel_worker_count` as additional workers; the root is null in the template and absent in both examples. | Arc02/P-15; do not replace null with zero. |
| Worker scope | Synthetic worker scopes record `agent-direct`, counts 1/2 and role lists of one/two strings; the template parent is absent. | Arc02/P-15 and source guidance; repeated values do not close a vocabulary or count policy. |
| Worker outputs | Template has `worker_outputs: []`; both synthetic records omit the root, including the two-worker example. | Arc02/P-15/source-guidance owner; preserve the guidance/example conflict and do not backfill. |

## Native conflicts and limits

The current guidance asks parallel work to retain each worker's inputs,
assigned coverage, returned outputs, conflicts and integration disposition,
while the parallel synthetic example records two workers without
`worker_outputs`. Worker agreement is not verification. Historical v3.2
requires exactly five balanced agents for its old process, but current
guidance does not require five extraction workers and the historical process is
not a current role vocabulary. Contributor seats, lookup packets and
extraction worker records remain separate.

The inventory is a frontmatter projection, and all three run witnesses are
template/synthetic records rather than real extraction traces. The historical
absence result is limited to the named Complete Musician and Erlang roots.
Hashes, ranges, target lookups and replay statuses establish only the checked
structural properties.

## Later ownership and gates

Slice19 retains its exact 19-pair cross-record run-reference Set C, Slice20
retains its exact 11-pair creation-time Set D, and Slice21 retains its exact
17-pair preparation/method-reference Set E. Those slices remain unopened.
The 248-pair complement retains directive02's later-family owners, including
markers, CQ retrieval/revision fields and result/decision-local roles.

P-15, repeated real extractions, the Complete Musician same-chapter trial,
conditional full-book work, runtime/graph and memory behavior, package gates,
UAT and Operator quality acceptance remain open. CRC must independently review
this semantic context and replay before Set B enters accepted coverage; CDC
retains arc composition and structural-change authority.
