# Project08 Arc06 Slice18: Run Scope, Workers, And Outputs

You are CC in the Operator-selected Three-Contributor Workflow and Expedited
Mode. Execute this initial Slice18 evidence assignment. CRC authored it under
`cdc-directive02.md`; return proposed-done evidence to CRC through the Operator.
Do not write a CRC/CDC verdict or treat structural replay as self-acceptance.

## Assignment And Authority

This is planning-only semantic evidence work. Interpret exactly 22
`extraction-run` field-path/record-kind pairs from directive02 Set B. The source
baseline is clean `ce3f77103eff5e07b3533a03c65f158684fc1039`. The
pre-opening planning baseline is clean
`eb7b606baac634f0a3beb3db5d7dcfea26c986e3`; at intake, capture the current
issued planning HEAD containing this prompt and require a clean planning tree.
If either relevant baseline or assignment has materially drifted, stop before
writing and report the mismatch to CRC.

Binding authority, highest first:

1. the Operator's Project08 instructions and active project/arc plans;
2. arc `cdc-directive02.md`, especially Exact Ownership, CRC Readiness, and
   Holds And Return;
3. this slice plan, ledger, and prompt for the exact open set and method;
4. current source guidance for documented behavior;
5. historical v3.2 files as comparison evidence only.

Conflicts at levels 1-3 block affected work. Current source records/guidance
may conflict with each other or historical instructions; record the conflict
and owner rather than choosing a schema. CC may challenge a mistaken
instruction, but may not change scope, requiredness, worker-count/reference
policy, acceptance or outside ownership.

Opening accounting is 555 full / 238 independently accepted / 317 remaining /
22 assigned / 295 outside. Assignment is not acceptance. Slice19-21 remain
unopened. No source, schema, helper/parser, package/install, runtime, graph,
memory, extraction/UAT or current-coverage edit is authorized.

## Required Reading And Execution Preflight

Use repository root `/Users/oubiwann/lab/billosys/ai-engineering` and planning
root `/Users/oubiwann/lab/billosys/ai-engineering/.worktrees/planning`. Read in
this order. Bounded reads must return the full required extent; recover any
truncation with contiguous ranges. Record source state, loaded extent, complete
query-output coverage and gaps in `artifacts/semantic-evidence.md` under
`Intake and contract readback`. Carry that pointer into `closing-report.md`.

### Required-full

| Order | Exact path and state | Why / before which step |
| --- | --- | --- |
| 1 | This `cc-prompt.md`, current issued planning HEAD | Entire active assignment; before all work |
| 2 | This slice's `slice-plan.md` and `ledger.md`, current issued planning HEAD | Exact boundary, source-grounded decisions, rows and stop conditions |
| 3 | `project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/cdc-directive02.md`, planning `eb7b606b` or identical current bytes | CDC authority, exact ownership, holds and review method |
| 4 | `project08-concept-card-metadata/AGENTS.md`, current issued planning HEAD | Project tooling, commit and no-Ruby/no-Python constraints |
| 5 | Source `knowledge/concept-cards/templates/extraction-run.md`, `knowledge/concept-cards/examples/extraction-run-trace.md`, and `knowledge/concept-cards/examples/parallel-worker-default-recipe.md`, source `ce3f7710` | All three native run witnesses, including bodies; before semantic interpretation |
| 6 | Source `knowledge/concept-cards/examples/claim-backed-card.md`, source `ce3f7710` | Exact populated target witness for one output reference; body and frontmatter both matter |
| 7 | Source `knowledge/concept-cards/references/record-field-groups.md` and `references/semantic-audit-boundaries.md`, source `ce3f7710` | Reference cardinality, run grouping and audit limits |
| 8 | `slice03-provenance-and-shared-reference-contracts/artifacts/evidence-replay-contract.md`, planning `eb7b606b` | Shared evidence mechanics; not proof this slice is covered |
| 9 | `slice17-run-preparation-method-and-reference-provenance/artifacts/handoff.md` and `crc-verification.md`, planning `eb7b606b` | Accepted predecessor limits and repaired endpoint/control lessons |

### Required-section

Read each named section completely, including tables/code blocks required by
the section. These are bounded because the full parent documents contain
substantial unrelated material.

| Order | Exact path/state and sections | Purpose |
| --- | --- | --- |
| 10 | Project `project-plan.md`, current issued HEAD: `Schema And Specification Discussion Gate`, `Current Direction: Semantic Families And Capabilities`, `Contributor Workflow`, `Design Handoff History`, `Arc Roadmap: Current Dependency Order`, `Scope And Version Boundaries`, `Closure And Plan Changes` | Project authority, P-15 and no premature acceptance |
| 11 | Arc `arc-plan.md`, current issued HEAD: `Current Review`, `Design Handoff History`, `Starting Evidence And Coverage`, `Operating Method`, `Slice Roadmap`, `Research And Capability Priorities`, `Deliverables And Acceptance` | Family method, owners and composition limits |
| 12 | Source `knowledge/concept-cards/SKILL.md`: `When To Use`, `Ownership And Dependencies`, `Guide Map`, `Record Templates`, `Representative Examples`, `References And Review Surfaces`; source `ce3f7710` | Current entrypoint and record/source boundaries |
| 13 | Source `guides/01-load-contract.md`: `Construct Boundaries`, `Distinct Evidence And Lifecycle Signals`, `Operating Modes And Availability`; `guides/02-operator-workflow.md`: `Record Scope Before Deriving Content`, `Agent-Direct Operation`, `Report The Handoff And Remaining Work`; source `ce3f7710` | Run scope, worker provenance, operation mode and result boundaries |
| 14 | Source `guides/03-extraction.md`: `Establish The Extraction Run`, `Record Confidence And Check The Output`, `Agent-Direct Operation`, `Handoff`; source `ce3f7710` | Current intended/actual output and parallel-work rules |
| 15 | Source `guides/04-re-extraction-preservation.md`: `Establish The Source Baseline And New Run`, `Compare Changed, Unchanged And Unavailable Material`, `Review The Candidate Set And Its Dependencies`, `Handoff`; source `ce3f7710` | Scope/output accounting and parallel-worker comparison limits |
| 16 | Source `guides/07-reconciliation.md`: `Establish Targets And Conflict Scope`, `Compare Evidence Before Deciding`; `guides/08-validation-verification.md`: `Define The Review Before Checking`, `Record Review Actors And Evidence Provenance`, `Prevent False Upgrades`; source `ce3f7710` | Worker disagreement/integration is not verification or automatic reconciliation |
| 17 | Source `knowledge/agent-coordination/guides/01-when-to-delegate.md`: `Core Policy`, `Operating Rule`, `Why The Boundary Exists`; `02-context-packets.md`: `Packet Purpose`, `Required Fields`, `Output Contract`; `03-result-integration.md`: `Parent-Context Responsibility`, `Integration Steps`, `Evidence Strength`; `04-anti-patterns.md`: `Thinking Delegation`, `Summary Trust`, `Acceptance By Formatting`; source `ce3f7710` | Distinguish extraction workers, lookup delegation, sustained contributor seats, returned output and acceptance |
| 18 | Historical `old/dev/concept-cards/0010-a-guide-for-parallel-concept-card-re-extraction-v3.2.md`: `Step 1.3: Create Balanced Agent Assignments`, `Step 1.4: Compile Agent Instructions`, and `Agent Prompt Template (Re-Extraction)` with all subsections from `Your Scope` through `File Naming`; planning current issued HEAD | Historical fixed-five process and output expectations, not current schema authority |
| 19 | `slice03-provenance-and-shared-reference-contracts/artifacts/worked-replay.md`: `Pinned inputs and prerequisites`, `Case 1`, `Case 2`, `Literal route`; planning `eb7b606b` | Reusable route pattern and operation-result classes |

### Required-data

Execute and inspect complete results; do not sample. Preserve commands, exits,
denominators, exclusions and errors in `artifacts/validation-evidence.md`.

1. **Exact set and opening coverage.** Read Set B from preserved escalation
   commit `dee3052c88e0fd9361e74200fd1eea26ade76435`, compare it with the live
   current register at issued planning HEAD, the frozen register, accepted and
   remaining sets. Required result: 22 unique assigned pairs, all remaining,
   no accepted overlap, 555/238/317/22/295, and the exact Slice18 path.
2. **Complete native population.** From the frozen inventory at
   `arc01-metadata-research-and-requirements/slice01-metadata-inventory-and-research-questions/artifacts/frontmatter-inventory.json`, SHA-256
   `afc1985f1998da0d1df0bdc5800aada9842e77873271fc65f3ae190c0e057b1b`,
   project all three parsed `record_kind == "extraction-run"` records with
   path, keys, shapes and complete values.
3. **Every Set B state.** Produce the 3 records x 22 paths matrix without
   collapsing root absence, parent absence, wrong-parent shape, child absence,
   null, empty list, empty mapping, scalar, populated mapping, populated
   sequence, sequence element or element-child state. Retain exact values/types.
4. **Availability exclusions.** Reproduce exactly three true YAML parse errors
   and fifteen `no-opening-frontmatter` records from all 2,124 inventory rows.
   Neither group is an absent extraction-run field.
5. **Historical comparison.** Query all 2,054 parsed records under the Complete
   Musician and Erlang roots. Required bounded observation: zero records have
   any of the eight Set B root fields. This is absence in those card inputs,
   not proof that their extraction processes had no scope, workers or outputs.
6. **Output target lookup.** Resolve both sequence entries from
   `extraction-run-trace.md` by exact `id`, `record_type` and `revision` against
   the source inventory/bytes at `ce3f7710`. Required opening result: the
   concept-card reference matches exactly one root record,
   `knowledge/concept-cards/examples/claim-backed-card.md`; the claim reference
   is a successful no-match because no standalone claim target exists. Mentions
   or nested references are not target matches. Separately query a missing
   inventory input or invalid Git authority and preserve the tool error.

Suggested exact-set check, expected to run from the planning checkout after
this prompt is issued:

~~~bash
set -euo pipefail
p=project08-concept-card-metadata
a=$p/arc06-semantic-families-and-capability-requirements
base=dee3052c88e0fd9361e74200fd1eea26ade76435
set_b=$(git show "$base:$a/crc-escalation02.md" |
  awk '/^~~~json$/{p=1;next} p && /^~~~$/{p=0;next} p' | jq -s '.[1]')
coverage=$(cat "$p/artifacts/semantic-coverage-current.json")
jq -n -e --argjson b "$set_b" --argjson c "$coverage" '
  ($b|length)==22 and ($b|unique|length)==22 and
  ($b-$c.remaining_pairs|length)==0 and
  ($b-$c.accepted_pairs|length)==22 and
  $c.counts=={full:555,accepted:238,remaining:317,next_slice:22,not_yet_sliced:295} and
  $c.next_slice=="arc06-semantic-families-and-capability-requirements/slice18-run-scope-workers-and-outputs" and
  ($c.next_slice_pairs|sort)==($b|sort)'
~~~

### Conditional And Reference-only

- **Conditional:** load another referenced source/body in full before relying
  on its semantics. Record trigger and extent. Do not follow unbounded links.
- **Conditional:** if an evidence row needs a range outside a named required
  section, read that exact containing section and register it before use. If
  unavailable, leave the semantic claim unresolved.
- **Conditional:** consult Slice15's accepted actor-mode evidence only if a
  `worker_scope.mode` comparison would otherwise conflate it with `actor.mode`;
  record why the comparison was triggered and preserve kind/path limits.
- **Reference-only:** Slice13-16 packets are provenance history, not current
  execution instructions. Do not load them by default.
- **Reference-only:** Slice19-21 exact sets. Do not create their files, analyze
  their meanings or treat future requirements as Slice18 output.

After loading, write a concise source-cited contract readback in your own words.
Connect scope, shape/state distinctions, count/reference limits, failure
classes, six-file fence and independent acceptance. The easily missed
constraint is twofold: `parallel_worker_count` is documented as **additional
workers**, while populated `worker_scope.worker_count` occurs on a different
surface; and template `output_refs` is a categorized mapping while the trace's
is a typed sequence. Neither pair may be silently equated or migrated. If the
intake is incomplete or contradictory, stop before dependent work.

## Baseline Reconnaissance And Binding Boundaries

CRC observed, but CC must reproduce, this native matrix:

| Witness | Set B state summary |
| --- | --- |
| Template `extraction-run.md` | Four scope/intent roots and `parallel_worker_count` are null; `worker_outputs` is an empty list; `output_refs` is a mapping with six empty-list children; sequence paths under `output_refs[]` encounter an object parent; `worker_scope` is absent |
| Synthetic `extraction-run-trace.md` | Four scope/intent roots, `parallel_worker_count` and `worker_outputs` are absent; `output_refs` is a two-entry sequence with populated ID/type/revision children, so categorized mapping children encounter an array parent; `worker_scope` is a populated map with mode `agent-direct`, count 1 and one role `extractor` |
| Synthetic `parallel-worker-default-recipe.md` | Four scope/intent roots, `parallel_worker_count`, `worker_outputs` and `output_refs` are absent; `worker_scope` is a populated map with mode `agent-direct`, count 2 and roles `source-scope`, `claim-support` |

Binding interpretation boundaries:

- template placeholders document supported surfaces, not requiredness or a
  completed run; null, empty and absent remain separate observations;
- `intended_scope`, `intended_outputs`, `actual_coverage` and `agent_scope` have
  no populated native witness here. Guidance supports intent/actual and actor/
  worker distinctions, but does not settle the fields' future exact schema;
- mapping-valued and sequence-valued `output_refs` are incompatible observed
  shapes. Do not infer that categorized mapping lists and sequence records are
  equivalent, dual serializations or a migration rule;
- `output_refs` identifies run outputs only within its recorded shape. It is not
  `worker_outputs`, validation, verification, reconciliation or admission;
- target lookup is exact on ID, record kind and revision. Nested mentions are
  not standalone records, a no-match is not an error, and a matching target
  does not prove production by a real run;
- `parallel_worker_count` is documented in the template body as **additional
  workers**. Synthetic bodies describe `worker_scope.worker_count` as one or
  two workers in those examples. Do not normalize the values or infer whether
  a coordinator is included outside the literal example text;
- `agent_scope` and `worker_scope` are distinct paths. Likewise accepted
  `actor.mode` and current `worker_scope.mode` are not interchangeable merely
  because both examples say `agent-direct`;
- `worker_outputs: []` in the template is different from its absence in both
  examples. The parallel example's body says two workers were used while the
  key is absent; preserve this guidance/example tension and route its policy
  decision to P-15/Arc02 or a named source-guidance repair;
- the five historical/current recipe roles are recommendations or observed
  assignments in context, not a closed role vocabulary. Contributor seats are
  collaboration authority, not extraction worker records;
- unresolved evidence gets an explicit question and owner, not a guessed
  normative disposition or opportunistic source repair.

## Required Outputs And Evidence Shapes

Create only these four artifacts and update only the two closeout files named
later. Use existing Bash, jq, Git, `shasum` and ordinary shell tools. Do not add
Ruby, Python, Fennel, Rust or any helper script.

### 1. `artifacts/semantic-membership.json`

Use valid JSON with exactly 22 unique membership objects, one per exact pair.
Reuse the local Slice17 registry conventions where applicable. Every membership
must include at least:

~~~text
field_path, record_kind, meaning_id, effective_meaning, applicability,
observed_states, evidence_ids, exceptions,
consequences.reader, consequences.extractor, consequences.query,
consequences.migration, unresolved_questions, disposition
~~~

`effective_meaning` is a bounded interpretation supported by evidence, not a
schema definition. Mapping-child and sequence-child members must state parent
shape applicability. Wildcard members must state element population and child
requirements without treating a mapping as a sequence. Shared evidence is
allowed only with member-specific applicability/exceptions. Do not use generic
"preserve for Arc02" as the only consequence.

Include an `evidence_registry` or established equivalent covering every cited
input. Each evidence row records:

~~~text
evidence_id, root, path, read_mode, authority_commit, sha256, role,
member_scope, source_range, interpretation, limit
~~~

Every snapshot row resolves bytes through its declared authority. Numeric line
ranges are validated at that authority. Approved nonnumeric descriptors must be
exact, allowlisted only for intended JSON evidence IDs, and validate the whole
intended document/projection. Unknown, null, misplaced, reversed or out-of-
bounds ranges reject. Hash/range identity is not semantic warrant.

### 2. `artifacts/semantic-evidence.md`

Include:

- intake and source-cited contract readback;
- exact population/exclusion census;
- complete 3 x 22 state/value matrix or lossless partition;
- documented-rule versus template observation versus synthetic observation
  versus historical comparison versus author inference versus unresolved;
- separate analyses for intent/actual scope, output reference shape/targets,
  worker counts/modes/roles and worker output provenance;
- explicit guidance/example conflicts, with affected members and owner;
- field-specific reader, extractor, query and migration consequences;
- explicit limits and decision owners for every unresolved question.

Do not prescribe desired conclusions as evidence. In particular, do not repair
the absent synthetic `worker_outputs`, rewrite either `output_refs` shape, or
declare a canonical count convention.

### 3. `artifacts/validation-evidence.md`

Contain a literal executable Bash replay plus recorded precommit/committed
results. The production predicates must validate:

- exact 22-pair set, uniqueness, inclusion/disjointness and 238/317/22/295;
- all membership IDs, evidence references and required consequence fields;
- native population, complete matrix, exclusions and 2,054-record historical
  comparison against independently authored expectations;
- every declared evidence authority, hash and range;
- output target result matrix and every negative control below;
- unchanged frozen/accepted predecessor evidence, relevant source bytes, exact
  six-file contribution scope and whitespace.

Support precommit mode from the working registry. Committed mode must load the
registry from `CC_COMMIT` and execute recipe bytes extracted from a separately
named `REPLAY_COMMIT`. Preserve a literal outer wrapper that loads the Markdown
from `REPLAY_COMMIT`, extracts exactly one nonempty Bash route, runs `bash -n`,
and executes with explicit `CC_COMMIT`/`REPLAY_COMMIT`. Missing, stale or
foreign recipe endpoints fail closed through that wrapper. Relevant source
bytes are pinned to `ce3f7710`; unrelated global HEAD movement may be reported
but must not replace authority. Preserve stdout, status and stderr-presence for
match, successful no-match and tool-error cases.

### 4. `artifacts/handoff.md`

Summarize the 22 bounded findings, native conflicts, member-specific unknowns
and decision owners. Preserve exact unopened ownership for Slice19-21 and the
248-pair complement. State how this evidence informs future schema/spec/source-
guidance discussion without adopting a schema or repairing source. Keep P-15,
repeated real extractions, Complete Musician trial, conditional full-book work,
runtime/memory and Operator quality acceptance explicitly open.

### 5-6. `ledger.md` and `closing-report.md`

Update all seven rows honestly. Proposed-done is the strongest CC status. The
closing report inventories every artifact, changed path, commit, check, failed
or unrun attempt, unresolved item and bubble-up. Do not write
`crc-verification.md` or update parent plans/ledgers/coverage.

## Implementation Sequence

1. Complete preflight and write intake/readback before semantic drafting.
2. Reproduce exact set, coverage, native/exclusion/historical populations and
   target census. Stop on a mismatch rather than adjusting denominators.
3. Build a machine-readable state projection before assigning meanings. The
   implementation must understand roots, mapping children, sequence wildcards
   and sequence-element children. This illustrative jq shape is not a tested
   drop-in route, but the state contract is binding:

~~~jq
def value_state($x):
  if $x == null then "null"
  elif ($x|type)=="array" and ($x|length)==0 then "empty-list"
  elif ($x|type)=="object" and ($x|length)==0 then "empty-mapping"
  elif ($x|type)=="array" then "populated-sequence"
  elif ($x|type)=="object" then "populated-mapping"
  else "populated-\($x|type)" end;

# For root.child: absent parent, unexpected parent shape, absent child, or value.
# For root[]: absent parent, unexpected non-array parent, empty/populated sequence.
# For root[].child: retain one state per element, including non-object element and
# absent child; never flatten the element results into a single invented value.
~~~

   Compare native output to an independently authored expected matrix. Do not
   derive expected and observed from the same function or registry.
4. Author all 22 contextual meanings from full witnesses/guidance. Record the
   two shape models and worker-output/count tensions; do not resolve them by
   preference.
5. Build native diagnostic objects from actual inventory/source observations,
   then compare with independent literal expectations using
   `jq -n -e --argjson actual ... --argjson expected ... '$actual==$expected'`.
6. Register all evidence and implement the literal fail-closed route and outer
   wrapper before claiming rows done. Run negative controls through the same
   production predicates as valid candidates.
7. Write handoff/ledger/closeout, inspect the staged union, commit only six
   explicit files, then create/select a distinct recipe endpoint and run the
   committed wrapper/replay. Correct only within the six-file scope.

## Native Diagnostics And Discriminating Oracles

| Row | Setup/action | Required positive observation | Incorrect behavior the control must reject |
| --- | --- | --- | --- |
| S18-1 | Compare directive Set B, coverage and frozen union | Exact 22; 555/238/317/22/295; no accepted overlap; exact Slice18 path | Extra/duplicate pair, accepted/outside pair, Set C-E member or changed denominator |
| S18-2 | Resolve every evidence row from its declared authority/range | Every row classified/retrievable at declared commit | Wrong/nonexistent authority, wrong hash, unknown/misplaced/out-of-bounds/reversed range, dangling evidence |
| S18-3 | Project all three records through shape-aware state logic | Exact matrix summarized above; 3 YAML errors; 15 no-frontmatter; 2,054 historical records with zero Set B roots | Mapping child applied to sequence, wildcard applied to mapping, absence rewritten as null/empty, malformed inputs treated as records |
| S18-4 | Compare full bodies/guidance and historical sections | Member-specific rule/observation/conflict/inference/unknown labels and consequences | Intent=actual; agent_scope=worker_scope; additional-worker count=worker count; five-role recipe=observed role list; contributor seats=run workers |
| S18-5 | Resolve both output entries by exact ID/kind/revision | Concept card exact match; claim successful no-match; missing input/authority tool error | Nested mention accepted as target, wrong kind/revision accepted, no-match rewritten as error or target absence treated as validation |
| S18-5 | Mutate matrix/diagnostic candidates | Correct native objects match independent expected objects | Mapping/sequence swap, empty-to-absent, null-to-zero, missing worker outputs silently backfilled, role scalar/list collapse, element child removed or wrong type passes |
| S18-6 | Run precommit and distinct-endpoint wrapper/replay | Status 0 with hashes/ranges, matrix, targets, controls, protected history/source, six-file scope and whitespace | Missing/stale/foreign endpoint, wrong authority/hash/range, altered protected input or seventh path passes |
| S18-7 | Walk handoff against directive02/plans | Slice19-21 unopened, 248 complement retained, all gates/conflicts explicit | Source repair, schema adoption, real extraction, memory admission, successor or arc acceptance claimed |

Use these controls through the same predicates as positives:

- change template `output_refs` from mapping to sequence; change trace
  `output_refs` from sequence to mapping;
- remove one empty categorized child, make one child scalar, and add a mapping
  child to the sequence candidate;
- remove one sequence element's `record_type`; change its revision; use the
  concept-card ID with `record_type: claim`; add a scalar element;
- change template `parallel_worker_count` from null to zero; copy synthetic
  `worker_scope.worker_count` into it; change a synthetic worker count to a
  string;
- remove `worker_scope`, turn `roles` into a scalar, swap mode with a role, and
  backfill `worker_outputs` in the parallel example;
- change template empty `worker_outputs` to absent and one null scope field to
  absent;
- mutate one evidence authority, digest and range independently;
- add one accepted/outside pair to the membership set;
- query a nonexistent inventory path or invalid Git authority and preserve its
  error separately from successful no-match.

Record actual statuses; do not demand a particular nonzero value unless the
tool contract guarantees it. A passing wrapper is structural evidence only.

Run at minimum: `jq empty` on the registry; full precommit route; committed
distinct-endpoint wrapper/replay; every listed control; `git diff --check`;
`git diff --cached --check`; exact staged/unstaged/named-new path-union checks;
and clean source/planning status after commit. Source package gates are not a
substitute and are not required for this planning-only packet. Record
exploratory failures and required checks not run.

## Scope, Stop Conditions, Commit, And Return

CC may change exactly:

~~~text
project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice18-run-scope-workers-and-outputs/artifacts/semantic-membership.json
project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice18-run-scope-workers-and-outputs/artifacts/semantic-evidence.md
project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice18-run-scope-workers-and-outputs/artifacts/validation-evidence.md
project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice18-run-scope-workers-and-outputs/artifacts/handoff.md
project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice18-run-scope-workers-and-outputs/ledger.md
project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice18-run-scope-workers-and-outputs/closing-report.md
~~~

Create `artifacts/` as needed. Everything else is read-only. Stop and return to
CRC if the set/population differs, a binding input is unavailable/truncated,
semantic evidence contradicts accepted work, normative resolution is needed,
the route cannot preserve mapping/sequence/no-match/error distinctions, or
context pressure threatens complete execution. CRC escalates structural issues
to CDC through the Operator. Do not silently shrink scope or continue into
Slice19.

Use explicit filenames in `git add --` and `git commit --only --`; never add a
directory/glob, use broad staging, or use `-a`. Inspect staged, unstaged and
untracked paths. Include both trailers:

~~~text
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>
~~~

Return exact opening/source/CC/recipe commits; six changed paths; intake/
readback pointer; exact set/census/matrix/target outcomes; per-row attestation;
precommit/committed exits; every failed/unrun check; deviations, unresolved
questions and workload observations including model/effort if known, context
recovery/compaction and correction categories. CRC independent verification,
CDC arc composition and Operator acceptance remain separate gates.

## Prompt-Author Readiness Record

CRC recorded all six readiness checks in `slice-plan.md`: source-grounded,
design-complete, guideline-applied, executable, falsifiable and coherent/
portable. The packet uses bounded sections plus complete three-record, exact-
set and output-target projections, leaving semantic drafting/replay/repair
headroom. Native controls force incompatible output shapes, count meanings,
missing worker provenance, exact target identity and failure classes to
disagree. This author self-check is not CC evidence or independent acceptance.
