# Slice17 CRC Verification

Current verdict: **accepted after Iteration01**. This is independent CRC review
under the Three-Contributor Workflow, not CDC arc composition or Operator
acceptance. Exactly eighteen bounded pairs enter accepted coverage once; live
accounting becomes 238 accepted / 317 remaining / zero assigned before the
successor opens.

Date: 2026-09-18. Opening planning `c40e52fc`, CC registry endpoint
`97a75091`, reported separate recipe endpoint `f82af9b7`, final documentation
endpoint `89d1b452`, source `ce3f7710`. Both worktrees were clean at review
intake. The opening-to-final diff contains exactly the six authorized Slice17
files; all three CC commits have both required trailers and `git diff --check`
passes.

## Independent Checks

- Read the active project/arc/slice plans, directive02, prompt, ledger, complete
  six-file packet, current source guidance, all three native extraction-run
  witnesses and the historical comparison sections. The eighteen interpretations
  remain bounded observations; they do not make synthetic examples real runs,
  equate singular/plural references, promote prepared provenance to claim
  support, or adopt timestamp/reference policy.
- Independently inspected the registry: 18 unique exact memberships, 18 unique
  meaning IDs, 39 unique evidence IDs, no missing member evidence, nonempty
  meanings/applicability/consequences/questions, and registry SHA-256
  `9b1db69bb5d1628e1e47fc93e866c2be03f7617d7ebb7e3a3978247c387dd777`.
- Extracted the literal route from committed recipe `f82af9b7`, ran `bash -n`,
  then executed it with registry `97a75091`. Exit status was 0. It reproduced
  the three native records, full 3 x 18 matrix, three YAML errors, fifteen
  no-frontmatter records, 2,054 historical records with zero presence for all
  twelve roots, both successful target no-matches and the status-128 tool error.
  All controls actually present in the route returned status 1.
- Direct recipe extraction from the opening and source commits failed closed
  because the validation path is absent. The direct Git operation returned 128,
  not the recorded 2; no literal wrapper mapping/executing those endpoints is
  preserved in the packet.

## Findings

| ID | Rows | Finding and evidence | Required correction |
| --- | --- | --- | --- |
| R1 | S17-1, S17-3, S17-5 | The issued prompt requires **all** listed controls (`cc-prompt.md` lines 334-350). The literal route (`validation-evidence.md` lines 279-294) omits at least four discriminators: template null-to-absent, addition of one accepted/outside membership pair, pointing one trace path at the other declared target, and a nonexistent inventory input or invalid revision. Existing empty-list, arbitrary wrong-path and missing synthetic-target checks do not execute those distinct cases. The closeout nevertheless says all required controls passed. | Add the four controls through the existing production predicates. Use a template null field such as `finished_at`; add a structurally complete cloned membership with a unique meaning ID but an accepted/outside pair; swap the trace input path to the prepared target (or vice versa); and run the native inventory query against a missing input or invalid Git authority. Record actual outputs/statuses and correct all “all controls” claims. Preserve every existing positive and negative check. |
| R2 | S17-6 | The artifact says a committed wrapper loaded recipe bytes from `REPLAY_COMMIT` and that missing/foreign recipe extraction returned status 2 (`validation-evidence.md` lines 299-301, 345-359), but it preserves only the inner route. Inside that route `REPLAY_COMMIT` is merely checked as an existing commit; the route cannot prove its own bytes came from that commit. CRC's direct extraction from the named missing/foreign commits returned Git status 128. The fail-closed property exists, but the claimed wrapper behavior is not reproducible from the packet. | Preserve a literal endpoint wrapper in `validation-evidence.md`. It must load the Markdown from the declared `REPLAY_COMMIT`, extract exactly one nonempty Bash route, syntax-check it, and execute it with the declared `CC_COMMIT`/`REPLAY_COMMIT`. Valid separate endpoints must pass; opening/foreign/missing route endpoints must fail closed. Either map extraction failure explicitly to 2 and show that wrapper, or record the actual nonzero status without claiming 2. |
| R3 | S17-3 | `validation-evidence.md` lines 337-339 describes “the three YAML-error paths” as “the two ... files for memory/priming/recognition.” The route and semantic evidence correctly identify three files, so this is a reporting contradiction rather than a census defect. | Change “two” to “three” and keep the exact three paths/count intact in closeout wording. |

S17-2, S17-4 and S17-7 have no finding. The semantic registry,
`semantic-evidence.md` and `handoff.md` should remain byte-identical unless a
new contradiction is discovered. R1-R3 are bounded replay/report corrections,
not a scope, architecture or acceptance change; no CDC escalation is needed.

## Bubble-Up To Arc06

The exact eighteen-pair boundary, 317 outside pairs, unopened Slice18-21 and
248-pair complement remain unchanged. P-15, source/schema/runtime/package,
memory, UAT and Operator gates remain open. Current correction assignment:
`cc-prompt-iteration01.md`. CRC will independently execute the returned wrapper
and route before any coverage transfer or Slice18 readiness work.

## Iteration01 Independent Review

Date: 2026-09-19. Assignment: `cc-prompt-iteration01.md`. CC contribution
endpoint `e123b60dd846f2dfaf655642c0198f6895d8d675`; independently selected
recipe endpoint `3e4ef8362b9bd917d09b44f30cb057d936a1f71d`; final documentation
endpoint `eb7b606baac634f0a3beb3db5d7dcfea26c986e3`; source endpoint
`ce3f77103eff5e07b3533a03c65f158684fc1039`. Both worktrees were clean at
review intake. The Iteration01 diff from CRC baseline `a0f51639` through final
documentation changes exactly the three authorized repair files, all commits
carry both repository trailers, and `git diff --check` passes.

CRC extracted the committed outer wrapper itself from `REPLAY_COMMIT=3e4ef836`,
syntax-checked it, and executed it against `CC_COMMIT=e123b60d`. The route
returned status 0. Same-endpoint replay at `e123b60d` also returned 0. Opening
recipe `c40e52fc`, foreign source `ce3f7710`, and an absent all-zero commit
failed closed with status 2. This independently establishes the preserved
endpoint behavior rather than relying on CC's recorded status.

R1 passes. The route retained every initial control and independently
reproduced the new discriminators: null-to-absent returned 1, adding an
accepted/outside membership returned 1, pointing a trace path at the other
declared target returned 1, and querying a missing inventory input returned
128 with nonempty stderr. The positive exact-set route still reports 555 full /
220 accepted / 335 remaining / 18 assigned / 317 outside.

R2 passes. The wrapper loads the recipe Markdown from its declared
`REPLAY_COMMIT`, extracts exactly one nonempty Bash route, runs `bash -n`, and
executes with explicit contribution and recipe endpoints. Its valid separate
and same-endpoint cases pass; missing, stale and foreign recipe endpoints fail
closed through the wrapper.

R3 passes. The exclusion account consistently records three YAML-error files
and fifteen no-frontmatter records. The native route also reproduced all three
extraction-run witnesses, the complete 3 x 18 state matrix, the bounded 2,054-
record historical absence, two successful declared-target no-matches, and the
deliberate status-128 tool error. All earlier controls continued to return 1.

The registry SHA-256 remains
`9b1db69bb5d1628e1e47fc93e866c2be03f7617d7ebb7e3a3978247c387dd777`.
These checks accept only the eighteen bounded contextual meanings and evidence
route. They do not adopt a run schema, requiredness, timestamp/reference
policy, source repair, completed extraction, claim support, runtime behavior,
memory admission or UAT result.

## Final Row Verdict

S17-1 through S17-7 are independently done. The initial R1-R3 findings remain
preserved above and are satisfied by Iteration01. All four supporting artifacts,
both issued prompts and the two close-set records are present at their canonical
paths. CC's repair touched only the three permitted existing files; there is no
silent artifact, row, deferral or no-op drop.

## Final Bubble-Up To Arc06

Slice17 delivers directive02 Set A without changing its eighteen-pair boundary
or any outside owner. Exactly those eighteen pairs enter accepted coverage
once, producing 238 accepted / 317 remaining / zero assigned before the next
open set. The evidence confirms, rather than resolves, the planned distinction
between operation/input/method/time provenance and Slice18's run scope, worker
and output semantics.

No finding changes Arc06's capability, sequence or acceptance contract.
Directive02 already authorizes fresh Slice18 readiness after this acceptance;
P-15, the 248-pair later-family complement, Slices19-21, source/schema/runtime/
package/memory/UAT work and Operator quality acceptance remain open.
