# CC Assignment: Arc06 Slice20 Cross-Record Creation Time

You are the Code Contributor (CC) for Project08 under the Operator-selected
Three-Contributor Workflow and Expedited Mode. Execute this evidence-only
semantic assignment and return the committed packet to CRC for independent
review. Your strongest status is **CC proposed-done**. Do not write
`crc-verification.md`, accept coverage, open Slice21 or claim CDC/Operator
acceptance.

## Assignment Identity And Baseline

- Project: `project08-concept-card-metadata`
- Arc: `arc06-semantic-families-and-capability-requirements`
- Slice: `slice20-cross-record-creation-time`
- Active prompt: `slice20-cross-record-creation-time/cc-prompt.md`
- Source checkout: `/Users/oubiwann/lab/billosys/ai-engineering`
- Source authority: `a5861c4b8e93af932d18cb571415cc570c758240`
- Planning checkout:
  `/Users/oubiwann/lab/billosys/ai-engineering/.worktrees/planning`
- Opening planning authority: `OPENING_COMMIT_PENDING`
- Frozen inventory authority: planning `5e6310b8ef6d48a45b816822cdbc658a62603ba6`
- Predecessor acceptance: Slice19 CRC record at the opening planning authority;
  accepted CC candidate `30facb12959880144e1a8f0511d8f308ec7d6772`

The opening placeholder must have been replaced by CRC before relay. Stop if it
is still present, if the active assignment in `slice-plan.md` differs, or if a
materially different source/planning state changes any binding input. Unrelated
source HEAD movement is not itself failure: verify the declared source blobs
and report drift. Do not silently repin or reinterpret inputs.

This is investigation and evidence work. It changes no source skill, template,
schema, helper, runtime, package, extraction output or memory surface.

## Required Outcome

Interpret exactly eleven `created_at` field-path/record-kind pairs as
contextual metadata observations. Produce a recoverable evidence packet that:

1. preserves absent, explicit-null and populated-string states;
2. records the lexical precision actually observed;
3. gives each record kind a bounded, evidence-cited effective meaning;
4. distinguishes containing-record creation from other temporal events;
5. exposes every unknown needed by later schema/specification work; and
6. proves its own exact scope, evidence identity and controls through committed
   replay.

Do not make a desired semantic conclusion the condition for passing. If full
context contradicts CRC reconnaissance, preserve the observation, stop any
affected conclusion and return the conflict to CRC.

## Exact Set D Boundary

~~~json
[
  ["created_at","claim"],
  ["created_at","competency-question"],
  ["created_at","concept-card"],
  ["created_at","memory-admission"],
  ["created_at","preservation-decision"],
  ["created_at","reconciliation-result"],
  ["created_at","relationship-edge"],
  ["created_at","source-locator"],
  ["created_at","source-support"],
  ["created_at","validation-result"],
  ["created_at","verification-result"]
]
~~~

Opening accounting is exactly 555 frozen full / 279 accepted / 276 remaining /
11 assigned / 265 outside. Assignment is not acceptance. Set D is incorporated
from `crc-escalation02.md` by `cdc-directive02.md`; do not regenerate it from a
field-name prefix. Set E's 17 pairs and the 248-pair complement remain outside.

## Required Reading And CC Intake

Perform this intake in the executing context before dependent semantic work.
Use bounded complete reads, recover truncation, and record the loaded extent,
authority and concise contract readback in
`artifacts/validation-evidence.md`. File existence, hashes and another
contributor's summary do not prove reading.

### Required-full

Read each complete file, including tables and code blocks, in this order:

1. this prompt;
2. `slice-plan.md` and `ledger.md` in this slice;
3. `../cdc-directive02.md`;
4. `../slice19-cross-record-run-references/crc-verification.md`;
5. all eleven source templates at source authority `a5861c4b`:
   - `knowledge/concept-cards/templates/claim.md`
   - `knowledge/concept-cards/templates/competency-question.md`
   - `knowledge/concept-cards/templates/concept-card.md`
   - `knowledge/concept-cards/templates/memory-admission.md`
   - `knowledge/concept-cards/templates/preservation-decision.md`
   - `knowledge/concept-cards/templates/reconciliation-result.md`
   - `knowledge/concept-cards/templates/relationship-edge.md`
   - `knowledge/concept-cards/templates/source-locator.md`
   - `knowledge/concept-cards/templates/source-support.md`
   - `knowledge/concept-cards/templates/validation-result.md`
   - `knowledge/concept-cards/templates/verification-result.md`
6. representative populated records and their run context:
   - Project05 Arc07 Slice02 `candidate-cards/cc-emergent-explanation.md`
   - Project05 Arc07 Slice02 `candidate-cards/support-emergent-explanation.md`
   - Project05 Arc07 Slice02 `extraction-run.md`
   - Arc01 Slice01 baseline rich-rerun `README.md` and
     `candidate-cards/cc-complementary-learning-systems.md`
   - Arc01 Slice01 baseline teaching-rerun `README.md` and
     `candidate-cards/cc-memory-forms.md`

Resolve the Project05 and baseline paths from the frozen inventory/input
register; register the actual paths and authorities, not the shorthand above.

### Required-section

Read each complete named section and any definitions it directly depends on at
source authority `a5861c4b`:

- `knowledge/concept-cards/guides/01-load-contract.md`: “Construct Boundaries”
  and “Distinct Evidence And Lifecycle Signals”;
- `knowledge/concept-cards/guides/02-operator-workflow.md`: “Record Scope
  Before Deriving Content”, “Maintain The Construct Boundaries” and “Keep
  Results Separate”;
- `knowledge/concept-cards/guides/05-evidence-lifecycle.md`: “Place Each Signal
  On Its Actual Subject” and “Maintain Separate Lifecycle Records”;
- `knowledge/concept-cards/guides/08-validation-verification.md`: “Define The
  Review Before Checking”, “Make Each Result Recoverable” and “Prevent False
  Upgrades”;
- `knowledge/concept-cards/guides/09-memory-admission.md`: “Establish The
  Admission Scope” and “Record Admit, Reject Or Defer”;
- full `knowledge/concept-cards/references/record-field-groups.md` and
  `knowledge/concept-cards/references/vocabulary.md`.

These guides provide construct/authority boundaries. They do not currently
define a global `created_at` datatype or policy. Record any contrary text
precisely rather than forcing this reconnaissance conclusion.

### Required-data

From the frozen inventory at planning `5e6310b8`, execute complete projections
for:

1. all parsed records in the eleven assigned kinds, with path, kind,
   `created_at` key presence, raw value, parsed type and value state;
2. all malformed/no-frontmatter exclusions and their classifications;
3. all 2,054 Complete Musician/Erlang records for `created_at` presence, type
   and value without sampling;
4. all 25 populated assigned records, grouped by kind, value and source family;
5. the exact eleven Set D pairs against current/frozen accepted/remaining sets.

Retain commands, exit statuses, denominators and complete machine-readable
results or lossless partitions in allowed evidence files. Inspecting the data
projection is not a substitute for the required full witness reads.

Opening observations to reproduce, not silently rewrite:

| Kind | Total | Absent | Explicit null | Populated string |
| --- | ---: | ---: | ---: | ---: |
| claim | 1 | 0 | 1 | 0 |
| competency-question | 2 | 1 | 1 | 0 |
| concept-card | 31 | 9 | 1 | 21 |
| memory-admission | 2 | 1 | 1 | 0 |
| preservation-decision | 1 | 0 | 1 | 0 |
| reconciliation-result | 2 | 1 | 1 | 0 |
| relationship-edge | 2 | 1 | 1 | 0 |
| source-locator | 1 | 0 | 1 | 0 |
| source-support | 5 | 0 | 1 | 4 |
| validation-result | 1 | 0 | 1 | 0 |
| verification-result | 1 | 0 | 1 | 0 |

The 25 populated values are expected to be date-only strings: eight
`2026-09-11` and seventeen `2026-09-12`. No populated value is expected to
include time-of-day or timezone. Treat a discrepancy as a finding/blocker, not
permission to alter the expected result after seeing output.

### Conditional And Reference-only

- **Conditional:** load any additional witness in full before relying on its
  body semantics; record why it was needed and the complete extent.
- **Conditional:** if a cited evidence range falls outside a required section,
  load its complete containing section before using it.
- **Conditional:** inspect extraction-run start/finish, validation, verification,
  reconciliation, preservation or admission records only when comparing the
  distinct temporal event. Register the exact witness and do not turn the
  comparison into a new field assignment.
- **Reference-only:** Slice14-19 supporting packets are historical provenance,
  not current instructions. Load a specific artifact only to reuse a checked
  evidence convention or compare an accepted meaning; record the trigger.
- **Reference-only:** Slice21 and later family details. Do not create or analyze
  their semantic packet.

After intake, write a concise source-cited contract readback in your own words.
Cover exact scope, state distinctions, semantic/non-normative boundary,
committed replay, six-file fence and independent acceptance. The easily missed
constraint is that a date matching a run date does not make `created_at` the
run timestamp, and a template null does not establish requiredness.

## Binding Semantic Design

The following decisions define the evidence boundary; they do not prescribe an
unsupported final vocabulary.

### State and lexical precision

- Treat missing key, explicit YAML null and populated scalar as distinct.
- Preserve the scalar's parsed type and literal lexical form. For current
  values, record date-only day precision. Do not append midnight, timezone or
  UTC and do not normalize a date into a datetime.
- Do not interpret an absent key as unknown-null, or null as “not created.”
  Null is an unfilled template value unless stronger evidence says otherwise.
- Historical absence is bounded evidence about those records, not proof their
  creation time was unknown or that the old method lacked temporal provenance.

### Kind-local subject

Use this as a hypothesis to test against full context: `created_at` is a
record-local assertion about the containing record. Each effective meaning
must name that kind's containing construct and explicitly exclude nearby event
times. At minimum assess:

- claim record creation, not when the proposition became true or was first
  stated in the source;
- competency-question record creation, not first ask/answer or answerability;
- concept-card record creation, not concept origin, source publication or
  memory admission;
- memory-admission record creation, not decision, approval, effective admission
  or runtime write;
- preservation-decision record creation, not preservation action/effective
  date;
- reconciliation-result record creation, not reconciliation execution or
  decision time;
- relationship-edge record creation, not when the real-world relationship
  began;
- source-locator record creation, not source publication/snapshot/access;
- source-support record creation, not source-span authorship, evidence
  selection or validation;
- validation-result record creation, not validation execution/completion;
- verification-result record creation, not verification review/completion.

If evidence supports a narrower or different bounded meaning, report it. Do
not invent event equivalence merely to make the table uniform.

### Revision and run boundaries

Current populated concept cards include revision 1, 2 and 3 records. A single
date-only value does not decide whether it is first-record creation, current-
revision creation, extraction/generation date, file serialization date or an
immutable lineage timestamp. Record those alternatives and the evidence needed
to distinguish them. Do not infer that revision changes preserve or reset the
field.

Arc07 card/support values and their extraction-run context may share a calendar
date. Compare the actual fields, precision and authority. Record equality as an
observation only. `started_at`, `finished_at`, run references and README dates
do not become the authority for `created_at`, and `created_at` does not prove a
real run, successful extraction, support, review or acceptance.

### Consequences and owners

Every membership needs concrete consequences:

- **reader:** how to interpret present date, null and absence without false
  chronology;
- **extractor:** what raw state/lexical form to preserve and what not to fill;
- **query:** how to filter/sort only within known precision and distinguish
  missing/null/populated values;
- **migration:** what information must be retained and which normalization
  decisions remain blocked.

Assign unresolved normative datatype, precision, timezone, requiredness,
immutability, revision and migration policy to P-15/Arc02 or an explicitly
named source-guidance owner. Do not use “later” without the decision and owner.

## Required Outputs

Change exactly these six files:

1. `artifacts/semantic-membership.json`
2. `artifacts/semantic-evidence.md`
3. `artifacts/validation-evidence.md`
4. `artifacts/handoff.md`
5. `ledger.md`
6. `closing-report.md`

### `semantic-membership.json`

Create valid JSON with exactly eleven unique membership objects. Each has:

~~~text
field_path, record_kind, meaning_id, effective_meaning, applicability,
observed_states, evidence_ids, exceptions,
consequences.reader, consequences.extractor, consequences.query,
consequences.migration, unresolved_questions, disposition
~~~

Use an atomic `evidence_registry`. Every cited evidence ID resolves to exactly
one actual path in one declared root at one real Git authority, with one
lowercase SHA-256 and one validated numeric range or narrowly allowlisted whole-
document descriptor. Include:

~~~text
evidence_id, root, path, read_mode, authority_commit, sha256, role,
member_scope, source_range, interpretation, limit
~~~

No pseudo-paths, multi-file rows, multi-hash strings, working-tree-only files,
temporary projections or unresolvable authority labels. Shared evidence is
allowed only with an explicit member-specific applicability statement. Hash
and range identity are structural evidence, not semantic warrant.

### `semantic-evidence.md`

Include the intake/readback, complete 49-record matrix, 25-value projection,
lexical-precision census, malformed/excluded counts, 2,054-record historical
comparison, full-witness analysis and kind-by-kind meanings. Label documented
rule, template observation, populated observation, historical comparison,
author inference and unresolved question separately.

Include native temporal diagnostics that compare, without conflating:

1. absent vs null vs date-only string;
2. date-only vs datetime/timezone lexical shapes;
3. record creation vs Arc07 run start/finish;
4. record creation vs source publication/snapshot/access;
5. result/decision record creation vs execution, review, approval or effective
   time;
6. record revision vs creation-date semantics.

### `validation-evidence.md`

Contain one literal executable Bash replay route plus intake/readback and
recorded precommit/committed results. Use existing Bash, jq, Git, `shasum` and
ordinary shell tools only.

The production predicates must validate:

- exact 11-pair Set D, uniqueness, inclusion/disjointness and
  279/276/11/265;
- exactly eleven complete memberships and all required consequence fields;
- every evidence citation, authority, path, digest and range;
- complete native matrix, 25-value projection, exclusions and historical
  comparison against independently authored expectations;
- temporal diagnostic objects and all controls below;
- unchanged frozen/accepted predecessor evidence and exact six-file scope;
- JSON and whitespace checks.

The route must support working-tree precommit mode and committed mode. In
committed mode, load contribution bytes from `CC_COMMIT`, and execute recipe
bytes extracted from a distinct `REPLAY_COMMIT`. The outer invocation must
fail closed on missing, stale or foreign endpoints. Record exact statuses and
stderr presence; do not collapse a semantic comparison failure into an input/
Git/tool error.

Recommended executable spine, illustrative rather than tested drop-in code:

~~~bash
set -euo pipefail
: "${CC_COMMIT:?required}"
: "${REPLAY_COMMIT:?required}"
: "${SOURCE_COMMIT:=a5861c4b8e93af932d18cb571415cc570c758240}"

# 1. Retrieve registry/report bytes from CC_COMMIT, never the mutable tree.
# 2. Derive exact Set D from the preserved escalation JSON block and compare it
#    to coverage and registry with jq -n -e.
# 3. Retrieve every atomic evidence row using its declared root/authority/path;
#    check SHA-256 and range before using it.
# 4. Derive actual state/value and temporal diagnostic objects from pinned
#    inputs; compare with independent literal expected objects using jq -n -e.
# 5. Run negative candidates through the same predicates as positives.
# 6. Verify contribution scope, protected history/source and whitespace.
~~~

The distinct recipe endpoint may update only the same six authorized files.
After the contribution commit exists, create/select a separate recipe commit,
extract the fenced route from that commit, run `bash -n`, and execute it with
explicit `CC_COMMIT` and `REPLAY_COMMIT`. Preserve both endpoint hashes in the
closing report. Do not execute mutable working-tree recipe bytes as committed
acceptance evidence.

### `handoff.md`

Summarize the eleven bounded meanings, observed states/precision, temporal
non-equivalences, unknowns and decision owners. Preserve Slice21 and the 248-
pair complement. Explain what this evidence contributes to P-15/Arc02 without
adopting a schema. Keep repeated real extraction, Complete Musician trial,
conditional full-book work, runtime/memory and Operator quality acceptance
open.

### `ledger.md` and `closing-report.md`

Walk all seven rows. Use doer-attested evidence and proposed-done only. The
closing report inventories every output, changed path, commit, check,
exploratory failure, unrun requirement and bubble-up. Do not edit parent plans,
coverage or reviewer records.

## Implementation Sequence

1. Complete and record intake/readback. Stop on missing/truncated/conflicting
   binding input.
2. Derive exact Set D and opening accounting from preserved authority. Do not
   hand-author the registry before this check passes.
3. Project all 49 assigned records into a machine-readable state/value table.
   Compare against a separately authored expected matrix; do not derive actual
   and expected from the same function or registry.
4. Inspect full templates and representative populated bodies. Author all
   eleven meanings, consequences and unresolved owners from context.
5. Build native temporal diagnostic objects from actual witnesses, then compare
   them to independent literal expectations.
6. Register every evidence input atomically. Implement positive predicates and
   route every negative control through those same predicates.
7. Run precommit validation. Write handoff, ledger and closeout. Inspect staged,
   unstaged and untracked unions; commit only six explicit paths.
8. Create/select the distinct recipe endpoint and run the committed replay.
   Correct only within the six-file fence and report final endpoints.

## Discriminating Controls And Oracles

| Row | Positive observation | Required rejected alternative |
| --- | --- | --- |
| S20-1 | Exact Set D, no accepted overlap, 279/276/11/265 | duplicate, Set E/outside pair, changed denominator |
| S20-2 | Every cited evidence row resolves at one real authority/path/hash/range | dangling/duplicate ID, wrong path, authority, digest, range or whole-document descriptor |
| S20-3 | Exact 49-row matrix, 25 date strings, 3 YAML errors, 15 no-frontmatter, 2,054 historical absences | absent rewritten as null; null/date coercion; malformed/no-frontmatter treated as records; historical root injection |
| S20-4 | Eleven kind-specific bounded meanings with explicit temporal exclusions | one universal timestamp meaning; concept/source/event time; lifecycle or approval authority inherited |
| S20-5 | Date-only precision and record-local event remain explicit | date changed to datetime; timezone appended; run date copied as authority; decision/effective time accepted as equivalent |
| S20-6 | Precommit and distinct-endpoint committed replay pass | missing/stale/foreign endpoint, mutable-tree recipe, seventh path or protected-input change passes |
| S20-7 | Set E, complement, P-15 and UAT remain open | schema/source repair, successor opening, memory admission or quality acceptance claimed |

At minimum run these controls through the production predicates:

- remove a `created_at` key from a null template candidate;
- replace absence with explicit null and explicit null with a date string;
- change one populated date and change another date-only value to
  `2026-09-11T00:00:00Z`;
- make one populated value numeric;
- copy an extraction-run `started_at` value into a candidate as if it were the
  authoritative `created_at` observation;
- label validation execution, verification review or admission decision time
  as containing-record creation;
- add a Set E/outside pair and an already accepted pair;
- inject a historical `created_at` root;
- mutate one evidence authority, path, digest and range independently;
- inject one dangling and one duplicate evidence ID;
- query a nonexistent frozen inventory path or invalid Git authority and retain
  its tool/input error separately from a semantic no-match/comparison failure;
- invoke missing, opening/stale and foreign recipe endpoints.

Record actual nonzero statuses; require a particular status only where the
tool/wrapper contract guarantees it. A passing replay proves only its checked
properties; CRC still reviews semantic warrant independently.

## Stop Conditions

Stop and return to CRC without pretending completion if:

- exact Set D or opening counts differ;
- required material is unavailable, truncated or contradictory;
- evidence changes an accepted Slice14-19 meaning;
- a normative timestamp/revision/migration decision is required;
- atomic committed evidence or fail-closed replay cannot be achieved within
  the six-file fence;
- the assignment no longer fits with recovery headroom; or
- any source/schema/helper/runtime/package/extraction/UAT/memory edit appears
  necessary.

CRC escalates structural changes to CDC through the Operator. Do not silently
shrink scope, waive a check or continue into Slice21.

## Commit And Return

Use explicit filenames with `git add --` and `git commit --only --`. Never use
a directory pathspec, broad glob, `git add .`, `git commit -a` or unrelated
staged content. Every CC-authored commit must include exactly once:

~~~text
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>
~~~

Return:

- exact opening, source, CC contribution, recipe and final documentation
  commits;
- exact six changed paths;
- intake/readback pointer;
- exact set/accounting and full census/precision results;
- temporal diagnostics and every control result;
- per-row proposed-done attestation;
- all exploratory failures, required checks not run, deviations and unresolved
  questions;
- workload observations: model/effort if known, context recovery/compaction,
  elapsed time and correction categories.

CRC independent verification, CDC arc composition, P-15 schema/specification
discussion, repeated real extraction, UAT and Operator acceptance remain
separate gates.
