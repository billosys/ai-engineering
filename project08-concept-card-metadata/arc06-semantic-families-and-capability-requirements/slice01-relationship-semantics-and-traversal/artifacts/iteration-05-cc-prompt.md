# CC Correction: Arc06 Slice01 Iteration 05

Complete the remaining R1/R4 findings in CDC's review of 91c7f5f3.
Use a fresh task/session. The operator may select Luna/xhigh for this trial;
do not change model/settings yourself or infer them from this prompt. Record
actual settings if available, otherwise unknown. Criteria do not vary by model.

Source cwd: /Users/oubiwann/lab/billosys/ai-engineering (read-only).
Planning cwd: /Users/oubiwann/lab/billosys/ai-engineering/.worktrees/planning.
Project: project08-concept-card-metadata.

## Load And Credit

Read project AGENTS/plan/ledger, Arc06 plan/ledger, Slice01 plan/ledger, the
CURRENT Iteration 04 section of cdc-verification.md, and the five CC artifacts
plus close report. Use the current source framework/verification/concept-cards
routes. Prior correction prompts and earlier review sections are history.

S1-1, S1-4 and S1-6 are independently done. Keep exact 35 pairs, the 23 existing
registered inputs, all verified numeric table values, native-result objects,
negative controls and corrected historical contrast/provenance. Do not restart
Arc01, repeat full historical prompt reads, redesign the registry, add source
scope, or redo correct semantics merely because this is a fresh session.

## A. Finish The Missing Context And Reconciliation

1. Inspect/register ONE existing frozen teaching-rerun card and its body.
   Example: `workbench/compcogneuro-teaching-rerun-2026-09-12/candidate-cards/cc-memory-forms.md`.
   Establish both absent relationship-reference keys, actual relationship prose,
   real-extraction/candidate provenance and the operational loss boundary.
   Use an original or preserved copy with exact original/copy mapping and hash;
   do not add an extraction or vendored corpus. Do not infer stored edges,
   supported assertions or semantic verification from the prose.
2. Finish the selected-card census by context family: template, synthetic
   examples, Arc07 pilot/expanded outputs, rich rerun and teaching rerun.
   Preserve the 31 parsed-card denominator and three malformed-rich exclusions.
   Keep Music/Erlang counts and the two edge contexts as already reproduced.
3. Connect at least ONE already registered legacy null-field witness to its
   relevant body section. Explain why absent/null metadata is not automatically
   "no conceptual relation" and why prose must not become an invented slug edge.
   No extra legacy witness is necessary: data-type-sizes, sc-hbase-protocol or
   error-handling-philosophy already provides this contrast.
4. Replace the unsupported inference from observed key presence to
   "requiredness" with explicit observed behavior and bounded policy uncertainty,
   unless actual documented authority establishes a rule.
   Correct the Arc07 registry role to no-edge/future-CQ, matching the repaired
   report. Reconcile only affected member/shared roles, report and handoff.
   Preserve the existing accepted predicate/component distinctions.

## B. Publish One Complete Current Replay

Use frozen parsed values with registered original-input hash checks, or an
established YAML parser. Do not extend awk/grep windows as a YAML interpreter.
No new custom helper, Ruby/Python, source change or general validation framework.

Publish literal code that emits/checks ALL census table values and component
states, not just selected anchors. Separate selected pairs from corpus records.
The CDC legacy query is reusable with attribution; extend only for the required
card-context and edge-component outputs. Keep absent/null/empty distinctions
and actual typed values visible.

Make the current route independently compare expected AND observed objects for
ALL FOUR cases to values derived from actual inputs, retaining actual bounded
target existence and support searches. Include the same wrong-endpoint and
wrong-revision expectation controls against unchanged inputs. The Iteration 04
objects are correct; preserve their successful behavior. Align operation text
with executed code; declared symmetric lookup is an adapter rule, not a
discovered extra relation. Historical blocks may remain labeled history, but
the current contract must not require splicing old and new blocks.

Retain fixed historical preservation 91db42fa -> a24758b4 and
3cf075ff -> 91c7f5f3. Record this run's actual opening head, honest pre-commit
checks and committed-review route. Do not count CDC edits as CC scope.
Run the complete CURRENT route after final edits: exact sets/accounting,
both evidence-reference layers, all registered hashes, all census outputs,
four cases and controls, JSON, whitespace, preservation and exact commit scope.

## Finish Or Size Explicitly

Walk all seven original rows and current R1/R4 findings. Retain CDC-done rows
without claiming the remaining four are independently accepted. Return an
attested close only if the whole remaining contract is met.

This is Iteration 05, not a new slice or a reset of the iteration budget.
If incomplete, preserve completed evidence and return a concrete sizing
proposal: exact remaining criteria/files, why another bounded unit is needed,
dependencies and acceptance checks. Do not issue a sixth iteration, open
Slice02, quietly transfer requirements or claim completion by exhaustion.

## Exact Edit And Commit Scope

Only these seven existing planning-relative files may change:

- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/semantic-membership.json
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/semantic-evidence.md
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/query-cases.json
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/validation-evidence.md
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/handoff.md
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/ledger.md
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/closing-report.md

Inspect staged changes and use every actual intended filename explicitly in
BOTH staging and git commit --only -- commands. Preserve unrelated edits.
Include the required trailers:
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>

Do not edit this prompt, CDC artifacts, parent plans, workload observations,
baselines, source skills/packages or runtime surfaces. No new extraction.
Return commit, actual checks and remaining limits; CDC decides closure.
