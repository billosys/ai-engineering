# CC Correction: Arc06 Slice01 Iteration 02

Start this in a NEW CC task/session, not a continuation or history-bearing fork
of "Read metadata research instructions". The prior repair reused that task.
Keep the current model/effort unless the operator changes them. Read this packet
afresh; do not create another task yourself. This prompt opens only the existing
35-pair Slice01 correction, not Slice02, a new extraction or final-profile design.

## Contract And Read Order

Source cwd: /Users/oubiwann/lab/billosys/ai-engineering.
Planning cwd: /Users/oubiwann/lab/billosys/ai-engineering/.worktrees/planning.
Project: project08-concept-card-metadata.
Record actual heads, dirty state, tools and available model/effort metadata.

Read project AGENTS/plan/ledger, Arc06 plan/ledger, Slice01 plan/ledger, then
the CURRENT Iteration 01 review in cdc-verification.md and its prior findings.
Read the five CC artifacts and closing report. Use the source collaboration,
project-management, work-verification and concept-cards routes prescribed by
the plans. Read the project semantic-family assessment and transition register.
Use bounded reads with enough headroom for evidence inspection and final replay.

The review of 904a5a0d retains correct population totals, fourteen hashes,
exact membership, scoped commits and the shared legacy assertion correction.
Do not redo accepted Arc01 semantics or mechanically rewrite the 555-pair map.
All seven criteria and original required contexts remain in force. The three
work units below organize the repair; they are not reduced acceptance criteria.

## 1. Contextual Evidence Before Conclusions (R1)

Preserve a literal selected-field census from frozen native values. Distinguish
Music, Erlang, Arc07 pilot/expanded, rich/teaching reruns, current templates and
synthetic examples. Cover the selected roots and nested members, with presence,
null, empty, type/shape and values. Reuse common evidence rather than writing a
separate paragraph for every cell. The 35 membership count and the 2,054/31/2
population counts are not a full field census.

Choose named populated and absent/empty/anomalous witnesses from that census,
including actual generated-card bodies. Register precise locations, hashes and
roles. Preserve three malformed rich-card limitations. When using baseline
copies, reuse the accepted original/copy mapping and register the inputs actually
used; do not silently replace frozen evidence with current files.

Read both complete v3.2 prompts in manageable sections. Relevant quotations
alone are not the requested full comparison. Attribute reused prior inspection
if available; never assert unseen reading. Do not execute historical recipes.

Trace native requested and declared target components separately:
- The populated edge requests endpoints cc-prepared-source-provenance and
  cc-claim-support-is-assertion-specific at revision 1. Inspect the matching
  minimal-card.md and claim-backed-card.md declarations.
- Its support-synthetic-edge-001 reference has no declared path. An ID-derived
  filename is a declared adapter candidate, NOT a native path. Search a named
  bounded record scope for declarations; retain what that search can establish.
- The rich card references a DIFFERENT edge. Its own support reference names
  support-evidence-map-definition-001. Do not attach the populated edge example's
  support to it, and do not treat card support as its edge's warrant.
- A missing rich edge leaves that edge's own support declaration unavailable.
  Distinguish not found in searched scope from unavailable, unperformed,
  unspecified path/revision and observed matching declarations.

Do not search the whole machine. Existing scoped roots suffice. No source
semantic verification, corpus edits, placeholder targets or runtime service.

## 2. Compute Native Diagnostic Results (R2)

Keep at least four diagnostics with actual input evidence and literal executable
operations. The current cases may be replaced or expanded as necessary, without
adding frozen memberships. Required behavior:
- A real prerequisite list yields the documented reversed edge orientation.
- A populated extends list yields outgoing extension plus the inverse reading.
  There are 519 populated legacy lists; inspect a suitable pair. Keep the empty
  list case as a separate negative when useful, not the only extension test.
- A real related/contrast assertion supports the declared symmetric lookup view
  from either endpoint without inventing a second assertion or filling prose
  gaps. Keep actual target absence separate from representation-level traversal.
- A current native edge/card-reference case compares endpoint identity/revision
  and unavailable support/target outcomes with the correct owning record.

Read native frozen values or parse actual registered frontmatter with established
tools. Derive observed results from those values and actual lookups, then compare
the computed structures against expected results. Do not merely assert that
handwritten observed fields contain the expected strings. Do not make expected
equal to computed output by construction. Record adapters, search boundaries,
actual outputs, missing operations and limits. These are bounded diagnostics,
not equivalent extraction runs or proof of final graph behavior.

Reuse jq/shell and established parsers. No new helper, Ruby or Python.
Tiny explicitly synthetic orientation fixtures remain allowed where justified,
but do not replace available real populated legacy instances.

## 3. Reconcile The Packet And Execute Every Current Recipe (R3/R1/R2)

Keep legacy lists as typed relationship assertions, not just navigation.
Preserve common-confusion semantics of historical contrasts_with in the
registry's effective meanings and consequences, alongside the current guide's
qualified-comparison meaning. Do not silently broaden either.

Connect the registered template/minimal/rich/generated witnesses to applicable
shared meanings and member roles. Reconcile the edge-ref group so it includes
both observed reference names without calling its own member an unrelated
collection. No arbitrary number of groups or paragraphs is required.

Update all five artifacts and row attestations consistently. Remove misleading
current claims, retaining superseded material as clearly labeled history.
Block 2 currently fails because case keys changed; fix its contract.
Run EVERY published current block independently, from its declared cwd, after
the last edit. Preserve failures and their fixes honestly. Check exact plan set,
frozen inclusion, accepted disjointness, both evidence layers, every input hash,
full selected-field census, computed cases, JSON, whitespace and scope.

Preservation: retain fixed 355d037f -> 400b847a and 46f60e46 -> 904a5a0d history.
Record this repair's actual opening head after CDC edits. Compare authorized
changes against that head; do not attribute CDC planning changes to CC.
Keep 115 accepted / 35 assigned / 405 outside until independent CDC acceptance.

Before proposed-done, walk all seven ORIGINAL ledger criteria and every current
review subfinding. Distinguish a reproduced result from an asserted read or
untested diagnostic. If substantive work remains and cannot fit with headroom,
stop with completed evidence plus a concrete remaining-work/sizing proposal.
Do not reset iteration history or claim that passing hashes closes semantics.

## Exact Edit And Commit Scope

Only these seven existing files may change:
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/semantic-membership.json
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/semantic-evidence.md
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/query-cases.json
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/validation-evidence.md
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/artifacts/handoff.md
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/ledger.md
- project08-concept-card-metadata/arc06-semantic-families-and-capability-requirements/slice01-relationship-semantics-and-traversal/closing-report.md

Inspect the staged diff. Enumerate those actual files in BOTH staging and
git commit --only -- <filenames>; never stage a directory or use commit -a.
Include both trailers:
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>

Do not edit reviewer artifacts, this prompt, parent plans, source skills,
packages, installed skills, accepted packets, baselines or workload records.
Return the commit and truthful criterion/finding dispositions. CDC remains
the closure gate. The operator will hand off the next task.

