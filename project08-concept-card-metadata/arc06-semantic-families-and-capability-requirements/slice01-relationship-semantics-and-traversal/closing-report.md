# CC Proposed-Done: Arc06 Slice01

Status: CC proposes completion after Iteration 04, pending independent CDC
review. This supersedes the Iteration 03 attestation, not the retained review
history or CDC's authority. CDC's Iteration 03 reproduction of S1-1 and S1-6
is retained; this packet does not claim that CC attestation itself closes the
other rows.
This is not operator acceptance and does not change the transition's 115
accepted pairs.

| ID | Attested result | Evidence |
| --- | --- | --- |
| S1-1 | Plan-derived exact 35 members are in the frozen remaining 440, disjoint from 115 accepted; 405 remain outside assignment. | semantic-membership.json; validation-evidence.md |
| S1-2 | Literal selected-field census distinguishes presence, absent, null, empty, type/shape, values and nested components across the 2,054/31/2 populations; four direct Erlang state witnesses and both real-extraction candidate contexts are registered. | semantic-membership.json; semantic-evidence.md; validation-evidence.md |
| S1-3 | Legacy lists remain typed assertions; common-confusion contrast, predicate orientation, representation, resolution, revision and warrant are distinct, including observed legacy null/absent states and corrected candidate provenance. | semantic-membership.json; semantic-evidence.md |
| S1-4 | Native endpoint request and target declarations compare identity and revision separately for both endpoints; unresolved rich declaration and pathless edge support remain separately bounded. | semantic-evidence.md; query-cases.json; validation-evidence.md |
| S1-5 | Four actual structured replays cover prerequisite, populated extension, reciprocal symmetry and unavailable edge-support declaration; the last two now compare complete native-derived structures and reject wrong expectations. | query-cases.json; validation-evidence.md |
| S1-6 | Handoff keeps 405 pairs and Slice02 outside this packet and names CQ/resolver/research re-entry questions. | handoff.md |
| S1-7 | Literal replay computes native outcomes, rejects wrong symmetry/target-revision expectations, validates all twenty-three registered hashes, and records both pre-commit and committed-review routes honestly. | validation-evidence.md |

Artifact inventory: `semantic-membership.json`, `semantic-evidence.md`,
`query-cases.json`, `validation-evidence.md`, and `handoff.md`. Scope specified
and delivered is the exact 35-pair table only. No source, schema, extraction,
corpus, runtime, package, baseline, parent-plan or CDC artifacts were changed.

Key findings: legacy relation lists are typed relationship assertions that also
support reader navigation, but are not independently identified current edge
records or source-supported edges. Their observed null/absent/empty states are
not interchangeable. Directed edge metadata separates meaning, endpoints,
support and closure; `relationship_type` versus `relation_type`,
`relationship_edge_refs` versus `relationship_refs`, target anchor, and
revision questions remain unresolved rather than normalized. The Arc07 and
rich-rerun cards are real extraction candidates, not verified/admitted outputs;
one explicitly has no edge and a future CQ, while the other names body-level
candidates with an empty stored list. CDC must reproduce the evidence before
any ledger closure.
