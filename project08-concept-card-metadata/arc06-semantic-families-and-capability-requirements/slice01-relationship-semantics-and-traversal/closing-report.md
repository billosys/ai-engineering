# CC Proposed-Done: Arc06 Slice01

Status: CC proposes completion after Iteration 03, pending independent CDC
review. This supersedes the Iteration 02 attestation, not the retained review
history or CDC's authority.
This is not operator acceptance and does not change the transition's 115
accepted pairs.

| ID | Attested result | Evidence |
| --- | --- | --- |
| S1-1 | Plan-derived exact 35 members are in the frozen remaining 440, disjoint from 115 accepted; 405 remain outside assignment. | semantic-membership.json; validation-evidence.md |
| S1-2 | Census of 2,054/31/2, named legacy/current/generated contexts, absent/empty/template limits, and visible full v3.2 prompt reading are attested. | semantic-membership.json; semantic-evidence.md |
| S1-3 | Legacy lists remain typed assertions; common-confusion contrast, predicate orientation, representation, resolution, revision and warrant are distinct. | semantic-membership.json; semantic-evidence.md |
| S1-4 | Native endpoint declaration matches, bounded legacy filename lookups, unresolved rich declaration, and pathless edge support are separately reported. | semantic-evidence.md; query-cases.json |
| S1-5 | Four actual structured replays cover prerequisite, populated extension, reciprocal symmetry and unavailable edge-support declaration. | query-cases.json; validation-evidence.md |
| S1-6 | Handoff keeps 405 pairs and Slice02 outside this packet and names CQ/resolver/research re-entry questions. | handoff.md |
| S1-7 | Literal replay computes native outcomes, includes a wrong-target negative control, validates all registered hashes, and labels preservation endpoint honestly. | validation-evidence.md |

Artifact inventory: `semantic-membership.json`, `semantic-evidence.md`,
`query-cases.json`, `validation-evidence.md`, and `handoff.md`. Scope specified
and delivered is the exact 35-pair table only. No source, schema, extraction,
corpus, runtime, package, baseline, parent-plan or CDC artifacts were changed.

Key findings: legacy relation lists are typed relationship assertions that also
support reader navigation, but are not independently identified current edge
records or source-supported edges. Directed edge metadata separates meaning,
endpoints, support and closure; `relationship_type` versus `relation_type`,
`relationship_edge_refs` versus `relationship_refs`, target anchor, and
revision questions remain unresolved rather than normalized. CDC must reproduce
the evidence before any ledger closure.
