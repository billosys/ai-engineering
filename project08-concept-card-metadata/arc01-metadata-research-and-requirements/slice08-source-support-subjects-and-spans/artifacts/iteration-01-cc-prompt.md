# CC Iteration 01: Complete Slice08 Reference And Semantic Evidence

Read ../cdc-verification.md, the original cc-prompt.md and its required load
set, current plans and ledger. Correct S8-R1/R2/R3 within the same 27 pairs.
S8-2/S8-4/S8-7 are CDC-reproduced; preserve and recheck them when affected.

## Required Corrections

1. Inspect all four supports' subject/source/snapshot/locator references against
   their actual targets. In semantic-evidence.md, add a bounded matrix (deduplicate
   targets with an explicit referring-record list where useful) recording
   requested ID/type/revision/path/fragment, target declarations and resolved,
   inconsistent or unknown components. Distinguish finding an ID in text/table
   from resolving a literal fragment under a named consumer convention. A
   card's revision or a referring claim's requested revision is not automatically
   an independently declared embedded-claim revision.
2. Register and use the actual concept-cards SKILL.md reference rules and relevant
   focused guidance. Inspect the acquisition record's declared commit versus
   source ID, prepared-manifest identity/revision versus snapshot role, and
   locator-map IDs versus fragments/revisions. Preserve any uncertainty or
   mismatch; do not fix the pilot files, infer identity declarations, or force
   a universal Markdown anchor convention. A diagnostic result may legitimately
   be unresolved. This does not authorize a new source-support verdict.
3. Correct the registry itself. Keep the distinct supported definitions; separate
   general snapshot/reference semantics from observed prepared-manifest usage
   and the pilot-specific ID. Replace the identical disposition repeated across
   27 entries with actual field/context consequences. Shared cautions can stay
   factored, but explain the consequences of losing subject revision, separating
   span selections, dropping context/quote policy, confusing notes with checksums,
   and unresolved references where each applies. Do not add unsupported schema
   rules. Keep meaning/member layers aligned and deduplicate repeated evidence IDs.
4. Complete the bounded historical comparison using the actual music/OTP cards
   already registered in Slice07 as starting evidence. Inspect and register
   relevant body citations, Source Reference and Verification Notes alongside
   the historical guide. Explain what those actual records carry, what the
   explicit subject/span representation adds, and what neither establishes.
   Preserve historical intent versus observed values; don't infer global absence
   or query equivalence from one sample or a guide.
5. Extend validation-evidence.md with literal executable reference/target and
   historical/context inspection routes, all newly registered hashes, existing
   set/census/reference-layer checks and honest preservation comparisons.
   Resolving two commit names is not a diff. Use a fixed entry/delivery comparison
   for earlier committed work (d977cb31 to 2dfe5577) and a clearly labeled
   current-state check for this repair before commit; do not demand unchanged
   prior plans across earlier authorized CDC updates. Run every block literally
   and record actual outcomes, including unresolved-target findings.
6. Update handoff, ledger and closing report with each finding's disposition,
   each of the seven original rows, four-artifact inventory and honest
   scope-as-specified/delivered reconciliation. Leave unmet criteria open.
   Do not use path existence/hash success as a substitute for identity analysis.

These are corrections to required evidence, not a new target-repair or schema
implementation slice. No source edits, pilot/baseline changes, new extraction,
helper, package/install, runtime or semantic claim verification. Existing
tools/parsers suffice; no Ruby/Python helper. Raise a concrete scope/sizing
issue before expanding work.

## Explicit Commit Scope

Only these six files under
project08-concept-card-metadata/arc01-metadata-research-and-requirements/slice08-source-support-subjects-and-spans/
may be changed and committed:

- artifacts/semantic-membership.json
- artifacts/semantic-evidence.md
- artifacts/validation-evidence.md
- artifacts/handoff.md
- ledger.md
- closing-report.md

Enumerate each actual full planning-relative filename in both staging and
git commit --only -- <filenames>. Inspect the staged diff and preserve unrelated
work. No directories, globs or commit -a. Do not edit CDC verification, plans
or prompts. Include both trailers:

~~~text
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>
~~~

Return commit, concrete reference/semantic findings, executed checks and limits.
The 67 accepted / 27 assigned / 461 other pairs, original criteria, parent/
research gates and P-14 remain unchanged. CDC independently decides closure.
