# CC Assignment: Project08 Arc01 Slice01

## Current Assignment: Iteration 01

The initial run below has been reviewed and requires changes. Read
`cdc-verification.md` and execute `artifacts/iteration-01-cc-prompt.md`.
The operator requires a Fennel replacement for the untracked Ruby helper.
Do not stage or commit any packet or iteration files. The operator's hold
supersedes the historical commit instructions below.

## Initial Assignment (Retained Context)

Read `../../project-plan.md`, `../../ledger.md`, `../arc-plan.md`,
`../ledger.md`, `slice-plan.md`, `ledger.md`, and `artifacts/planning-brief.md`
relative to this slice directory. Read Project08 `AGENTS.md` and the current
source collaboration-framework, project-management wayfinder, work-verification
and concept-cards routes before beginning. Use the source checkout for skills;
installed copies and old Project05 version labels may be stale.

Execute the slice plan fully: inventory historical and current metadata, trace
information and query capabilities, preserve the ignored workbench comparison
inputs, and produce the named research handoff artifacts. Parse structured data
with a YAML parser. Read both v3.2 prompts in full. Inspect actual field values
and representative bodies; do not stop at a key-name comparison or infer
capability preservation from generic guide wording.

Source checkout: `/Users/oubiwann/lab/billosys/ai-engineering` on `main`.
Planning checkout: `/Users/oubiwann/lab/billosys/ai-engineering/.worktrees/planning`
on `planning`. Verify these live. This slice authorizes planning/evidence writes
only in this slice directory: the five named output reports, baseline evidence
copies, ledger updates and closing report. The supplied planning brief is an
input. If reproducibility needs a small helper, put it under this slice's
`artifacts/` and enumerate it in the evidence and commit scope.

Treat 4.8.1 as the expected baseline and 4.9.x as Project08's intended delivery
range. Record actual source revision and hashes. Do not edit skill versions,
historical prompts, existing cards, source files or other projects in this slice.
Keep historical labels as observed and explain their relationship to current
release lineage only where evidence establishes it.

Produce `artifacts/input-register.md`, `artifacts/metadata-inventory.md`,
`artifacts/capability-crosswalk.md`, `artifacts/research-agenda.md`,
`artifacts/validation-evidence.md` and the registered baseline copies. Every
discovered field needs a disposition, including fields absent from the operator's
examples. Report malformed/inaccessible input, missing references, real semantic
loss, and uncertain interpretation. Additional research or a split slice is
preferable to quietly shrinking the inventory.

Validate using the slice plan and ledger, record commands/results, update each
ledger row with evidence strength, and write `closing-report.md` as CC
proposed-done. Include findings, limits, changed assumptions, per-row disposition,
and the next research needs for Arc01. Do not write `cdc-verification.md` or
declare the arc/project closed. Do not run extraction or select the final schema.

Historical commit procedure, currently suspended by the operator:
commit only your delivered slice files on planning after inspecting the diff and
staged state. In `git add` and `git commit --only --`, explicitly enumerate the
five report filenames, each copied baseline/helper filename, this slice's
`ledger.md`, and `closing-report.md`. Expand the actual list in the command;
no directory pathspecs, globs, `git add .`, `git add -A`, or `git commit -a`.
Preserve unrelated staged files. Include one contiguous trailer block:

```text
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>
```

Return the planning commit, artifact locations, validation summary and unresolved
findings, with independent CDC verification explicitly pending.
