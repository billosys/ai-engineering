# Slice04 Iteration 01: Correct And Ground The Semantic Packet

## Continuation After The Status Correction

Commit `41df3361` corrects the handoff and closing-report status only. CDC
accepts that correction; it does not complete this assignment. Continue this
same Iteration 01 with the substantive work below, starting from the current
checkout. Another status-only report is not the requested semantic repair.
Correct ownership according to evidence; the 303/252 counts are not frozen.
If capacity is the blocker, report concrete completed/remaining family work
and a bounded sizing proposal, not merely a repetition of the review findings.

Use a fresh CC context. Read Project08 AGENTS.md, the current project/arc/slice
plans and ledgers, this slice's `cdc-verification.md`, and the original
`cc-prompt.md` load set. Repair commit `76d284f4`; preserve its history.
This is the first corrective iteration of Slice04, not another Slice01 run.

CDC reproduced exact mechanical coverage: 303 assigned + 252 remainder =
555 unique expected pairs, zero missing, extra or duplicates. Keep that
coverage, not those ownership counts. The semantic work itself is incomplete.
Do not describe the initial packet as accepted or open Slice05.

## 1. Correct Ownership From Actual Meanings

Address S4-R1 across the full map, not only its examples. CQ coverage cannot be
a fallback category for unmatched fields. Review each member against its
record definition, actual values and the accepted Slice04/05 division.

- Run/check `actual_coverage`, verifier/reconciler identities and admission/
  reconciliation decisions are not CQ coverage. Assign their relevant
  lifecycle/provenance families to Slice05 with reasons.
- Locator `representation`, `numbering_basis`, `range_convention` and
  mapping/context fields require source-coordinate meanings, not pedagogical
  classification. Relation direction belongs with graph meaning.
- Source support's `subject_ref` may target a claim, edge or CQ assertion;
  do not redefine it as CQ-only because one observed target is a CQ.
- Distinguish record identity, performer identity, target/reference identity,
  revision, state, synthetic/example markers and context-dependent uses.

Write explicit intended families and reasons for the Slice05 remainder.
Shared groups with explicit member lists are allowed if their meanings truly
match. Do not implement Slice05 semantics to conceal an ownership error, or
move unresolved in-scope work there merely to make this slice appear complete.
No new fixed family count or 303/252 quota is imposed.

## 2. Author The Evidence, Not Just Family Tags

Address S4-R2. Read the original required full v3.2 prompts and relevant
predecessor differences, real examples from each assigned corpus, and actual
current definitions. Record what was inspected and what could not be settled.
Do not assert that the previous packet demonstrates reads it did not document.

Every assigned contextual membership must resolve to its actual meaning,
subject, relevant value/shape distinctions, evidence and old/current disposition.
Definitions and evidence may be shared by genuinely equivalent members; use
explicit contextual overrides where they differ. A family ID pointing to a
generic paragraph is insufficient. Do not generate repetitive "losing X
prevents querying X" text as analysis.

Use named record paths and actual values/body sections, plus exact definition
anchors. Treat `untyped` as a mechanical bucket; distinguish music, Erlang
and any other actual roles/generations where semantics differ. Explicitly
document evidence for any equivalence you retain.

Correct the demonstrated conclusions:

- Null does not by itself mean inapplicable. Preserve unknown/unfilled/
  unavailable/inapplicable distinctions and record unresolved meanings.
- A source reference does not prove author/title/chapter/page values were
  preserved elsewhere. Trace the actual destination or state the preservation
  gap/uncertainty. Distinguish capability from observed migration.
- Prerequisite depth is not a page/chapter locator. Preserve the original
  meaning even when the generated card is inconsistent with instructions.
- Alias key presence is not full equivalence of authority-control behavior.
- Flat prerequisite lists and typed edges need a concrete direction/lookup
  comparison; richer record structure does not alone prove easier traversal.

Provide real body/metadata and query/migration consequences across all assigned
families, including preserved, missing, ambiguous and changed behavior as
actually observed. The review's accent-types example is a counterexample, not
a substitute for the full required representative comparison set.
Keep future standards/schema choices in the research questions, not in
purported observations. Do not verify the source books' claims in this slice.

## 3. Reproduce And Reconcile The Packet

Address S4-R3 with an exact inspected-input register: paths, sections, file
hashes, revisions where available, actual execution planning/source bases,
sample records, full-prompt observations, predecessor differences and limits.

Use the CDC expected-versus-actual pair comparison or stronger equivalent.
Add fail-fast assertions for missing/extra/duplicate pairs and dangling family/
evidence references. Check both JSON files and the remainder's ownership
contract. Record actual commands, outputs and statuses for structural checks
separately from authored semantic review. Inspect null/empty/container cases
and finer-context treatment; total counts alone are not acceptance.

Refresh the handoff from the corrected map. Explicitly retain Slice05's
lifecycle work, final semantic/mechanical join, literal nine-root replay,
established-JSON/closing-delimiter-at-EOF instruction disposition, and original
Slice01 register/crosswalk/research/close-packet reconciliation. These remain
future execution under the existing plan, not completed work.

## 4. Close Honestly And Commit Narrowly

Address S4-R4: update the original eight ledger rows with attested evidence and
write a complete CC proposed-done report with a row walk, actual validation
results, explicit delivered-file inventory and scope-as-specified/delivered
bubble-up. Preserve CDC's independent status and prior findings.

Write only the existing six CC artifacts, this slice's ledger/closing report
and genuinely necessary small validation helpers. Read-only: source skills,
historical corpus, Slice01 artifacts, plans, CDC verification and CDC prompt.
No Ruby/Python helpers; use approved Fennel and established parsers if needed.
No source/package/install work or new full census for unchanged inputs.

If the semantic work cannot be completed with review headroom, return the
specific incomplete families/criteria for a tracked sizing decision. Do not
submit another superficial all-covered conclusion.

Commit actual enumerated files only, preserving unrelated changes, with both
repository co-author trailers. Report the commit, corrected ownership counts,
concrete semantic findings, checks and remaining limitations. Independent CDC
review, Slice01 composition and later research remain separate gates.
