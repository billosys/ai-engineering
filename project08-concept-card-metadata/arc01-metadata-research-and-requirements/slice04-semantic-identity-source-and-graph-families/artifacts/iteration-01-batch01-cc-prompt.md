# Slice04 Iteration 01: First Identity-Evidence Checkpoint

## Required Completion After Review Of 776c5cb1

This same batch remains active. CDC verified the exact ten-pair projection,
the four supplied hashes and the useful distinction between legacy filename
slugs and current record identity. Preserve those results. Complete these
missing requirements in the existing two batch artifacts:

1. Add an inspected card from each missing set: design-scale-erlang-otp,
   the captured rich rerun and the captured teaching rerun. Inventory population
   references do not substitute for their actual values/body comparisons.
   Use a usable rich card; three malformed records do not invalidate its
   entire population. Preserve and explain any malformed evidence inspected.
2. Register exact paths, hashes, sections and revisions for all inspected
   records/definitions, not abbreviated names. Record observations from the
   extraction prompt and the predecessor comparisons, not only their existence.
3. Make every meaning/evidence ID resolve. Seven JSON IDs do not match the
   report's named meanings, including `aliases-current`, `record-revision`
   and `title-display`. Evidence tags such as `current-template` need
   explicit registered targets. Supply contextual distinctions or evidence
   for equivalence, including the untyped music/Erlang cases and alias lists
   versus their members. No particular registry format is imposed.
4. Provide explicit dispositions and traceable comparisons for every member,
   directly or via a documented shared meaning. The report says the JSON
   includes dispositions, but none of its entries does; make the representation
   and its description agree.
5. Record and run literal validation commands with outputs and exit statuses:
   exact expected-pair comparison, definition/evidence reference checks,
   input hashes, unchanged original partition/baselines and whitespace.
   `jq parses it` without a command or result is not the requested record.

Update partial-status evidence without claiming batch acceptance or slice
closure. Do not shrink this already bounded batch, add a new helper framework
or deliver only another status correction. The remaining instructions and
five-file write scope below still apply.

Produce the evidence-backed work below in a fresh context. The need for a
rewrite is established and authorized; this assignment is not another audit
of whether a rewrite is needed. It is a deliberately small starting unit.

## Read And Establish Inputs

Read Project08 AGENTS.md, project/arc/slice plans and ledgers, this slice's
CDC review, and the full Iteration 01 correction prompt. Use the current
source collaboration-framework, work-verification and concept-cards routes.
Source: `/Users/oubiwann/lab/billosys/ai-engineering` on `main`.
Planning: its `.worktrees/planning` checkout on `planning`.
Record actual commit bases/statuses; baseline source is `e763c661`.

Read both v3.2 prompts in full from the planning checkout:

- `old/dev/concept-cards/0009-howto-concept-card-extraction-with-claude-code-v3.2.md`
- `old/dev/concept-cards/0010-a-guide-for-parallel-concept-card-re-extraction-v3.2.md`

Inspect identity/label differences in the two predecessors:

- `old/dev/concept-cards/0007-howto-concept-card-extraction-with-claude-code-v3.1.md`
- `old/dev/concept-cards/0008-a-guide-for-parallel-concept-card-re-extraction-v3.1.md`

Inspect the current concept-card template, relevant entrypoint/guide/reference
definitions and actual concept-card examples. Use the preserved Slice01
inventory and baseline paths to select at least one real card from each of:
Complete Musician, design-scale-erlang-otp, Arc07, the rich rerun and the
teaching rerun. They are evidence of generated representation, not proof of
what their source books say. Preserve malformed YAML and identify its limits.

## Exact First-Batch Boundary

Analyze these ten observed pairs. The `untyped` label is mechanical: separate
music/Erlang and other observed contexts when their meanings or shapes differ.
Additional contextual entries may map to the same mechanical pair.

| Normalized path | Observed kind |
| --- | --- |
| aliases | concept-card |
| aliases | untyped |
| aliases[] | concept-card |
| aliases[] | untyped |
| concept | untyped |
| concept_slug | concept-card |
| id | concept-card |
| revision | concept-card |
| slug | untyped |
| title | concept-card |

Use actual evidence to determine meaning and equivalence. Do not infer that
slug and ID are interchangeable, that record revision is a source edition,
that a displayed title is a persistent identity, or that alias presence proves
all prior authority-control behavior was retained. Distinguish null placeholders,
missing values, populated values and invalid YAML; do not invent equivalence.

## Deliver Actual Analysis

Create two partial artifacts in this slice's `artifacts/`:

1. `batch01-identity-evidence.md`: an inspected-input register with exact
   paths/sections, SHA-256 hashes, revision identities and availability;
   substantive observations from both full prompts and the relevant
   predecessor comparison; contextual meanings and explicit old/current
   dispositions; named real values and body sections; concrete lookup/
   migration consequences; unresolved questions; and executed validation
   commands, outputs/statuses and limitations.
2. `batch01-identity-membership.json`: explicit memberships for the ten
   mechanical pairs, with finer corpus/role contexts where needed, linked
   to the corresponding authored meanings and evidence. Document the
   artifact shape in the Markdown report. Shared definitions are allowed
   when justified; generic family tags are not sufficient.

For each meaning, distinguish what instructions require, what templates can
represent and what the inspected cards actually contain. Give the relevant
definition and real-record evidence anchors. Where absence or uncertainty is
the conclusion, name the inspected scope; do not silently fill it with an
inference. Evidence may support equivalence or reveal a difference.

Complete the analysis before the handoff summary. This is not a retagging of
the old six-family map. Leave that map and the original 555-pair partition
unchanged as unaccepted comparison inputs. The new batch is a partial authored
replacement that will require explicit integration after review.

## Verify The Batch And Stop For Review

Verify that the unique path/kind projection is exactly the ten pairs above,
all occur in Slice01's mechanical index, and any multiple contextual entries
have an explained distinction. Verify every authored meaning/evidence reference,
input hashes and cited locations. Parse the JSON with an established parser
and record actual checks separately from semantic judgments.

Check the unchanged full partition and source/baseline inputs using Git or
hash comparisons; do not rerun the full corpus inventory. Run planning
whitespace checks. No source-skill changes, extraction, package/install work,
new parser framework, Ruby/Python helper or runtime action is authorized.
Use existing tools; a new validation helper is not needed for ten pairs.

Stop after this batch for independent review of its evidence structure and
analysis. Report concrete findings and any specific obstacle encountered.
If an input is unavailable, identify the exact input and affected claims;
do not convert that into a blanket refusal to analyze accessible material.

## Status And Explicit Commit Scope

Update the existing `ledger.md`, `artifacts/handoff.md` and
`closing-report.md` with partial-progress evidence. Keep unmet rows open
and preserve CDC's scoped S4-8 result. The report must say **batch delivered,
Slice04 still changes-required**, not slice proposed-done. It should enumerate
the ten covered pairs and the remaining scope; no semantic claim is made for
the other 545 pairs, including the existing Slice05 allocation.

The only permitted writes are these five files under the current slice:

- `artifacts/batch01-identity-evidence.md`
- `artifacts/batch01-identity-membership.json`
- `artifacts/handoff.md`
- `ledger.md`
- `closing-report.md`

This explicitly supersedes the full correction prompt's write list for this
checkpoint only. Leave plans, prompts, CDC review and earlier evidence intact.
Commit these actual files with individually enumerated pathspecs in staging
and commit commands, preserving unrelated staged changes. Include:

```text
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>
```

Return the commit, actual semantic findings, evidence paths and limitations.
CDC reviews this batch before planning the next execution unit. Full Slice04
acceptance still requires the complete corrected packet and original eight
criteria; this checkpoint is neither a new slice nor a replacement criterion.
