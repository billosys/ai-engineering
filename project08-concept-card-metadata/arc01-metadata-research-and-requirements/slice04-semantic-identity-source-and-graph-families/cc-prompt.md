# CC Assignment: Project08 Arc01 Slice04

## Current Route After Batch01 Acceptance

Batch01 is accepted within its ten-pair/sample boundary. Slice04 is not closed.
The next CC assignment is the sibling slice, relative to Project08:

```text
arc01-metadata-research-and-requirements/slice06-record-identity-and-classification/cc-prompt.md
```

Earlier Iteration 01 and Batch01 prompts below are retained as history and
overall remaining requirements, not the next execution assignment.

## Current Assignment: Iteration 01

Next run: execute `artifacts/iteration-01-batch01-cc-prompt.md`. It defines
a bounded first evidence-producing checkpoint under this same iteration;
the full correction prompt remains the overall acceptance contract.

The packet in `76d284f4` passes exact mechanical pair coverage but fails
semantic and evidence review. Read `cdc-verification.md`, then execute
`artifacts/iteration-01-cc-prompt.md` in a fresh context. The original plan
and eight-row ledger still govern acceptance. Slice05 remains unopened.

## Initial Assignment (Retained Scope)

Use a fresh CC context for authored semantic work. The original inventory
slice is still open; do not execute its old iteration prompts or open research.

## Read First

From the Project08 directory, read `AGENTS.md`, `project-plan.md`, project
`ledger.md`, this arc's `arc-plan.md` and `ledger.md`, then this slice's
`slice-plan.md` and `ledger.md`.

In sibling `slice01-metadata-inventory-and-research-questions/`, read:

- `artifacts/planning-brief.md` and `artifacts/remediation-decomposition.md`.
- `artifacts/iteration-04-sizing.md` and the latest `cdc-verification.md`
  sections; earlier iteration findings are historical unless still open.
- `artifacts/metadata-inventory.md`, `artifacts/capability-crosswalk.md`,
  `artifacts/research-agenda.md`, `artifacts/input-register.md`.
- `artifacts/frontmatter-inventory.json` and
  `artifacts/field-dispositions.json` as mechanical evidence, not trustworthy
  authored semantics. Query relevant sections rather than dumping the corpus.

Source checkout: `/Users/oubiwann/lab/billosys/ai-engineering`, branch `main`.
Planning checkout: the source checkout's `.worktrees/planning`, branch
`planning`. Confirm both live, record statuses/commit bases and preserve
unrelated work. Slice01 sizing baseline is `2b26282e`; source baseline is
`e763c661` with concept-cards 4.8.1. Report drift, do not silently repin.

Load the current source `knowledge/collaboration-framework/SKILL.md`,
`knowledge/project-management/SKILL.md`, its `guides/README.md`,
`knowledge/work-verification/SKILL.md` and `knowledge/concept-cards/SKILL.md`.
Use source guides for the operations you inspect, not historical framework paths.

## Execute The Semantic Comparison

Size the assigned work before authoring. The accepted split gives you identity/
labels, classification/progression, bibliography, source snapshot/representation,
locators/spans, claim/support structure, relationships and CQs/coverage. Other
families belong to Slice05 as specified in the decomposition decision. This is
not permission to assign an inconvenient in-scope field to the remainder.
If it genuinely cannot fit, identify the exact scope and requested amendment.

Read both full v3.2 prompts in the planning checkout:

- `old/dev/concept-cards/0009-howto-concept-card-extraction-with-claude-code-v3.2.md`
- `old/dev/concept-cards/0010-a-guide-for-parallel-concept-card-re-extraction-v3.2.md`

Inspect predecessor differences in:

- `old/dev/concept-cards/0007-howto-concept-card-extraction-with-claude-code-v3.1.md`
- `old/dev/concept-cards/0008-a-guide-for-parallel-concept-card-re-extraction-v3.1.md`

Read the relevant live definitions, including concept-cards guides
`01-load-contract.md`, `03-extraction.md`,
`04-re-extraction-preservation.md`, `06-graph-cq.md`, the corresponding
templates/examples, and references `record-field-groups.md` and `vocabulary.md`.
Read document-extraction guides `03-output-contract.md` and
`09-locator-model.md`. Follow additional definition routes when needed;
register the actual inspected set and limitations, not a blanket claim.

Inspect representative real values AND bodies from Complete Musician, Erlang
(including design-scale-erlang-otp), Arc07 and both captured CompCogNeuro reruns.
Include missing/minimal, rich and anomalous cases. Do not edit malformed
historical YAML or treat synthetic templates as proof of generated behavior.
Use Slice01's inventory and registered paths to locate exact examples.

For every assigned contextual membership, record:

- Explicit normalized path, observed kind and finer corpus/role distinctions
  when necessary, linked to a stable authored family ID.
- What subject the value describes, its actual meaning and relevant
  null/empty/container distinctions.
- Exact definition path/section and representative record path/value; include
  revision/hash identities in the input register.
- Observed old/current representation and disposition: preserved, renamed,
  relocated, weakened, absent, ambiguous, intentionally changed or current-only,
  as evidence warrants. Separate unresolved design questions from observations.
- A concrete lookup/migration/source-review consequence and body interaction.

Do not derive prose from suffixes. `actor.id` identifies a performer,
`source_refs[].id` identifies a source, and an assertion's ID identifies that
assertion. Actor provenance is Slice05's responsibility; use that distinction
to set the boundary, not to lump all IDs together. Aliases occur in legacy and
current records: any claim of weakening needs a demonstrated difference.
Field absence, information relocation and lost direct queryability differ.

## Deliver And Check

Create the six artifacts named in this slice plan:
`input-register.md`, `semantic-families.md`, `semantic-membership.json`,
`remainder-membership.json`, `handoff.md`, `validation-evidence.md`.
All belong in this slice's `artifacts/`.

Document the two membership artifacts' shape. Explicit membership arrays
may group several observed kinds only when the same meaning truly applies;
expansion must be deterministic. Each analyzed member resolves to an authored
family and evidence, with contextual overrides where a family-level summary
would hide differences. Each remainder member has a Slice05 owner/family and
reason, not an invented completed semantic disposition.

Independently derive all observed path/kind pairs from Slice01's index. It
currently has 308 distinct paths and 555 pairs across 13 labels. Confirm the
assigned/remainder union covers every pair, with no extras or unexplained
duplicates. `untyped` is not one homogeneous semantic record class. Record
finer contextual splits and any discovered mechanical gap explicitly.

Check every assigned family against real definitions and records; include
each disposition category you claim. Record structural checks separately from
semantic review. Give exact repeatable commands, actual outputs/exit statuses
and limitations. Check JSON with an established parser, local cited paths and
hashes, unchanged source/census/baseline evidence, and planning whitespace.
You do not need another nine-root census for unchanged mechanical inputs.

Slice05 owns the final mechanical/authored Fennel join, literal macOS Bash 3.2
nine-root replay, codec/EOF instruction resolution and original report updates.
Name these and any newly discovered integration issue in the handoff. Do not
overwrite Slice01's index with a partially analyzed result.

## Scope, Status And Commit

Write only this slice's six artifacts, necessary small approved validation
helpers if genuinely needed, its ledger and a CC `closing-report.md`.
Use Fennel for new scripts, established parsing libraries, and no Ruby/Python
source or wrappers. Do not introduce a general framework to do the comparison.
All source, historical records, Slice01 artifacts and reviewer records remain
read-only. No package rebuilds or installs are authorized or required.

Walk all eight ledger rows in the close report with exact evidence and
attested/proposed-done status. Include an explicit artifact inventory and
scope-as-specified versus scope-as-delivered bubble-up. Do not author
`cdc-verification.md`, close Slice01, advance to Slice02 or claim Arc02
architecture acceptance.

Commit the delivered files using explicitly enumerated filenames in both
staging and commit commands, never a directory, wildcard or `-a`. Inspect
the staged diff and preserve unrelated staged work. Include these trailers:

```text
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>
```

Report commit, semantic findings, assigned/remainder counts, reproducible
checks and remaining gaps. CDC review remains separate; Slice05's detailed
planning starts from the independently reviewed packet.
