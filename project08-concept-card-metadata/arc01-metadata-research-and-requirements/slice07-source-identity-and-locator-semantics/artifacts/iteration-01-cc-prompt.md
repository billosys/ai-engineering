# CC Iteration 01: Complete Slice07 Contextual Semantics

Correct S7-R1 through S7-R3 in `../cdc-verification.md`. This is the same
twenty-pair slice, not a new allocation or a standards/schema design task.
Read the original `../cc-prompt.md` and its ordered load set, current plans
and ledger, then the CDC findings. Source remains read-only.

## Preserve What Passed

Exact twenty-pair coverage, frozen inclusion, accepted-47 disjointness and
scoped preservation passed independently. Keep that set unchanged. Retain the
address-versus-support distinction, explicit unknowns and template limitations.
S7-2/S7-7 are CDC-reproduced; do not overwrite that provenance with CC-only
attestation. Recheck them after changes and report regressions if any.

## Complete The Authored Comparison

Replace the two generic definition/disposition patterns with inspected,
field-specific meanings. For each membership explain what it denotes, its
observed shape/context, the evidence supporting that meaning, a useful lookup
or preservation consequence, and remaining ambiguity. Shared cautions can be
factored, but each application must be justified.

Distinguish author attribution, source title and slug, chapter title and ordinal,
section and page; source identity, snapshot, resource and representation;
locator kind/value, numbering base/sequence and range endpoint convention;
context hints, original/prepared counterpart references, mapping evidence and
prepared-source provenance. These are review questions, not a new accepted
schema or permission to infer equivalence. In particular, evidence of an
address mapping is not the address itself or evidence warranting a concept claim.

Query the frozen census for all seven legacy fields by music/Erlang and
relevant source family. Preserve absent/null/value/shape distinctions. Register
that input and literal queries; inspect named examples and body references
supporting the important differences, including null/no-page and non-book
contexts where available. Do not substitute CDC's aggregate counts for your
own executed comparison or infer inapplicability from nulls.

Inspect historical Provenance guidance and a relevant checked predecessor
difference. Use prior full-read evidence honestly: distinguish inherited
observations from material freshly inspected, register the actual dependencies
of claims, and make exact sections resolvable.

Use the current locator template's full relevant body, extraction locator
guidance and document-extraction locator model. The existing PDF/EPUB handoff
examples and Project05 Arc07 Slice02 locator map named in CDC's S7-R2 supply
bounded comparison cases. Read and register their relevant contexts, not just
their filenames. Show at least one concrete PDF page-basis distinction and
one resource/line/anchor distinction; inspect populated map/reference usage.
Label synthetic convention, external map, embedded record, template root and
populated root separately. Check declared target identities when accessible;
retain unavailable originals and unresolved mappings. Do not execute a
conversion or invent physical files for synthetic example paths.

Write per-field dispositions with concrete consequences. For example, explain
which information allows a lookup to select a snapshot/resource and interpret
a range, which information supports an original/prepared correspondence, and
what historical attribution cannot establish. Keep migration/standards
questions explicit rather than answering them by representability.

## Replay And Closeout

Update the four existing artifacts. Supply and execute literal commands from
a declared cwd for every reported census/comparison, exact expected set derived
independently from the unchanged slice-plan table, uniqueness, inventory
inclusion, accepted-set disjointness, both evidence reference layers, and every
registered input hash. Register sections/roles that actually support each
meaning. JSON validity and hash resolution are not semantic verification.

Pin both endpoints for historical prior-packet preservation checks. Separately
record entry/current worktree states and input checksums; preserve original
corpora and Slice01/04/06. Record exact command results or labeled summaries
and separate your semantic inspection from CDC's independent review.

In handoff.md name concrete unresolved questions and integration/sizing
consequences; do not merely repeat the remainder count. In closing-report.md
walk each S7-1 through S7-7 individually, inventory all four artifacts, name the
explicit commit scope, disposition each CDC finding and give an honest
scope-as-specified/delivered bubble-up. If something remains missing, leave its
row open and identify it. Do not claim CDC closure or move unfinished work
to another slice.

The 47 accepted / 20 assigned / 488 other pairs remain unchanged. No source
edit/version bump, extraction, new helper, schema implementation, package,
install, runtime, source-support verification or parent closure. Project08's
Complete Musician P-14 trial remains later work, not part of this repair.

## Explicit Commit Scope

Only these six existing files may be edited and committed, under
`project08-concept-card-metadata/arc01-metadata-research-and-requirements/slice07-source-identity-and-locator-semantics/`:

- artifacts/semantic-evidence.md
- artifacts/semantic-membership.json
- artifacts/validation-evidence.md
- artifacts/handoff.md
- ledger.md
- closing-report.md

Use individually enumerated full planning-relative filenames for both staging
and `git commit --only -- <filenames>`. Inspect the staged diff and preserve
unrelated work. No directory/glob staging or commit -a. Include both trailers:

~~~text
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>
~~~

Return the scoped commit, concrete semantic differences, executed validation
results and any remaining limitations. Do not write CDC verification or edit
plans/criteria. Independent CDC review remains the next gate.
