# CC Assignment: Project08 Arc01 Slice06

Use a fresh context. Deliver the 37-pair evidence packet defined in
`slice-plan.md`, against all seven `ledger.md` rows. This is execution of a
bounded semantic comparison, not another audit of whether such work is needed.

## Load And Scope

Read Project08 `AGENTS.md`, project plan/ledger, arc plan/ledger, this slice's
plan/ledger, then the current source collaboration-framework, project-management
wayfinder, work-verification and concept-cards entrypoints and relevant guides.
Source checkout is `/Users/oubiwann/lab/billosys/ai-engineering` on `main`;
planning is its `.worktrees/planning` checkout. Confirm current identities
and dirty state; preserve unrelated work.

Read sibling Slice04's accepted
`artifacts/batch01-identity-membership.json`,
`artifacts/batch01-identity-evidence.md` and latest `cdc-verification.md`.
The registry/analysis in `995c86d6` was independently reviewed. CDC corrected
only its replay documentation; do not reproduce the old broken paths.
Use sibling Slice01's `artifacts/frontmatter-inventory.json` and
`artifacts/field-dispositions.json` as mechanical inputs. Their original
semantic annotations and Slice04's old full assignment map remain unaccepted.

The scope is exact: root id/revision for eleven non-card record kinds, root
record_type for all twelve typed kinds, and category/subcategory/tier in
untyped records. The complete kind list is in the slice plan. Exclude the ten
accepted Batch01 pairs. Do not conflate root record identity with actor IDs,
subject_refs, endpoint IDs or lifecycle decisions. Explain context without
absorbing those separately owned fields.

## Author The Evidence

Inspect the corresponding current templates, operation-specific guidance,
field-group/vocabulary references and available real/synthetic records.
Record which construct each record represents and what its identity/revision/
type value means. Template placeholders and generated populated fields are
different evidence. If only a template establishes a meaning, say so.
Keep shared rules with explicit contextual applications, not 37 copies of
generic "losing this field prevents lookup" prose.

For category/subcategory/tier, inspect the relevant v3.1/v3.2 Classification
sections and actual music/Erlang values using the existing parsed census.
Select named body examples that illuminate the difference between domain
taxonomy and prerequisite depth. Record missing, inconsistent or ambiguous
behavior instead of silently repairing historical evidence. Check how the
current template and inspected current cards represent or omit those meanings.
Do not select a future classification standard or equate absence with loss
unless the observed evidence supports that conclusion.

Every selected pair must resolve to an authored definition, concrete input
evidence, explicit contextual disposition, and a useful lookup/migration/body
consequence. Use finer contexts when needed. Dispositions describe observations;
future design recommendations are separate. Cite exact paths/sections and
hashes. Reuse checked prior input identities where applicable; report actual
reads and comparisons honestly.

## Outputs And Verification

Write the four artifacts named in the plan under this slice's `artifacts/`:
`semantic-evidence.md`, `semantic-membership.json`,
`validation-evidence.md`, `handoff.md`.

Provide a registry with resolvable meaning and evidence IDs, explicit
dispositions and documented contextual overrides. Prove the pair projection
equals the independently derived 37-pair set, is present in Slice01 and is
disjoint from Batch01. Check both membership and meaning evidence references.
Derive checksum inputs from durable records; no unexplained temporary files.
Run every documented command literally from the stated cwd and record actual
results/statuses. Separate automated checks from semantic inspection.

Source and prior evidence are read-only. No raw corpus regeneration, source-
skill edit, new schema, package build/install, full extraction, or runtime work.
Use existing tools; any genuinely needed new script must be Fennel with
established parsing libraries, not Ruby or Python. Do not introduce a general
framework for this bounded comparison.

If execution reveals genuinely oversized scope, record concrete completed
evidence plus specific remaining contexts and a proposed bounded amendment.
Do not return only a repeated diagnosis or silently transfer the work to
Slice05. No forced all-done report or fixed semantic-family count is required.

## Close And Commit

Update this slice's ledger with attested evidence and write its CC
`closing-report.md`, walking all seven rows with results, exact output files
and scope-as-specified/delivered bubble-up. Do not write CDC verification or
close Slice04/Slice01. The 508 pairs outside accepted Batch01 plus this slice
remain separately owned; this is not a whole-inventory completion claim.

Only write the four named artifacts, this slice's ledger and closing report
(plus a genuinely necessary helper explicitly inventoried in the report).
Leave plans/prompts, other slices, source and historical records untouched.
Commit individually enumerated filenames in both staging and commit commands,
preserving unrelated staged work. Include both trailers:

```text
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>
```

Return commit, concrete semantic findings, exact coverage, validation results
and remaining limitations. CDC then reviews this slice and sizes the next
bounded part of the original Slice04 remainder.
