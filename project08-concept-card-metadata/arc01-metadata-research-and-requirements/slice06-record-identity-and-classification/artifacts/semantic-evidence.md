# Slice06 Semantic Evidence

## Exact scope and evidence boundary

The registry covers exactly 37 root pairs. It now records the current identity
contract (SKILL.md lines 138-144), record-field-groups rows 9-20, templates,
and the available populated records. Templates establish representation and
null placeholders; they do not establish generated populated values.

The shared rule is conditional: stable id and revision preserve record identity;
a reference normally carries id/revision/path, adding record_type or a section
anchor when disambiguation is needed. This is not a mandated type-qualified
composite key and does not establish global ID uniqueness. Each membership
applies that rule to its named construct: claim assertion, CQ question,
extraction-run trace, admission decision, preservation decision,
reconciliation result, relationship edge, locator, support linkage, validation
result, or verification result. Root IDs/revisions are not actor, subject,
endpoint, target, source, or method identities.

## Populated evidence and limits

The frozen census supplies populated synthetic CQ, extraction-run,
memory-admission, reconciliation and relationship-edge examples, plus a
generated Arc07 source-support record. The edge root
edge-prepared-provenance-precedes-support is distinct from its endpoint IDs;
the extraction-run root differs from IDs in its inputs/outputs. These examples
are labelled synthetic or generated in the registry, not real extraction.
Claim, preservation-decision, source-locator, validation-result and
verification-result have template-only root evidence in this inspected census.
The teaching current cc-pattern-completion card has populated id/revision/type
but lacks category, subcategory and tier; this generated absence is separate
from template omission.

## Classification census and comparisons

Frozen inventory query over the music/Erlang legacy population finds all 2,054
records carry category/subcategory/tier keys. Category and tier are strings;
subcategory is a string in 1,974 and null in 80. Observed distinct values:
category 26, subcategory 408 including null, tier 3. The Erlang Behaviour card
is a named null-subcategory example, so null is present data—not missing or
automatic inapplicability.

The v3.2 prompt defines category as primary domain taxonomy, subcategory as
optional finer classification, and tier as foundational/intermediate/advanced.
It defines foundational as no prerequisites within the source. Accent Types
observes tier foundational and prerequisites [meter], while its body says metric
accent depends on meter. Meter is in the same Complete Musician source
directory, creating a bounded tension with that historical definition; this
does not establish a whole graph or prove the card wrong. Erlang
application-behaviour instead observes applications-releases/applications/
intermediate with otp-application prerequisite. Category/subcategory support
legacy discovery; tier supports limited pedagogical filtering, never source
coordinates or a future taxonomy.

## Limits and handoff

## Iteration 02 corpus-specific completion

Frozen inventory SHA-256 is afc1985f1998da0d1df0bdc5800aada9842e77873271fc65f3ae190c0e057b1b.
Complete Musician has 390 records: 10 string category values, 67 string
subcategory values, and three string tier values. Its category vocabulary
includes analysis, chords, counterpoint, harmony, rhythm-meter and
scales-modes; tier frequencies are advanced 141, foundational 79,
intermediate 170. Erlang has 1,664 records: 16 string category values, 341
subcategory values (1,584 strings and 80 present nulls), and three string tier
values. Its categories include applications-releases, distribution,
otp-behaviours, processes-concurrency and tooling; tiers are advanced 330,
foundational 403, intermediate 931. All three keys are present in every record
in both populations. These are raw observations, not normalization or a
taxonomy-quality verdict.

Meter SHA-256 is 944771789c8e1dcf7fcbdb3d20f48e73d9a58a6eba22780e7f23a6b76fbbc221;
its frontmatter identifies source_slug complete-musician, so the Accent
Types/Meter tension is bounded within the same source. v3.1 0007
Classification lines 103-124 and v3.2 0010 supply the same field/depth
meanings; no relevant semantic change is observed. Later work must explicitly
choose preservation/migration treatment rather than presume loss.

This is same-context CC evidence, not CDC verification. It proves neither
migration loss nor future classification/key policy. The other 508 pairs remain
with their recorded owners; Slice04 integration awaits CDC review.
