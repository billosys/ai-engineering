# Arc 01 Slice 02 Ledger: Diagnostic Hardening

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| S02-01 | Existing Slice 01 happy-path behavior and public API remain intact. | Run `cargo test`; inspect `src/lib.rs`, `fixtures/valid/happy_path.lykn`, and `examples/generated/happy_path.cpp` for unchanged API and deterministic output expectations. | serious | Slice 01 CDC bubble-up | done | `cdc-verification.md`; reproduced | API and happy-path output preserved. |
| S02-02 | Malformed top-level and malformed expression syntax have explicit structured diagnostics and tests. | Inspect invalid fixtures/tests for top-level and expression malformed cases; run `cargo test`. | correctness-grade | arc plan | done | `cdc-verification.md`; reproduced | Malformed top-level and expression tests passed. |
| S02-03 | Binary operator arity errors are rejected with clear diagnostics and tests. | Inspect tests or fixtures for too-few and too-many operands; run `cargo test`. | correctness-grade | arc plan | done | `cdc-verification.md`; reproduced | Too-few and too-many operand cases verified. |
| S02-04 | Unsupported forms remain rejected through structured parse diagnostics and regression tests. | Inspect unsupported-form fixture/test and error assertions; run `cargo test`. | correctness-grade | arc plan | done | `cdc-verification.md`; reproduced | Unsupported-form regression verified. |
| S02-05 | Unknown identifiers and duplicate bindings are rejected through structured codegen diagnostics and tests. | Inspect codegen error tests for both cases; run `cargo test`. | correctness-grade | Slice 01 implementation surface | done | `cdc-verification.md`; reproduced | Unknown and duplicate binding diagnostics verified. |
| S02-06 | Direct literal division by zero is rejected before emitting C++ and has a regression test. | Inspect codegen or validation logic and test for `(/ expr 0)`; confirm generated C++ is not emitted for that input. | serious | C++ safety boundary | done | `cdc-verification.md`; reproduced | Manual CLI redirect confirmed no C++ on stdout. |
| S02-07 | C++-unsafe identifiers are rejected rather than emitted as invalid or misleading C++ locals. | Inspect identifier validation and tests for at least one C++ reserved word and one hyphenated identifier. | serious | Slice 01 CDC bubble-up | done | `cdc-verification.md`; reproduced | Reserved word and hyphenated identifier cases verified. |
| S02-08 | CLI invalid-input behavior is covered by at least one regression test and remains stdout/stderr separated. | Run `cargo test`; inspect CLI test assertions for non-zero status, empty stdout, and stderr diagnostic. | correctness-grade | Rust CLI guidance | done | `cdc-verification.md`; reproduced | Test and manual redirect check verified separation. |
| S02-09 | Validation gates pass: `cargo fmt --check`, `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, and C++17 happy-path smoke. | Run each command from `implementation/lykn-cpp-transpiler`; run the generated C++ smoke binary and confirm it prints `9`. | serious | framework validation discipline | done | `cdc-verification.md`; reproduced | All validation gates passed; C++ smoke printed `9`. |
| S02-10 | CC provides a closing report with row-by-row dispositions, validation evidence, artifact inventory, and bubble-up notes for Arc 01. | Inspect `closing-report.md`; confirm every S02 row appears exactly once with attested evidence or a valid deferral/no-op rationale. | serious | ledger discipline | done | `cdc-verification.md`; reproduced | Closing report walks all ten rows exactly once. |
