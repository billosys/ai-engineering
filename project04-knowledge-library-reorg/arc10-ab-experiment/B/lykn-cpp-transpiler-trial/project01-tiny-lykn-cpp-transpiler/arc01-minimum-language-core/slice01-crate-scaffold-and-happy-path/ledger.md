# Arc 01 Slice 01 Ledger: Crate Scaffold and Happy Path

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| S01-01 | A Cargo crate exists at `implementation/lykn-cpp-transpiler` with conventional library/binary separation. | Inspect `Cargo.toml`, `src/lib.rs`, and `src/main.rs`; confirm logic is not concentrated solely in `main.rs`. | serious | slice plan | done | `cdc-verification.md` S01-01; reproduced | Crate and module split verified. |
| S01-02 | The public library exposes a fallible `transpile_to_cpp(source: &str)`-style API using a structured error type. | Inspect `src/lib.rs` and `src/error.rs` or equivalent; confirm user input errors return `Result`, not panic/unwrap. | correctness-grade | Rust guidance | done | `cdc-verification.md` S01-02; reproduced | Public API and structured errors verified. |
| S01-03 | The parser accepts the tiny Slice 01 source subset and builds an AST for statements and integer expressions. | Run parser/unit tests; inspect `src/ast.rs` and `src/parser.rs` or equivalent. | correctness-grade | slice plan | done | `cdc-verification.md` S01-03; reproduced | Parser scope verified through source inspection and tests. |
| S01-04 | Code generation emits deterministic one-file C++17 using only the approved subset. | Run generation tests or snapshot tests; inspect generated output for approved constructs and absence of excluded constructs. | correctness-grade | C++ guidance | done | `cdc-verification.md` S01-04; reproduced | Exact output test and C++17 smoke verified. |
| S01-05 | At least one valid fixture transpiles to the expected C++ and at least one invalid fixture returns a diagnostic. | Run `cargo test`; inspect `fixtures/valid/` and `fixtures/invalid/`. | serious | trial prompt | done | `cdc-verification.md` S01-05; reproduced | Valid and invalid fixtures verified. |
| S01-06 | The thin CLI reads one source file and writes generated C++ to stdout while diagnostics go to stderr with non-zero failure. | Run CLI integration test or command smoke documented in the closing report. | correctness-grade | Rust CLI guidance | done | `cdc-verification.md` S01-06; reproduced | Valid and invalid CLI behavior verified. |
| S01-07 | Validation gates pass: `cargo fmt --check`, `cargo check`, `cargo clippy -- -D warnings`, and `cargo test`. | Run each command from `implementation/lykn-cpp-transpiler`. | serious | framework validation discipline | done | `cdc-verification.md` S01-07; reproduced | All required Cargo gates passed. |
| S01-08 | CC provides a closing report with row-by-row dispositions, validation evidence, artifact inventory, and bubble-up notes for Arc 01. | Inspect `closing-report.md`; confirm every S01 row appears exactly once with attested evidence or a valid deferral/no-op rationale. | serious | ledger discipline | done | `cdc-verification.md` S01-08; reproduced | Closing report walks all eight rows and includes bubble-up. |
