# Slice 03: Package Path Gate Implementation Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | The implementation provides the Slice 02 public entry points: `make check-package-paths`, executable `scripts/check-package-paths`, and `package-path-exceptions.tsv` if needed for current transitional policy. | From implementation checkout: `rg -n "check-package-paths" Makefile && test -x scripts/check-package-paths && test ! -e scripts/check-package-paths.py && { test -f package-path-exceptions.tsv || rg -n "no exception file" .worktrees/planning/project01-harmonise-paths/arc01-distribution-path-contract/slice03-package-path-gate-implementation/closing-report.md; }` | correctness | slice-plan | done | `artifacts/entrypoint-checks.txt` shows Make target/help lines and exit 0 for executable no-suffix script/no `.py` path. | The checker entry point is intentionally no-suffix because `/scripts/*.py` is ignored. |
| F-2 | `make check-package-paths` validates generated zip archives named by `INSTALL_ZIPS` as the authoritative package surface. | From implementation checkout: `make check-package-paths` and inspect output for `zips scanned: 12` or an equivalent per-zip listing covering every current `INSTALL_ZIPS` archive. | serious | slice-plan | done | `artifacts/make-check-package-paths.txt` records `zips scanned: 12`, `markdown files scanned: 171`, `hard failures: 0`. | The target depends on `all`, then scans `$(INSTALL_ZIPS)`. |
| F-3 | The checker has reproducible Markdown-aware parser coverage for inline links, image links, reference definitions, fenced code, indented code where feasible, conservative code spans, anchors, placeholders, and external URLs. | From implementation checkout: run the checker self-test or fixture command chosen by the implementation, and inspect the artifact transcript under this slice's `artifacts/` directory for each parser case name. | serious | slice-plan | done | `artifacts/parser-self-test.txt` records all required parser cases as `ok`. | The hard-fail parser is a small Markdown scanner, not the Slice 01 raw regex audit. |
| F-4 | The checker implements classification and exit-code behavior for hard failures, warnings, explicit exceptions, skipped external URLs, parser-suppressed material, unclassified findings, and malformed exception schema. | From implementation checkout: run the checker current-package command plus malformed-exception/self-test command, then inspect output/artifacts for exit code 0, 1, and/or 2 demonstrations as applicable. | correctness | slice-plan | done | `artifacts/make-check-package-paths.txt` demonstrates exit 0 current scan; `artifacts/hard-failure-fixture.txt` demonstrates exit 1; `artifacts/malformed-exception-schema.txt` demonstrates exit 2. | Current repo state passes with transitional warnings; malformed schema exits 2. |
| F-5 | The report output is stable and separates hard failures, warnings, explicit exceptions, skipped external URLs, and parser-suppressed or omitted material without filtered-CSV overclaiming. | From implementation checkout: `make check-package-paths` and inspect the captured artifact output for separate named buckets. | correctness | slice-plan | done | `artifacts/make-check-package-paths.txt` reports separate buckets: hard failures 0, warnings 426, explicit exceptions 3, skipped external URLs 656, parser-suppressed material omitted by Markdown parser. | Parser-suppressed material is stated as omitted, not counted as zero. |
| F-6 | Durable slice-produced evidence artifacts live under `slice03-package-path-gate-implementation/artifacts/`, not implementation `workbench/` or `/private/tmp` alone. | From planning worktree: `find project01-harmonise-paths/arc01-distribution-path-contract/slice03-package-path-gate-implementation/artifacts -maxdepth 2 -type f -print` and inspect filenames. | correctness | slice-plan | done | Artifact inventory is in the close report; `find .../artifacts -maxdepth 2 -type f -print` lists current scan, parser, malformed schema, hard-failure, compatibility, diff, and entrypoint transcripts. | Temporary scratch was used only to create fixture inputs; durable transcripts live here. |
| F-7 | Implementation scope stays within the Slice 03 gate: no mature guide prose edits, no CCDP package target, no package layout expansion, and no planning-methodology changes. | Inspect implementation diff or commit: `git diff --name-only` or `git show --name-only --format=%H <implementation-commit>` and compare with allowed paths. | serious | slice-plan | done | `artifacts/implementation-diff-scope.txt` lists only `Makefile`, `package-path-exceptions.tsv`, and `scripts/check-package-paths`. | No guide prose, CCDP package target, package layout, or planning-methodology edits were made. |
| F-8 | Existing package build and skill-description checks still pass after the gate implementation. | From implementation checkout: `make check-skills` and `make all`. | serious | slice-plan | done | `artifacts/make-check-skills.txt` records `>> all skill descriptions within limit`; `artifacts/make-all.txt` records successful zip build completion. | `make check-package-paths` also rebuilds through `all`; this row remains independently evidenced. |
| F-9 | The close report walks every ledger row, names the implementation commit or diff state, inventories produced artifacts, and bubbles findings up to Arc 01. | From planning worktree: `test -f project01-harmonise-paths/arc01-distribution-path-contract/slice03-package-path-gate-implementation/closing-report.md && rg -n "F-1|F-2|F-3|F-4|F-5|F-6|F-7|F-8|F-9|Artifacts|Bubble-up to Arc 01" project01-harmonise-paths/arc01-distribution-path-contract/slice03-package-path-gate-implementation/closing-report.md` | correctness | slice-plan | done | `closing-report.md` walks F-1 through F-9, inventories artifacts, records uncommitted implementation diff state, and includes Bubble-up to Arc 01. | |
