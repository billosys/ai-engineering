README.md:20:- the **Composite Cognition Dispatch Protocol**, an RFC-style protocol for
README.md:33:- [Composite Cognition Dispatch Protocol](#composite-cognition-dispatch-protocol)
README.md:121:  that wrote the code), from line/function details through module, package,
README.md:152:1. **Build it:** `make collab-framework` produces `collaboration-framework.zip`
README.md:155:   - *Claude Desktop / claude.ai / Cowork:* upload the zip in your Skills
README.md:187:- [`LEDGER-DISCIPLINE.md`](./templates/LEDGER-DISCIPLINE.md) — the per-scale verification protocol (slice / arc / project)
README.md:223:from your project's `CLAUDE.md`, or install the packaged zip (below). Every
README.md:242:Everything packages through the [`Makefile`](./Makefile). Each zip is named
README.md:248:make skills            # just the ten per-domain zips
README.md:249:make collab-framework  # just collaboration-framework.zip
README.md:252:make rust              # -> rust-guidelines.zip
README.md:253:make go                # -> go-guidelines.zip
README.md:254:make cpp               # -> cpp-guidelines.zip
README.md:255:make js                # -> javascript-deno-guidelines.zip
README.md:256:make erlang            # -> erlang-guidelines.zip
README.md:257:make cobalt            # -> cobalt-guidelines.zip
README.md:258:make design            # -> visual-design-system.zip
README.md:259:make tailwindcss       # -> tailwindcss.zip
README.md:260:make deno              # -> deno-js-linter.zip
README.md:261:make biome             # -> biome-js-linter.zip AND biome-linter.zip
README.md:267:make clean             # remove build/ and all generated zips
README.md:271:The zips upload directly into Claude Desktop / claude.ai as skills; unzipped
README.md:282:├── protocols/         # RFC-style protocol drafts, including CCDP
README.md:297:The the first protocol work for this repo is the
README.md:298:**Composite Cognition Dispatch Protocol** (CCDP): a proposed RFC for
README.md:303:CCDP treats service outputs as claims with epistemic status, not just data. It
README.md:315:[Composite Cognition Dispatch Protocol](https://github.com/billosys/ai-engineering/blob/main/protocols/ccdp/composite-cognition-dispatch-protocol.md).
README.md:317:We've also created a [visual guide](https://billo.systems/ai-engineering/protocols/ccdp/visual-guide/) for the protocol which provides an overview of its goals, what it does, and how it's useful. There's also a brief comparison to other protocols at the end.
protocols/ccdp/Makefile:1:# Makefile — CCDP RFC assembly.
protocols/ccdp/Makefile:3:# Builds the ccdp-assembler tool and runs it over the chapter sources in
protocols/ccdp/Makefile:4:# src/ to produce composite-cognition-dispatch-protocol.md. Defaults to
protocols/ccdp/Makefile:9:.PHONY: ccdp-rfc ccdp-rfc-strict ccdp-rfc-kramdown ccdp-rfc-kramdown-strict clean
protocols/ccdp/Makefile:11:ASSEMBLER := tools/ccdp-assembler/target/release/ccdp-assembler
protocols/ccdp/Makefile:13:OUTPUT := composite-cognition-dispatch-protocol.md
protocols/ccdp/Makefile:16:## ccdp-rfc: assemble the chapters into GFM, with validation
protocols/ccdp/Makefile:17:ccdp-rfc: $(ASSEMBLER)
protocols/ccdp/Makefile:20:## ccdp-rfc-strict: same as ccdp-rfc, but fail the build on any validation warning
protocols/ccdp/Makefile:21:ccdp-rfc-strict: $(ASSEMBLER)
protocols/ccdp/Makefile:24:## ccdp-rfc-kramdown: assemble the chapters into kramdown-rfc-flavored markdown
protocols/ccdp/Makefile:25:ccdp-rfc-kramdown: $(ASSEMBLER)
protocols/ccdp/Makefile:28:## ccdp-rfc-kramdown-strict: same as ccdp-rfc-kramdown, but fail the build on any validation warning
protocols/ccdp/Makefile:29:ccdp-rfc-kramdown-strict: $(ASSEMBLER)
protocols/ccdp/Makefile:32:$(ASSEMBLER): tools/ccdp-assembler/src/*.rs tools/ccdp-assembler/Cargo.toml
protocols/ccdp/Makefile:33:	cd tools/ccdp-assembler && cargo build --release
protocols/ccdp/Makefile:38:	cd tools/ccdp-assembler && cargo clean
Makefile:7:# Every zip is named after the `name:` declared in the skill's frontmatter
Makefile:19:	collaboration-framework.zip \
Makefile:20:	rust-guidelines.zip go-guidelines.zip cpp-guidelines.zip javascript-deno-guidelines.zip \
Makefile:21:	erlang-guidelines.zip cobalt-guidelines.zip visual-design-system.zip \
Makefile:22:	tailwindcss.zip deno-js-linter.zip biome-js-linter.zip biome-linter.zip
Makefile:23:INSTALL_SKILLS := $(INSTALL_ZIPS:.zip=)
Makefile:25:.PHONY: all skills install uninstall clean help check-skills check-package-paths \
Makefile:29:# Every SKILL.md (and the two biome/deno variants) packaged by this Makefile.
Makefile:47:	@echo "  make collab-framework   -> collaboration-framework.zip (SKILL.md + framework docs)"
Makefile:48:	@echo "  make rust               -> rust-guidelines.zip"
Makefile:49:	@echo "  make go                 -> go-guidelines.zip"
Makefile:50:	@echo "  make cpp                -> cpp-guidelines.zip"
Makefile:51:	@echo "  make js                 -> javascript-deno-guidelines.zip"
Makefile:52:	@echo "  make erlang             -> erlang-guidelines.zip"
Makefile:53:	@echo "  make cobalt             -> cobalt-guidelines.zip"
Makefile:54:	@echo "  make design             -> visual-design-system.zip"
Makefile:55:	@echo "  make tailwindcss        -> tailwindcss.zip"
Makefile:56:	@echo "  make deno               -> deno-js-linter.zip"
Makefile:57:	@echo "  make biome              -> biome-js-linter.zip AND biome-linter.zip"
Makefile:58:	@echo "  make skills             -> all per-domain zips"
Makefile:61:	@echo "  make install            -> build all zips and install them into $(INSTALL_DIR)"
Makefile:63:	@echo "  make clean              -> remove build/ and all *.zip"
Makefile:64:	@echo "  make check-package-paths -> validate Markdown paths inside generated zips"
Makefile:73:CF_ZIP   := $(CF_NAME).zip
Makefile:88:	docs/pm/06-confirmation-protocol.md \
Makefile:100:## collab-framework: build collaboration-framework.zip (SKILL.md + its 9 files)
Makefile:115:	@cd "$(BUILD)" && zip -r -q -X "../$(CF_ZIP)" "$(CF_NAME)"
Makefile:117:	@unzip -l "$(CF_ZIP)"
Makefile:121:## collab-framework-clean: remove the staging dir (leaves the zip in place)
Makefile:128:# pack_skill packages one skill file plus its sibling guides/ dir into a zip
Makefile:151:	echo ">> writing $$name.zip"; \
Makefile:152:	rm -f "$$name.zip"; \
Makefile:153:	( cd "$(BUILD)" && zip -r -q -X "../$$name.zip" "$$name" ); \
Makefile:155:	unzip -l "$$name.zip"; \
Makefile:157:	echo ">> done: $$name.zip"
Makefile:188:# frontmatter name; build both zips.
Makefile:197:## skills: build every per-domain zip
Makefile:200:## all: build every per-domain zip plus the collaboration-framework zip
Makefile:208:## check-package-paths: build all zips and validate package-context Markdown paths
Makefile:209:check-package-paths: all
Makefile:210:	@./scripts/check-package-paths --exceptions package-path-exceptions.tsv $(INSTALL_ZIPS)
Makefile:215:## install: uninstall old copies, build all zips, and install them into $(INSTALL_DIR)
Makefile:217:	@echo ">> installing skill zips into $(INSTALL_DIR)"
Makefile:218:	@for zip in $(INSTALL_ZIPS); do \
Makefile:219:		if [ ! -f "$$zip" ]; then \
Makefile:220:			echo "ERROR: missing zip: $$zip" >&2; exit 1; \
Makefile:222:		unzip -o -q "$$zip" -d "$(INSTALL_DIR)"; \
Makefile:234:## clean: remove the staging dir and every generated zip
Makefile:238:	@echo ">> cleaned build/ and all skill zips"
Makefile:240:ccdp:
Makefile:241:	@cd protocols/ccdp && make
protocols/ccdp/src/09-routing.md:5:Routing is the Dispatcher's core function: given a Request with a `capability_type`, select the Service best suited to handle it. CCDP routing is *envelope-based* — the Dispatcher makes routing decisions from envelope metadata and Registry data, never from Content.
protocols/ccdp/src/09-routing.md:21:If `envelope.destination_id` is non-null, route to the specified Service. If the Service is not registered, not ACTIVE, or not healthy, return error `-32001` (service unavailable). Do not fall through to capability-based routing. Explicit destinations MUST also pass authorization, provenance feasibility, deadline, budget, and isolation checks — the same policy filters applied to normal routing (Steps 2–6). An explicit destination is a routing hint, not a bypass of protocol enforcement. If any check fails, the Dispatcher MUST return the appropriate error, not silently fall through to normal routing.
protocols/ccdp/src/09-routing.md:63:- `"escalate"`: Treat as implicit escalation with reason `PROVENANCE_BELOW_REQUIREMENT` and `envelope.escalation.escalation_origin: "dispatcher"` (Section 7.3.4). Because no candidate passed the provenance filter, there is no originating Service whose `escalation_chain` to walk — the implicit escalation routes directly to `org.ccdp.human_review` as the terminal target, subject to the same authorization and data-class checks as Section 13.4 step 6.
protocols/ccdp/src/09-routing.md:106:4. **Human queue.** If no automated Service can handle the request, route to a Service of type `org.ccdp.human_review`. This is the terminal escalation target.
protocols/ccdp/src/09-routing.md:141:│ org.ccdp.deduction   │ z3-prover  │ HEALTHY  │ CLOSED      │ 0.35     │
protocols/ccdp/src/09-routing.md:142:│ org.ccdp.deduction   │ isabelle   │ DEGRADED │ CLOSED      │ 0.80     │
protocols/ccdp/src/09-routing.md:143:│ org.ccdp.planning    │ fd-planner │ HEALTHY  │ CLOSED      │ 0.10     │
protocols/ccdp/src/09-routing.md:144:│ org.ccdp.language.*  │ llm-pool   │ HEALTHY  │ CLOSED      │ 0.55     │
protocols/ccdp/src/09-routing.md:145:│ org.ccdp.human_review│ review-q   │ HEALTHY  │ CLOSED      │ 0.20     │
protocols/ccdp/src/09-routing.md:146:│ org.ccdp.verification│ verifier   │ UNHEALTHY│ OPEN        │ —        │
protocols/ccdp/src/09-routing.md:150:The Routing Table is refreshed from the Registry at a configurable interval (RECOMMENDED: every 30 seconds) and updated in real-time by health check responses. It is an internal Dispatcher structure, not a protocol element — its format is implementation-defined.
protocols/ccdp/src/09-routing.md:152:**Wildcard capability matching.** Capability type patterns ending in `.*` match any capability type that shares the prefix up to the wildcard. `org.ccdp.language.*` matches `org.ccdp.language.generation`, `org.ccdp.language.translation`, etc., but not `org.ccdp.language` itself (the wildcard requires at least one additional segment). Wildcard patterns are valid only in token scopes (Section 15.3.2) and routing configuration; they MUST NOT appear in Capability Records or Registry lookups. The routing table format is implementation-defined and MAY support pattern matching (e.g., glob or prefix wildcards). Pattern syntax, if supported, is not part of the CCDP wire protocol — it is a deployment-local routing configuration concern. The `org.ccdp.language.*` entry in the example above illustrates implementation flexibility, not a normative capability identifier.
protocols/ccdp/src/04-terminology.md:5:**Artifact Reference.** The `artifact_ref` object within an Evidence entry (see Evidence Entry below), pointing to a verifiable artifact (proof object, test result, signed attestation) stored outside the CCDP message. Artifact References appear in Evidence entries. The `uri` form, dereference authority, and access-control requirements are deployment-defined. Artifact References SHOULD include an `integrity` hash for content verification (REQUIRED at grades VALIDATED and above — see Evidence Entry). Artifact References that appear in audit records are subject to the audit retention policy (Section 11.5). Deployments MUST ensure that referenced artifacts remain resolvable for the configured audit retention period. Decomposition Plan result references (Section 14.3.3) are typed JSON Pointer `$ref` objects and are not Artifact References.
protocols/ccdp/src/04-terminology.md:7:**Audit Record.** A structured log entry created by the Dispatcher for each message processed. Audit Records are defined in Section 11. Each Audit Record carries an `audit_schema_version` field independent of the CCDP document and wire versions.
protocols/ccdp/src/04-terminology.md:17:**Capability Type.** A string identifier for a class of cognitive function, using reverse-domain notation (e.g., `org.ccdp.deduction`, `org.ccdp.planning`). The protocol defines a set of well-known Capability Types (Section 8.3); implementations MAY define additional types through the Registry.
protocols/ccdp/src/04-terminology.md:19:**Conformance Level.** The level of protocol compliance claimed by an implementation: Core (all Core requirements) or Full (all Core requirements plus all Full requirements). Conformance is defined by explicit, stably-identified requirement tables (e.g. `DISP-CORE-NNN`, `DISP-FULL-NNN` for the Dispatcher), not by the sum of all SHOULD statements in the specification. Service and Registry stable IDs are an open item (Section 18). See Section 16.4.
protocols/ccdp/src/04-terminology.md:21:**Content.** The payload of a CCDP message — the actual cognitive input or output. Content is opaque to the Dispatcher: the Dispatcher MUST NOT interpret, transform, or make routing decisions based on Content. The Dispatcher MAY perform structural validation of Content against JSON Schemas and MAY resolve typed references within Content wrappers during decomposition plan execution (Section 14). These are structural operations, not semantic interpretation (see Structural Validation vs Semantic Interpretation below). Content structure is governed by the Capability Record's input and output schemas.
protocols/ccdp/src/04-terminology.md:25:**Decomposition.** The process of breaking a complex request into a set of typed sub-requests, each routable to a different Service. Decomposition is performed by a Decomposition Service — a Service whose Capability Type is `org.ccdp.decomposition` — and produces a Decomposition Plan (Section 14).
protocols/ccdp/src/04-terminology.md:29:**Dispatcher.** The central routing component of a CCDP system. The Dispatcher receives messages, reads their Envelopes, makes routing decisions based on Envelope metadata and Registry lookups, forwards messages to Services, and manages audit logging, health monitoring, and escalation routing. The Dispatcher is a constrained protocol enforcement and execution coordinator. It performs structural operations — routing, schema validation, decomposition plan execution, typed-reference resolution, structural result assembly, audit logging, health monitoring, and deadline/budget enforcement — but has no cognitive capability. It never reasons about what content means. A conforming Dispatcher MUST NOT reason about message Content (Section 16.1).
protocols/ccdp/src/04-terminology.md:31:**Envelope.** The structured metadata portion of a CCDP message, containing all information the Dispatcher needs for routing, audit, and protocol enforcement. The Envelope includes message type, identity and tracing fields, routing fields, constraint fields, provenance fields, audit fields, and extensible metadata. The Dispatcher reads the Envelope and selected structural Content fields (schema shapes, typed references) to route and process messages (Section 7).
protocols/ccdp/src/04-terminology.md:33:**Escalation.** A structured protocol response indicating that the originating actor (a Service or the Dispatcher) determined that a request cannot be fulfilled at the required provenance grade or capability level. An Escalation is a first-class message type (not an error), carrying the reason for escalation, the achieved provenance grade (if any), a partial result (if available), and a suggested routing target. The Dispatcher routes Escalations according to the Escalation Chain (Section 13). The `escalation_origin` field (Section 7.3.4) distinguishes Service-generated from Dispatcher-generated Escalations.
protocols/ccdp/src/04-terminology.md:45:    "uri": "urn:ccdp:artifact:abc123",
protocols/ccdp/src/04-terminology.md:73:**Message.** The atomic unit of CCDP communication. A Message consists of an Envelope and a Content payload, encoded as a JSON-RPC 2.0 request or response. Messages are typed by the `envelope.type` field (Section 7).
protocols/ccdp/src/04-terminology.md:75:**Message Type.** The classification of a CCDP Message by its protocol function. The defined Message Types are: REQUEST, RESPONSE, NOTIFICATION, ESCALATION, HEALTH_REQUEST, HEALTH_RESPONSE, and DECOMPOSITION_RESULT (Section 7.2).
protocols/ccdp/src/04-terminology.md:85:**Registry.** The Capability Registry — a service that stores Capability Records, enforces schema versioning and compatibility, and answers routing queries from the Dispatcher. The Registry is a CCDP infrastructure component, not a cognitive Service. Its interface is defined in Section 8; its implementation is not specified.
protocols/ccdp/src/04-terminology.md:87:**Request.** A CCDP Message of type REQUEST, carrying a cognitive task from a requester (human or Service) to the Dispatcher for routing to an appropriate Service.
protocols/ccdp/src/04-terminology.md:89:**Requester.** The human, application, or Service that originates a CCDP Request. The Requester is identified by the `source_id` envelope field and authenticated via bearer tokens (for external requesters) or mTLS identity (for Service-to-Service sub-requests).
protocols/ccdp/src/04-terminology.md:91:**Response.** A CCDP Message of type RESPONSE, carrying the result of a cognitive task from a Service back through the Dispatcher to the requester. Every Response MUST carry Provenance metadata (Section 10).
protocols/ccdp/src/04-terminology.md:93:**Service.** Any component that implements one or more Capabilities and communicates with the Dispatcher using the CCDP protocol. Services are heterogeneous: they may be LLM endpoints, theorem provers, classical planners, databases, human review queues, composite LLM+service hybrids, or Decomposition Services. From the Dispatcher's perspective, all Services expose the same typed interface — the Dispatcher does not know or care what implementation backs a Service (Section 5.3).
protocols/ccdp/src/04-terminology.md:103:**Trace Context.** The W3C Trace Context fields (`traceparent` and `tracestate`) propagated through CCDP messages to enable distributed tracing across the system. CCDP maps these to Envelope fields (Section 11.3).
protocols/ccdp/src/17-security-considerations.md:5:CCDP operates in an environment where:
protocols/ccdp/src/17-security-considerations.md:17:3. Attackers may attempt to exploit protocol features (escalation chains, decomposition, provenance) for unauthorized access or quality degradation.
protocols/ccdp/src/17-security-considerations.md:22:1. Multiple colluding compromised components (this is a single-fault model). While multi-component collusion is out of scope for protocol-level defense, it is a high-risk scenario for large-scale deployments. Deployments handling sensitive data or operating across organizational boundaries SHOULD implement defense-in-depth measures beyond the protocol: independent audit verification, cross-domain provenance spot-checking, and organizational access controls that limit the blast radius of a single compromised identity.
protocols/ccdp/src/17-security-considerations.md:32:**CCDP mitigations:**
protocols/ccdp/src/17-security-considerations.md:34:- Input schema validation (Section 8.2.2) constrains the structural shape of Content, reducing the protocol-level attack surface. Schema validation does not mitigate semantic prompt-injection attacks — a structurally valid string can still be a prompt injection. Content-level injection defense is the Service's responsibility (see Section 17.4).
protocols/ccdp/src/17-security-considerations.md:37:**Residual risk:** Content injection is fundamentally a Service-level concern. CCDP's contribution is ensuring that injection cannot affect protocol behavior (routing, audit, provenance) — only the Service's content processing.
protocols/ccdp/src/17-security-considerations.md:43:**CCDP mitigations:**
protocols/ccdp/src/17-security-considerations.md:44:- Evidence entries must substantiate grades above ASSERTED (Section 10.3). A grade of FORMALLY_VERIFIED without an Evidence entry containing `method: "formal_verification"` and an `artifact_ref` with `artifact_type: "proof_certificate"` (or equivalent verifiable proof artifact) is a protocol violation (Section 10.3).
protocols/ccdp/src/17-security-considerations.md:49:**Residual risk:** A sufficiently sophisticated attacker could forge evidence entries (e.g., generate a fake proof object). Full mitigation requires independent proof checking — the protocol makes evidence available for checking but does not perform the check itself.
protocols/ccdp/src/17-security-considerations.md:55:**CCDP mitigations:**
protocols/ccdp/src/17-security-considerations.md:70:**CCDP mitigations:**
protocols/ccdp/src/17-security-considerations.md:82:**CCDP mitigations:**
protocols/ccdp/src/17-security-considerations.md:93:**CCDP mitigations:**
protocols/ccdp/src/17-security-considerations.md:104:**CCDP mitigations:**
protocols/ccdp/src/17-security-considerations.md:105:- Evidence `artifact_ref` fields are references (URIs), not inline data. Access to the referenced artifacts is governed by the artifact storage system's access controls, not by CCDP.
protocols/ccdp/src/17-security-considerations.md:109:**Residual risk:** Free-text fields (`evidence.description`, `escalation.detail`) can carry arbitrary text. Deployments processing sensitive data SHOULD implement content scanning on these fields. The same exfiltration risk applies to all free-text and URI fields in the protocol: `escalation.detail`, `routing_decision.reason`, `schema_validation` error messages, notification content, and audit record annotations. Deployments processing sensitive data SHOULD implement content scanning or field-length limits on all free-text fields, and SHOULD classify protocol fields by sensitivity level (public, internal, restricted) with access controls matching the classification.
protocols/ccdp/src/17-security-considerations.md:115:**CCDP mitigations:** None at the protocol level. This is an inherent property of any system that exposes latency data.
protocols/ccdp/src/17-security-considerations.md:121:The NSA/CISA AI deployment security guidance [NSA-CISA-2024] makes specific recommendations for securely deploying AI systems generally, not specific to any single protocol. This table maps those general recommendations to CCDP features — it is not a claim that CCDP was independently assessed against them.
protocols/ccdp/src/17-security-considerations.md:123:| NSA/CISA Recommendation | CCDP Response | Requirement Level |
protocols/ccdp/src/17-security-considerations.md:135:Three security concerns that CCDP does not fully address, stated without softening because the credibility of the security design depends on not overclaiming:
protocols/ccdp/src/17-security-considerations.md:137:**Content-level attacks are the Service's problem.** CCDP protects the protocol layer — routing, audit, provenance. It does not protect the content layer. A prompt injection that causes an LLM to produce wrong output is invisible to CCDP unless the output fails schema validation or provenance verification. The protocol provides the infrastructure for detecting such failures (provenance grades, cross-checking, auditing) but does not perform the detection.
protocols/ccdp/src/17-security-considerations.md:139:**The trust model is single-fault.** CCDP is designed to detect and contain compromise of a single component. If both a Service and the Registry are compromised, the attacker can redirect traffic and falsify provenance without detection. Multi-component compromise requires organizational security measures beyond the protocol's scope.
protocols/ccdp/src/17-security-considerations.md:141:**Provenance grades are claims, not proofs.** A provenance grade is a structured assertion by the Service about its own output's epistemic status. While evidence entries make some grades independently verifiable (proof objects can be checked, test results can be re-run), the protocol fundamentally trusts that Services honestly report their grades. Systematic dishonesty requires organizational and auditing countermeasures, not just protocol design.
protocols/ccdp/src/12-flow-control.md:7:Without resource signals, the Dispatcher cannot make resource-rational decisions. TCP solved this problem with congestion control — window advertisements, ECN, slow start, AIMD. CCDP solves it with explicit resource signals at the protocol level: cost budgets on requests, resource consumption on responses, and capacity advertisements from services.
protocols/ccdp/src/12-flow-control.md:101:    "org.ccdp.deduction": {
protocols/ccdp/src/12-flow-control.md:107:    "org.ccdp.verification": {
protocols/ccdp/src/12-flow-control.md:157:When a Service is overloaded, it needs a way to signal the Dispatcher to slow down. CCDP supports back-pressure through three mechanisms:
protocols/ccdp/src/12-flow-control.md:167:A Service MAY respond to a CCDP Request with HTTP 429 instead of a CCDP Response. The `Retry-After` header (in seconds) tells the Dispatcher when to retry. The Dispatcher:
protocols/ccdp/src/12-flow-control.md:174:More precisely: when a Service returns HTTP 429 (Too Many Requests) instead of a CCDP Response, the Dispatcher MUST: (a) log the 429 in the audit trail as a rate-limit event with the Service's `Retry-After` header value if present, (b) treat the Service as temporarily at capacity (equivalent to health status DEGRADED for the requested capability), (c) follow the retry/reroute strategy in Section 13.5.1, and (d) if the Dispatcher itself rate-limits a requester, it MUST return a CCDP error response with code `-32014` (rate limited) and include the `Retry-After` value in the error `data` field. Error `-32003` is reserved for all-services-unhealthy conditions (Section 9.2 Step 3); it is distinct from Dispatcher-side rate limiting. HTTP 429 MUST NOT be passed through to the requester as a raw HTTP response.
protocols/ccdp/src/12-flow-control.md:197:In CCDP, the analog is error amplification across serial cognitive operations. A small uncertainty in Decomposition can produce large errors in downstream sub-results, which compound when composed. The protocol does not solve this problem directly — it is a content-level concern, not a protocol-level one — but it provides the tools for detecting it:
protocols/ccdp/src/05-architecture-overview.md:5:A CCDP system has a star topology with the Dispatcher at the center. All communication between Services passes through the Dispatcher. Services do not communicate directly with each other.
protocols/ccdp/src/05-architecture-overview.md:37:This topology is a deliberate design choice, not a scaling constraint. The Dispatcher is the single point of protocol enforcement — authentication, routing, audit logging, health monitoring, and deadline enforcement all happen at the Dispatcher. A Service that bypasses the Dispatcher bypasses all of these guarantees.
protocols/ccdp/src/05-architecture-overview.md:39:The star topology avoids the O(N²) communication explosion that full-mesh agent architectures face [arXiv-Agent-Comms]. With N services, CCDP requires N links (Dispatcher ↔ Service), not N(N-1)/2. The cost is that the Dispatcher is a single point of failure; high-availability deployment is an infrastructure concern outside this specification's scope, but the protocol's self-contained message design simplifies Dispatcher replication, though production deployments must address shared state for replay caches, circuit-breaker state, health tables, and audit-store consistency (see Section 15.5 and Section 13.6).
protocols/ccdp/src/05-architecture-overview.md:43:A CCDP system comprises four kinds of components. Each has a defined role and a defined boundary of responsibility.
protocols/ccdp/src/05-architecture-overview.md:47:The Dispatcher is the protocol's routing and enforcement engine. It is a constrained protocol enforcement and execution coordinator — not a reasoner. Its duties include structural operations that go beyond simple routing, but it has no cognitive capability and never reasons about message content. Its responsibilities are:
protocols/ccdp/src/05-architecture-overview.md:70:A Service implements one or more Capabilities and communicates with the Dispatcher using CCDP messages. A Service's responsibilities are:
protocols/ccdp/src/05-architecture-overview.md:79:**Idempotency scope.** The idempotency requirement is scoped by service type. Mode 2 (deterministic) Services MUST return byte-identical responses for the same `request_id`. Mode 1 (LLM) and Mode 3 (composite) Services MUST return semantically equivalent responses — the same conclusion at the same or higher provenance grade — but MAY differ in surface form (wording, formatting). For protocol conformance purposes, idempotency is tested by requiring Services to cache and return the original Response for duplicate `request_id` values within the idempotency window. Semantic equivalence is a service-quality property, not a protocol-testable conformance requirement. Mode 4 (human queue) Services MUST return the same response if the human has already submitted a result for the `request_id`; if the human has not yet responded, a retry with the same `request_id` is a no-op (the request remains in the queue). Implementations SHOULD maintain a response cache keyed by `request_id` with a configurable TTL (RECOMMENDED: 24 hours).
protocols/ccdp/src/05-architecture-overview.md:100:The Human Supervisor occupies the top of the supervision tree. The Human Supervisor is not a CCDP component in the protocol sense — the protocol does not specify the human interface — but the protocol is designed to support human supervision:
protocols/ccdp/src/05-architecture-overview.md:127:The most architecturally significant mode. An LLM sits in front of a deterministic service as a translator: it converts a natural-language request into the service's formal input language, passes it through, and converts the formal output back to natural language. From the Dispatcher's perspective, this is a single Service with a single typed interface — the internal LLM translation layer is not visible in the protocol.
protocols/ccdp/src/05-architecture-overview.md:147:Mode substitution has multiple protocol-visible effects: Provenance Grade, cost (different modes have different cost profiles), latency, evidence entries (different methods produce different evidence), signing (some modes may not support signing), and isolation properties. For example, a Mode 2 replacement will report FORMALLY_VERIFIED where the Mode 4 predecessor reported HUMAN_ATTESTED. Consumers of the output can use the Provenance Grade to calibrate their trust — the protocol ensures the change in backing implementation is transparent through the epistemic metadata. Mode substitution is transparent only when the replacement mode satisfies the consumer's full `provenance_requirement` — including `min_policy_grade`, `required_methods`, `required_evidence_types`, and any signing or isolation requirements. A consumer requiring `HUMAN_ATTESTED` cannot be served by a Mode 2 replacement reporting `FORMALLY_VERIFIED` unless the consumer's policy accepts formal verification as equivalent for that claim type. Deployments SHOULD configure mode-substitution policies per capability type that account for all these effects, not just provenance.
protocols/ccdp/src/05-architecture-overview.md:151:Decomposition — breaking a complex request into typed sub-requests — is itself a cognitive act. Rather than requiring the Dispatcher to perform decomposition (which would violate the constrained-coordinator principle) or requiring the human to pre-decompose all requests (which does not scale), CCDP treats decomposition as a first-class Service with Capability Type `org.ccdp.decomposition`.
protocols/ccdp/src/05-architecture-overview.md:157:Result composition is a structural operation: the Dispatcher assembles typed sub-results according to the plan's composition specification (template assembly, concatenation, or selection) without reasoning about what the sub-results mean. When composition requires cognitive judgment (e.g., synthesizing sub-results into a coherent narrative), the plan routes composition to a dedicated Composition Service with capability type `org.ccdp.composition` (Section 14.3.4).
protocols/ccdp/src/05-architecture-overview.md:167:CCDP's architecture maps to the classic supervision-tree model:
protocols/ccdp/src/05-architecture-overview.md:171:- **Services are worker processes.** Each supervised, each with a typed protocol on its wire. They crash loudly (return structured errors or Escalations) rather than silently emitting corrupt output.
protocols/ccdp/src/05-architecture-overview.md:173:- **Typed protocols on the wires.** Every link between Dispatcher and Service is a typed contract enforced by the Registry. Malformed messages are rejected at the boundary. This is the supervision tree's process isolation principle expressed as protocol enforcement.
protocols/ccdp/src/08-capability-registry.md:19:    "capability_type": "org.ccdp.deduction",
protocols/ccdp/src/08-capability-registry.md:21:    "endpoint": "https://z3-prover-01.internal:8443/ccdp",
protocols/ccdp/src/08-capability-registry.md:61:      "endpoint": "https://z3-prover-01.internal:8443/ccdp/health",
protocols/ccdp/src/08-capability-registry.md:75:      {"kind": "capability_type", "value": "org.ccdp.human_review"}
protocols/ccdp/src/08-capability-registry.md:99:**`endpoint`** (string, REQUIRED): The HTTPS endpoint where the Service accepts CCDP messages.
protocols/ccdp/src/08-capability-registry.md:130:**`escalation_chain`** (array of typed entries, REQUIRED but MAY be empty): Ordered list of fallback targets to try if this Service returns an Escalation. The Dispatcher processes the chain in order. An empty chain means escalation goes directly to the Human Supervisor's queue. Escalation chain entries are typed objects: `{"kind": "service_id", "value": "human-review-queue-01"}` or `{"kind": "capability_type", "value": "org.ccdp.human_review"}`. The `kind` field disambiguates the entry; string-only entries are ambiguous and MUST NOT be used.
protocols/ccdp/src/08-capability-registry.md:154:| `org.ccdp.deduction` | Logical deduction, theorem proving, SMT solving | Mode 2 or 3 |
protocols/ccdp/src/08-capability-registry.md:155:| `org.ccdp.planning` | Task planning, decomposition into action sequences | Mode 2 or 3 |
protocols/ccdp/src/08-capability-registry.md:156:| `org.ccdp.language.generation` | Natural language generation, drafting, summarization | Mode 1 |
protocols/ccdp/src/08-capability-registry.md:157:| `org.ccdp.language.translation` | Translation between natural languages or representations | Mode 1 or 3 |
protocols/ccdp/src/08-capability-registry.md:158:| `org.ccdp.language.analysis` | Natural language understanding, classification, extraction | Mode 1 |
protocols/ccdp/src/08-capability-registry.md:159:| `org.ccdp.verification` | Verification of code, proofs, plans against specifications | Mode 2 or 3 |
protocols/ccdp/src/08-capability-registry.md:160:| `org.ccdp.selection` | Ranking, scoring, best-of-N selection | Mode 1, 2, or 3 |
protocols/ccdp/src/08-capability-registry.md:161:| `org.ccdp.retrieval` | Information retrieval, database query, knowledge lookup | Mode 2 |
protocols/ccdp/src/08-capability-registry.md:162:| `org.ccdp.decomposition` | Request decomposition into typed sub-requests | Mode 1 or 3 |
protocols/ccdp/src/08-capability-registry.md:163:| `org.ccdp.human_review` | Human review, judgment, attestation | Mode 4 |
protocols/ccdp/src/08-capability-registry.md:164:| `org.ccdp.code.generation` | Source code generation | Mode 1 or 3 |
protocols/ccdp/src/08-capability-registry.md:165:| `org.ccdp.code.execution` | Code execution in a sandboxed environment | Mode 2 |
protocols/ccdp/src/08-capability-registry.md:166:| `org.ccdp.composition` | Custom composition of Decomposition sub-results requiring judgment (Section 14.3.4) | Mode 1 or 3 |
protocols/ccdp/src/08-capability-registry.md:213:This specification defines the Registry's logical operations and data model. It does not mandate a specific wire protocol for Registry access. Implementations MAY expose the Registry as a REST API, a gRPC service, an in-process library, or any other mechanism that satisfies the operation contracts above. If two CCDP deployments need their Registries to interoperate (e.g., federated routing), they MUST agree on a common Registry API binding outside this specification. A future CCDP extension MAY define a standard Registry wire protocol.
protocols/ccdp/src/08-capability-registry.md:223:Schema evolution is the chronic wound of typed protocols. CCDP addresses it through the Registry, which enforces compatibility rules at registration time — not at the Dispatcher, which should not need to understand schema evolution.
protocols/ccdp/src/08-capability-registry.md:272:The specific availability strategy is an implementation concern, not a protocol requirement. The protocol requires only that routing decisions are logged (including whether they were made from live Registry data, cached data, or static fallback), so that the audit trail reflects the data freshness of each routing decision.
protocols/ccdp/src/01-abstract.md:1:# CCDP: Composite Cognition Dispatch Protocol
protocols/ccdp/src/01-abstract.md:10:This document specifies the Composite Cognition Dispatch Protocol (CCDP), a message-envelope protocol for routing cognitive requests through a deliberately constrained dispatcher to a heterogeneous set of cognitive services — large language models, theorem provers, classical planners, databases, human review queues, and composite LLM+service hybrids — under human supervision. CCDP is a supervision-tree protocol, not an agent-to-agent protocol: one side of every link is a protocol enforcement and execution coordinator that reads envelope metadata and performs structural operations — routing, schema validation, plan execution, audit — without reasoning about message content. The protocol carries the intelligence the dispatcher does not have.
protocols/ccdp/src/01-abstract.md:12:CCDP's novel contribution is the epistemic dimension of cognitive dispatch. All RESPONSE, ESCALATION, and DECOMPOSITION_RESULT messages that carry cognitive outputs include a provenance grade indicating the evidence strength behind them — from opaque assertion through formal verification to human attestation — with defined composition rules for how grades propagate through multi-service operations. Error responses, health messages, and notifications do not carry provenance. Escalation is a first-class protocol operation, not an error state: a service that cannot meet the requested provenance grade returns a structured escalation that the dispatcher routes upward. Structured audit metadata is mandatory for every Dispatcher-mediated hop — every message the Dispatcher processes generates an audit record. Typed service contracts are enforced through a capability registry with schema versioning and compatibility checking.
protocols/ccdp/src/01-abstract.md:14:CCDP layers on HTTP transport and JSON-RPC 2.0 wire format, adding an envelope-based routing layer with provenance, audit, cost signals, health monitoring, and deadline propagation. It is designed so that a conforming Dispatcher can be implemented as a self-contained-message coordinator with no natural-language understanding — each message carries all context needed for processing; however, high-availability deployments require shared operational state for replay caches, circuit-breaker state, health tables, and audit stores (Section 5.1) — while conforming Services range from stateless functions to long-running human review queues behind the same typed interface. Security and audit properties in this specification are protocol-level requirements; production deployments must additionally address operational security, key management, monitoring, and infrastructure hardening beyond the protocol's scope.
protocols/ccdp/Makefile:1:# Makefile — CCDP RFC assembly.
protocols/ccdp/Makefile:3:# Builds the ccdp-assembler tool and runs it over the chapter sources in
protocols/ccdp/Makefile:4:# src/ to produce composite-cognition-dispatch-protocol.md. Defaults to
protocols/ccdp/Makefile:9:.PHONY: ccdp-rfc ccdp-rfc-strict ccdp-rfc-kramdown ccdp-rfc-kramdown-strict clean
protocols/ccdp/Makefile:11:ASSEMBLER := tools/ccdp-assembler/target/release/ccdp-assembler
protocols/ccdp/Makefile:13:OUTPUT := composite-cognition-dispatch-protocol.md
protocols/ccdp/Makefile:16:## ccdp-rfc: assemble the chapters into GFM, with validation
protocols/ccdp/Makefile:17:ccdp-rfc: $(ASSEMBLER)
protocols/ccdp/Makefile:20:## ccdp-rfc-strict: same as ccdp-rfc, but fail the build on any validation warning
protocols/ccdp/Makefile:21:ccdp-rfc-strict: $(ASSEMBLER)
protocols/ccdp/Makefile:24:## ccdp-rfc-kramdown: assemble the chapters into kramdown-rfc-flavored markdown
protocols/ccdp/Makefile:25:ccdp-rfc-kramdown: $(ASSEMBLER)
protocols/ccdp/Makefile:28:## ccdp-rfc-kramdown-strict: same as ccdp-rfc-kramdown, but fail the build on any validation warning
protocols/ccdp/Makefile:29:ccdp-rfc-kramdown-strict: $(ASSEMBLER)
protocols/ccdp/Makefile:32:$(ASSEMBLER): tools/ccdp-assembler/src/*.rs tools/ccdp-assembler/Cargo.toml
protocols/ccdp/Makefile:33:	cd tools/ccdp-assembler && cargo build --release
protocols/ccdp/Makefile:38:	cd tools/ccdp-assembler && cargo clean
protocols/ccdp/visual-guide/ccdp-reference.md:1:# CCDP Visual Guide — Reference Document
protocols/ccdp/visual-guide/ccdp-reference.md:5:> visual guide to the Composite Cognition Dispatch Protocol (CCDP). Every
protocols/ccdp/visual-guide/ccdp-reference.md:14:CCDP is a protocol that routes cognitive work to the right tool for the job —
protocols/ccdp/visual-guide/ccdp-reference.md:21:are rock-solid and which are educated guesses. CCDP splits that work up: a
protocols/ccdp/visual-guide/ccdp-reference.md:31:protocol that carries that distinction lets you build reliable systems from
protocols/ccdp/visual-guide/ccdp-reference.md:48:*not* intelligent — it's a protocol enforcement engine, like a network switch
protocols/ccdp/visual-guide/ccdp-reference.md:76:`org.ccdp.decomposition`. Receives complex requests, emits structured
protocols/ccdp/visual-guide/ccdp-reference.md:85:O(N²) communication explosion and gives a single point of protocol
protocols/ccdp/visual-guide/ccdp-reference.md:121:| 3 | Epistemic | *No TCP/IP analog — CCDP's novel layer* | Provenance grades and evidence, escalation semantics, decomposition plans, cost and resource signals |
protocols/ccdp/visual-guide/ccdp-reference.md:124:**Key point for the visual:** Layer 3 (Epistemic) is what makes CCDP
protocols/ccdp/visual-guide/ccdp-reference.md:125:different from every existing protocol. It's the layer that carries "how much
protocols/ccdp/visual-guide/ccdp-reference.md:126:should you trust this answer" as a first-class protocol field. Color it
protocols/ccdp/visual-guide/ccdp-reference.md:198:A company deploys CCDP to handle mixed cognitive workloads — legal analysis,
protocols/ccdp/visual-guide/ccdp-reference.md:211:| Claude (LLM) | Mode 1 | `org.ccdp.language.generation`, `org.ccdp.language.translation` | ASSERTED | $0.01–0.50/req |
protocols/ccdp/visual-guide/ccdp-reference.md:212:| Z3 Prover | Mode 2 | `org.ccdp.deduction` | FORMALLY_VERIFIED | $0.001/req |
protocols/ccdp/visual-guide/ccdp-reference.md:213:| OptaPlanner | Mode 2 | `org.ccdp.planning` | COMPUTED | $0.01/req |
protocols/ccdp/visual-guide/ccdp-reference.md:214:| Code Verifier (LLM + test runner) | Mode 3 | `org.ccdp.code.verification` | VALIDATED | $0.10/req |
protocols/ccdp/visual-guide/ccdp-reference.md:215:| Legal Review Queue | Mode 4 | `org.ccdp.legal.compliance` | HUMAN_ATTESTED | $50/req, 2 days |
protocols/ccdp/visual-guide/ccdp-reference.md:216:| Decomposition Service (LLM + validator) | Mode 3 | `org.ccdp.decomposition` | VALIDATED | $0.05/req |
protocols/ccdp/visual-guide/ccdp-reference.md:217:| PostgreSQL | Mode 2 | `org.ccdp.data.retrieval` | COMPUTED | $0.0001/req |
protocols/ccdp/visual-guide/ccdp-reference.md:222:1. Request arrives at Dispatcher with `capability_type: org.ccdp.decomposition`.
protocols/ccdp/visual-guide/ccdp-reference.md:243:"Through CCDP" on the right. The left side shows what happens when you just
protocols/ccdp/visual-guide/ccdp-reference.md:244:ask an LLM. The right side shows what CCDP adds. Use a consistent layout:
protocols/ccdp/visual-guide/ccdp-reference.md:250:> The following views trace through CCDP's big ideas in the order they appear
protocols/ccdp/visual-guide/ccdp-reference.md:252:> plain LLM on the left, and through a CCDP system on the right. The
protocols/ccdp/visual-guide/ccdp-reference.md:254:> you *which parts of its answer you can trust* — and CCDP can.
protocols/ccdp/visual-guide/ccdp-reference.md:270:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:271:- Envelope: `capability_type = org.ccdp.legal.analysis`,
protocols/ccdp/visual-guide/ccdp-reference.md:282:**Takeaway:** Cognitive outputs are claims, not data. CCDP makes the evidence
protocols/ccdp/visual-guide/ccdp-reference.md:283:strength a first-class protocol field so you can tell the difference.
protocols/ccdp/visual-guide/ccdp-reference.md:298:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:299:- Dispatcher reads the envelope: `capability_type = org.ccdp.language.translation`.
protocols/ccdp/visual-guide/ccdp-reference.md:330:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:331:- Envelope: `capability_type = org.ccdp.planning`,
protocols/ccdp/visual-guide/ccdp-reference.md:357:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:370:**Takeaway:** Escalation is a first-class protocol operation, not an error.
protocols/ccdp/visual-guide/ccdp-reference.md:388:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:390:  (a) `org.ccdp.planning` — build the rota (→ constraint solver, grade:
protocols/ccdp/visual-guide/ccdp-reference.md:391:  FORMALLY_VERIFIED); (b) `org.ccdp.language.generation` — write the email
protocols/ccdp/visual-guide/ccdp-reference.md:419:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:424:- The audit trail is mandatory — it's a core protocol requirement, not an
protocols/ccdp/visual-guide/ccdp-reference.md:434:**Takeaway:** Audit is not a best practice — it's a protocol requirement.
protocols/ccdp/visual-guide/ccdp-reference.md:449:- *Tool descriptions are free text meant for LLM consumption. The protocol
protocols/ccdp/visual-guide/ccdp-reference.md:452:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:454:  (`org.ccdp.deduction`), input JSON Schema, output JSON Schema, cost hints
protocols/ccdp/visual-guide/ccdp-reference.md:477:- *Cost and deadline are infrastructure concerns, not protocol features.*
protocols/ccdp/visual-guide/ccdp-reference.md:479:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:490:- *Cost budgets and deadlines are protocol-level fields, not afterthoughts.
protocols/ccdp/visual-guide/ccdp-reference.md:493:**Takeaway:** TCP has congestion signals. A cognitive dispatch protocol needs
protocols/ccdp/visual-guide/ccdp-reference.md:494:cognitive-resource signals. CCDP has them.
protocols/ccdp/visual-guide/ccdp-reference.md:510:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:514:  `org.ccdp.language.generation` is rejected for `org.ccdp.deduction`.
protocols/ccdp/visual-guide/ccdp-reference.md:520:- *Security by protocol guarantee, not by implementation discipline.*
protocols/ccdp/visual-guide/ccdp-reference.md:522:**Takeaway:** A cognitive dispatch protocol with security as a protocol
protocols/ccdp/visual-guide/ccdp-reference.md:537:**Through CCDP:**
protocols/ccdp/visual-guide/ccdp-reference.md:550:  at a time. The protocol is the stable spine.*
protocols/ccdp/visual-guide/ccdp-reference.md:554:a protocol rewrite.
protocols/ccdp/visual-guide/ccdp-reference.md:560:CCDP maps to the classic OTP supervision-tree model:
protocols/ccdp/visual-guide/ccdp-reference.md:566:- **Services** = worker processes. Supervised, typed protocol on the wire.
protocols/ccdp/visual-guide/ccdp-reference.md:580:## Section 8: Why Existing Protocols Aren't Enough (for a "Why CCDP?" view)
protocols/ccdp/visual-guide/ccdp-reference.md:585:| **A2A** | Agent Cards for discovery. Task lifecycle. Opacity principle. | Assumes both sides are agents. CCDP's dispatcher is deliberately not an agent — it's a constrained coordinator. Peer-to-peer topology doesn't match CCDP's star. |
protocols/ccdp/visual-guide/ccdp-reference.md:587:| **FIPA-ACL** | Typed communicative acts — exactly the right concept. | Never escaped the lab. No verifiable identity, no governance, no runtime tooling. CCDP inherits the insight, designs against the failure modes. |
protocols/ccdp/visual-guide/ccdp-reference.md:597:- **4** protocol layers (Transport, Routing/Audit, Epistemic, Content)
protocols/ccdp/visual-guide/ccdp-reference.md:599:- **14** CCDP-specific error codes (plus 5 standard JSON-RPC codes)
protocols/ccdp/visual-guide/ccdp-reference.md:614:- **Escalation** (not "error", not "failure") — it's a first-class protocol
protocols/ccdp/visual-guide/ccdp-reference.md:620:  `org.ccdp.deduction`
protocols/ccdp/json/FINDINGS.md:1:# FINDINGS — CCDP v0.2 JSON/JSON-RPC Extraction
protocols/ccdp/json/FINDINGS.md:4:JSON surface of CCDP draft v0.2 (document version 0.2.0). **Descriptive, not
protocols/ccdp/json/FINDINGS.md:6:written, and these findings feed CCDP v0.3 and the ccdp-rs grammar audit.
protocols/ccdp/json/FINDINGS.md:138:**F-12 [gap] — `org.ccdp.notification`** appears as `capability_type` in the
protocols/ccdp/json/FINDINGS.md:140:types, and `org.ccdp.*` is a reserved namespace (§7.3.1).
protocols/ccdp/json/FINDINGS.md:195:requester's signature travels; the `org.ccdp.signature` metadata key is
protocols/ccdp/json/FINDINGS.md:214:keys in `org.ccdp.dispatcher.*`". But §13.4.1 has the Dispatcher append
protocols/ccdp/json/FINDINGS.md:215:`org.ccdp.escalation_history` and `org.ccdp.partial_results` to the forwarded
protocols/ccdp/json/FINDINGS.md:216:Request's `metadata` — keys *not* under `org.ccdp.dispatcher.*`, hence inside
protocols/ccdp/json/FINDINGS.md:240:claim rather than a CCDP message, but it is a monetary-value example in the
protocols/ccdp/json/FINDINGS.md:258:in the Capability Record, §8.2.2) while §7.2's table routes `ccdp/
protocols/ccdp/src/15-security.md:5:CCDP defines security requirements at the protocol level — authentication, authorization, token scoping, message signing, and replay protection. Some requirements (mutual TLS, bearer-token validation) are enforced by the protocol infrastructure. Others (isolation, workload attestation) are protocol requirements that depend on deployment infrastructure for enforcement. The protocol specifies what must be true; the deployment ensures it is true.
protocols/ccdp/src/15-security.md:7:This design choice is a direct response to the MCP release-candidate retrospective [MCP-2026-07-28], which found that a security posture "highly dependent on implementation discipline rather than protocol guarantees" fails unpredictably across deployments.
protocols/ccdp/src/15-security.md:9:Every CCDP deployment MUST implement the security requirements in this section. There are no "development mode" exceptions in the specification — while individual deployments MAY relax requirements in non-production environments, the protocol defines a security floor that conforming implementations MUST meet in production.
protocols/ccdp/src/15-security.md:28:- Tokens MUST be scoped to specific Capability Types. A token authorized for `org.ccdp.language.generation` MUST NOT be accepted for `org.ccdp.deduction`.
protocols/ccdp/src/15-security.md:30:- Tokens SHOULD be issued by an OAuth 2.0 authorization server using Pushed Authorization Requests [RFC 9126] with PKCE [RFC 7636]. Implementations requiring issuer validation SHOULD follow [RFC 9207]. PAR and PKCE apply to the authorization-code issuance flow — how tokens are obtained from the authorization server. They are best practices for token issuance, not requirements for token validation. The Dispatcher, acting as a resource server, validates tokens by checking signature/introspection, expiration, audience, and scope (Section 15.3). The issuance-flow recommendations apply to authorization-server deployments, not to CCDP Dispatchers.
protocols/ccdp/src/15-security.md:61:  "scope": ["org.ccdp.deduction", "org.ccdp.language.*"],
protocols/ccdp/src/15-security.md:73:Wildcard scopes (e.g., `org.ccdp.language.*`) match all subtypes. Wildcard scope matching uses the following grammar: a scope pattern ending in `.*` matches any scope string that begins with the prefix (everything before `.*`) followed by a dot and one or more additional segments. `org.ccdp.language.*` matches `org.ccdp.language.generation` but not `org.ccdp.language` or `org.ccdp.*`. Exact-match scopes take precedence over wildcard matches. A token with scope `["org.ccdp.language.generation"]` is authorized for that specific capability; a token with scope `["org.ccdp.language.*"]` is authorized for any capability under that prefix.
protocols/ccdp/src/15-security.md:83:For environments requiring end-to-end integrity (the requester must be able to verify that the Service's response was not modified by the Dispatcher or any intermediary), CCDP supports application-level message signing:
protocols/ccdp/src/15-security.md:90:    "org.ccdp.signature": {
protocols/ccdp/src/15-security.md:104:**Signed components.** The `signed_fields` array identifies top-level components of the CCDP message to include in the signature. Valid values are `"envelope"` and `"content"`. The signature input is the JCS [RFC 8785] canonical form of a JSON object containing exactly those components:
protocols/ccdp/src/15-security.md:112:Message signing is OPTIONAL for CCDP Core conformance. For CCDP Full conformance, message signing is REQUIRED for Services that produce responses at grade FORMALLY_VERIFIED or HUMAN_ATTESTED, and RECOMMENDED for all other Services. For deployments spanning untrusted administrative domains (different organizations, different cloud regions), message signing is REQUIRED regardless of conformance level.
protocols/ccdp/src/15-security.md:122:CCDP defines two signing profiles with different mutable-field sets:
protocols/ccdp/src/15-security.md:124:**Requester-outbound profile.** The requester signs before the Dispatcher processes the message. Mutable fields excluded from `envelope` before signing: `audit` (added by the Dispatcher on receipt; absent on the requester's outbound envelope), `remaining_budget_ms` (decremented by the Dispatcher at each subsequent hop, per Section 12.4), `destination_id` (excluded only when null or absent at signing time; a non-null requester-specified `destination_id` is part of the requester's routing intent and MUST remain in the signed envelope — see Section 7.3.2 and Section 9.2 Step 1), and metadata keys in `org.ccdp.dispatcher.*`. All other envelope fields are immutable after signing. The Dispatcher verifies the Requester's signature — computed over the envelope as the requester sent it — before processing.
protocols/ccdp/src/15-security.md:126:**Service-response profile.** The service signs its response. Mutable fields excluded from `envelope` before signing: `audit` (added or updated by the Dispatcher when forwarding the response, per Section 7.5) and metadata keys in `org.ccdp.dispatcher.*`. The `remaining_budget_ms` field is not present on response messages. All other envelope fields, and the entire `content` object, are immutable after signing. The `signed_fields` array MUST include both `"envelope"` and `"content"`. The Dispatcher verifies the Service's signature before forwarding to the Requester.
protocols/ccdp/src/15-security.md:159:Isolation requirements declared in Registry metadata are *policy inputs*, not protocol-enforceable guarantees. The Dispatcher trusts that the deployment infrastructure enforces isolation as declared. For deployments requiring stronger assurance, implementations SHOULD support workload attestation — the Service provides a signed attestation (e.g., via a Trusted Platform Module or confidential-computing attestation service) that its runtime environment matches the declared isolation requirements. Attestation verification is an optional Full-conformance feature.
protocols/ccdp/src/15-security.md:174:The MCP fault taxonomy study identified tool naming collisions as an attack vector — malicious entries in public registries with names that shadow legitimate tools. CCDP mitigates this through:
protocols/ccdp/src/15-security.md:183:Services that require credentials (API keys, database passwords, etc.) MUST NOT receive them through the CCDP protocol. Credentials are provisioned through out-of-band mechanisms (environment variables, secret managers, key vaults). The CCDP protocol carries authentication tokens for *protocol-level* identity, not application-level credentials.
protocols/ccdp/src/15-security.md:197:Rate limiting parameters are deployment-configured, not protocol-specified.
protocols/ccdp/json/MANIFEST.md:1:# CCDP JSON / JSON-RPC Corpus — MANIFEST
protocols/ccdp/json/MANIFEST.md:4:JSON-RPC surface of the CCDP specification: every fenced JSON example
protocols/ccdp/json/MANIFEST.md:9:JSON Schemas (ccdp-rs) will be checked against. Nothing in the spec was
protocols/ccdp/json/MANIFEST.md:12:**Spec revision extracted against.** CCDP Draft v0.2, document version 0.2.0
protocols/ccdp/json/MANIFEST.md:13:(§2.1), wire protocol version `"1.0"`; README states "review iterations
protocols/ccdp/json/MANIFEST.md:47:| 06-protocol-layers.md | 0 | 0 | — |
protocols/ccdp/src/README.md:1:# CCDP: Composite Cognition Dispatch Protocol — Specification v0.2
protocols/ccdp/src/README.md:10:2. **Conventions** — RFC 2119/8174 language, JSON format, naming conventions, document vs. wire-protocol versioning
protocols/ccdp/src/README.md:19:11. **Audit Trail** — Mandatory audit as core protocol, record-level and per-message-type field tables, audit store failure behavior, W3C Trace Context, storage requirements
protocols/ccdp/src/README.md:21:13. **Error Handling and Escalation** — Error taxonomy (protocol/service/epistemic), error codes, escalation reasons, escalation chain processing (with authorization/budget/isolation checks), health monitoring, circuit breakers
protocols/ccdp/src/README.md:33:- **Coordinator Dispatcher / structural protocol:** The Dispatcher is a constrained protocol enforcement and execution coordinator — it performs structural operations (routing, schema validation, decomposition plan execution, typed-reference resolution) but never semantic interpretation of Content
protocols/ccdp/src/README.md:38:- **Security as a protocol-level requirement:** Security properties are protocol-level requirements; production enforcement depends on deployment infrastructure, key management, and operational practices beyond the protocol's scope. Mutual authentication and token scoping required at all levels; message signing required for Full conformance with high-grade provenance and cross-domain deployments
protocols/ccdp/src/21-version-history.md:9:The wire protocol version remains `"1.0"` during this draft cycle.
protocols/ccdp/src/21-version-history.md:13:Distinguished document version (`0.2.0`) from wire protocol version (`"1.0"`).
protocols/ccdp/src/21-version-history.md:26:protocol enforcement and execution coordinator, not a cognitive reasoner.
protocols/ccdp/src/21-version-history.md:80:no-candidate unavailability routes directly to `org.ccdp.human_review`.
protocols/ccdp/src/21-version-history.md:86:Added `org.ccdp.composition` capability for semantic composition. Added
protocols/ccdp/src/14-decomposition.md:7:CCDP resolves this by treating decomposition as a first-class Service: a dedicated Decomposition Service with Capability Type `org.ccdp.decomposition` that receives complex requests and emits structured Decomposition Plans. The Dispatcher routes to the Decomposition Service first, then routes each sub-request from the plan independently. The Dispatcher performs only structural operations — routing, dependency resolution, typed result-reference substitution; the decomposition intelligence lives in a dedicated, auditable Service behind a typed interface.
protocols/ccdp/src/14-decomposition.md:13:**Explicit decomposition request.** The requester sets `capability_type` to `org.ccdp.decomposition`, indicating that the request should be decomposed rather than handled directly. The Dispatcher routes to the Decomposition Service, receives a plan, and executes it.
protocols/ccdp/src/14-decomposition.md:32:    "schema_ref": "org.ccdp.decomposition/output/v1",
protocols/ccdp/src/14-decomposition.md:40:          "capability_type": "org.ccdp.language.translation",
protocols/ccdp/src/14-decomposition.md:59:          "capability_type": "org.ccdp.deduction",
protocols/ccdp/src/14-decomposition.md:77:          "capability_type": "org.ccdp.language.translation",
protocols/ccdp/src/14-decomposition.md:182:- `"selection"`: Select the best sub-result by a criterion (useful for cross-checking). Allowed `selection` criteria are: `"highest_provenance"` (select the sub-result with the highest provenance grade in policy order), `"lowest_cost"` (select the sub-result with the lowest reported cost), `"first_completed"` (select the first sub-result to arrive — for latency-optimized redundant dispatch). Other selection criteria require routing to an `org.ccdp.composition` Service, as they may involve semantic judgment.
protocols/ccdp/src/14-decomposition.md:185:When `method` is `"custom"`, the composition payload is routed to a Service with capability type `org.ccdp.composition`. This is a well-known capability type (added to Section 8.3's well-known types). The Composition Service receives the sub-results and the composition specification and returns the composed result. The Dispatcher does not perform custom composition itself — it delegates to the Composition Service and forwards the result.
protocols/ccdp/src/14-decomposition.md:227:3. **Dispatch independent sub-requests.** For each sub-request with no dependencies (or all dependencies satisfied), create a CCDP Request with:
protocols/ccdp/src/14-decomposition.md:241:**Composition boundary.** For `template`, `concatenation`, and `selection` composition methods, the Dispatcher performs structural assembly: it places sub-results into the template slots, concatenates them in order, or selects by a typed criterion (highest provenance grade, lowest cost). These are mechanical operations on typed wrappers, consistent with the Coordinator Dispatcher model. The Dispatcher MUST NOT perform composition that requires reasoning about content meaning — such composition MUST be routed to an `org.ccdp.composition` Service.
protocols/ccdp/src/14-decomposition.md:251:The Decomposition Service implements Capability Type `org.ccdp.decomposition` with:
protocols/ccdp/src/14-decomposition.md:253:**Input schema:** A CCDP Request (the request to be decomposed). The Decomposition Service receives the original Content and must emit a valid Decomposition Plan.
protocols/ccdp/src/14-decomposition.md:265:A sub-request in a Decomposition Plan MAY itself have `capability_type: "org.ccdp.decomposition"`, producing a nested decomposition. The Dispatcher handles this recursively: the sub-decomposition produces its own plan, which the Dispatcher executes as a nested sub-tree of the parent plan.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2:title: "CCDP: Composite Cognition Dispatch Protocol"
protocols/ccdp/composite-cognition-dispatch-protocol.md:4:  A message-envelope protocol for routing cognitive requests through a
protocols/ccdp/composite-cognition-dispatch-protocol.md:146:This document specifies the Composite Cognition Dispatch Protocol (CCDP), a message-envelope protocol for routing cognitive requests through a deliberately constrained dispatcher to a heterogeneous set of cognitive services — large language models, theorem provers, classical planners, databases, human review queues, and composite LLM+service hybrids — under human supervision. CCDP is a supervision-tree protocol, not an agent-to-agent protocol: one side of every link is a protocol enforcement and execution coordinator that reads envelope metadata and performs structural operations — routing, schema validation, plan execution, audit — without reasoning about message content. The protocol carries the intelligence the dispatcher does not have.
protocols/ccdp/composite-cognition-dispatch-protocol.md:148:CCDP's novel contribution is the epistemic dimension of cognitive dispatch. All RESPONSE, ESCALATION, and DECOMPOSITION_RESULT messages that carry cognitive outputs include a provenance grade indicating the evidence strength behind them — from opaque assertion through formal verification to human attestation — with defined composition rules for how grades propagate through multi-service operations. Error responses, health messages, and notifications do not carry provenance. Escalation is a first-class protocol operation, not an error state: a service that cannot meet the requested provenance grade returns a structured escalation that the dispatcher routes upward. Structured audit metadata is mandatory for every Dispatcher-mediated hop — every message the Dispatcher processes generates an audit record. Typed service contracts are enforced through a capability registry with schema versioning and compatibility checking.
protocols/ccdp/composite-cognition-dispatch-protocol.md:150:CCDP layers on HTTP transport and JSON-RPC 2.0 wire format, adding an envelope-based routing layer with provenance, audit, cost signals, health monitoring, and deadline propagation. It is designed so that a conforming Dispatcher can be implemented as a self-contained-message coordinator with no natural-language understanding — each message carries all context needed for processing; however, high-availability deployments require shared operational state for replay caches, circuit-breaker state, health tables, and audit stores (Section 5.1) — while conforming Services range from stateless functions to long-running human review queues behind the same typed interface. Security and audit properties in this specification are protocol-level requirements; production deployments must additionally address operational security, key management, monitoring, and infrastructure hardening beyond the protocol's scope.
protocols/ccdp/composite-cognition-dispatch-protocol.md:158:This document specifies a protocol for composite cognition dispatch. It is published as an initial specification for examination, implementation feedback, and iterative refinement. Distribution is unlimited.
protocols/ccdp/composite-cognition-dispatch-protocol.md:162:This specification uses two independent version identifiers. The **document version** (currently 0.2.0) tracks the maturity of the specification text — its completeness, internal consistency, and review status. The **wire protocol version** (currently `"1.0"`, carried in every message's `envelope.ccdp_version` field) tracks the on-the-wire format. A document revision that clarifies prose, tightens conformance requirements, or adds non-breaking features increments the document version without changing the wire protocol version. A document revision that changes message structure, adds required envelope fields, or alters wire semantics increments the wire protocol version. Implementations negotiate by wire protocol version; the document version is for human readers and specification governance.
protocols/ccdp/composite-cognition-dispatch-protocol.md:164:**Pre-1.0 stability note.** While the document version is below 1.0 and no conforming implementations have been declared, document revisions MAY change wire-visible semantics without incrementing the wire protocol version. The wire protocol version will be frozen at the first stable release (document version 1.0.0). Implementers building against pre-1.0 drafts should track the document version and expect wire-level changes between revisions.
protocols/ccdp/composite-cognition-dispatch-protocol.md:180:Where examples show JSON structures with `// ...` comments, the comments are an expository convenience and are not valid JSON. Implementations MUST NOT include comments in wire-format messages. The content type for all CCDP messages is `application/json` with charset UTF-8. JSON numbers follow IEEE 754 double-precision semantics; implementations requiring higher precision for monetary or cryptographic values SHOULD use string-encoded representations. Enumeration values in this specification use `UPPER_SNAKE_CASE` (e.g., `FORMALLY_VERIFIED`, `SEARCH_EXHAUSTED`). Reverse-domain identifiers (capability types, metadata namespace keys) use dot-separated segments (e.g., `org.ccdp.language.generation`). These conventions are normative for wire-format values.
protocols/ccdp/composite-cognition-dispatch-protocol.md:182:Monetary values in CCDP messages SHOULD be represented as strings (e.g., `"4.50"` not `4.50`) to avoid IEEE 754 floating-point precision loss. All monetary-value examples in this specification use string representations.
protocols/ccdp/composite-cognition-dispatch-protocol.md:196:All examples in this specification are informative unless explicitly marked as normative. Tables that list requirements use normative language (MUST, SHOULD, MAY) and carry the same force as prose requirements. Diagrams are informative aids. Design notes (marked as such) are informative rationale and do not create protocol obligations.
protocols/ccdp/composite-cognition-dispatch-protocol.md:208:This architecture requires a protocol. Requests must flow from humans (and from services making sub-requests) through a central point — the dispatcher — to the appropriate service, with responses flowing back. The dispatcher must route correctly, and every link in the chain must be auditable. The protocol must carry enough structure for routing decisions to be classification rather than reasoning — the dispatcher reads envelopes, not content.
protocols/ccdp/composite-cognition-dispatch-protocol.md:210:No existing protocol jointly satisfies these requirements: typed envelope routing through a non-cognitive coordinator, mandatory structured audit, provenance grades with composition rules, first-class escalation, cost and deadline signals, and a capability registry with schema versioning.
protocols/ccdp/composite-cognition-dispatch-protocol.md:218:MCP is the closest existing protocol to CCDP's problem space — it connects language models to external capabilities. Its core abstraction (tools, resources, prompts) covers most LLM-to-service interactions.
protocols/ccdp/composite-cognition-dispatch-protocol.md:224:**Designed for smart consumers, not dumb dispatchers.** MCP assumes the client (the LLM/host) is the intelligent party — it interprets natural-language tool descriptions, decides which tools to invoke, and manages conversation flow. Tool descriptions are free-text strings meant for LLM consumption. For CCDP's dispatcher — a protocol enforcement and execution coordinator that operates on envelope metadata — MCP carries insufficient routing structure. The protocol intelligence lives in the consumer, not the envelope.
protocols/ccdp/composite-cognition-dispatch-protocol.md:226:**No mandatory audit.** MCP does not mandate structured logging of tool invocations. Audit trails are implementation concerns. For a supervision-tree architecture where every link must be inspectable, audit metadata must be a core protocol field, not an afterthought.
protocols/ccdp/composite-cognition-dispatch-protocol.md:228:**No cost or resource signals.** MCP provides no protocol-native required cost budget, consumption reporting, or deadline enforcement fields comparable to CCDP's flow-control layer (Section 12). A dispatcher cannot make resource-rational routing decisions without this information. TCP has congestion signals (ECN, window advertisements); a cognitive dispatch protocol needs cognitive-resource signals.
protocols/ccdp/composite-cognition-dispatch-protocol.md:230:**Security by implementation discipline.** MCP's security posture is highly dependent on implementation discipline rather than protocol guarantees: no mandated authentication, tool parameter injection enabling arbitrary code execution, tool naming collisions exploitable from public registries. The 2026-07-28 spec adds OAuth 2.0 authorization with Pushed Authorization Requests [RFC 9126] and PKCE [RFC 7636], but the "security by convention" orientation persists.
protocols/ccdp/composite-cognition-dispatch-protocol.md:232:**No epistemic dimension.** Most fundamentally, MCP treats service outputs as data. CCDP treats them as *claims with epistemic status*. A prover's output and an LLM's output are structurally different kinds of evidence, and the protocol must carry that distinction. MCP has no concept of provenance grades, evidence strength, or provenance-grade-below-threshold escalation (CCDP's `PROVENANCE_BELOW_REQUIREMENT` escalation reason).
protocols/ccdp/composite-cognition-dispatch-protocol.md:239:A2A's limitation for CCDP is that it assumes both sides of a link are *agents* — capable, autonomous entities that negotiate and decide. CCDP's dispatcher is deliberately not an agent. It is a protocol enforcement and execution coordinator. A2A's complexity (Agent Card infrastructure, multi-transport support, autonomous negotiation) is overkill for a system where one side is a constrained coordinator, not an autonomous agent, and its peer-to-peer topology does not match CCDP's star topology.
protocols/ccdp/composite-cognition-dispatch-protocol.md:246:gRPC's *implementation complexity* works against the constrained-coordinator principle. Schema version management is a chronic operational wound — the discipline of never reusing field numbers, managing `.proto` file distribution, and coordinating the protoc toolchain across heterogeneous services creates friction that compounds. A dispatcher should not need a protoc toolchain. CCDP adopts gRPC's design principles (typed contracts, deadline propagation, interceptor-style audit) without its implementation weight, using JSON Schema for runtime-validatable contracts and JSON-RPC 2.0 for the wire format.
protocols/ccdp/composite-cognition-dispatch-protocol.md:253:FIPA-ACL never escaped the lab. It lacked verifiable identity, governance frameworks, runtime tooling, and practical deployment paths. It was described in a comprehensive survey as having limited practical deployment despite its formal elegance [arXiv-Agent-Comms]. CCDP inherits FIPA's insight — speech acts as message types — while designing explicitly against its failure modes: every protocol feature must be practically deployable with minimal tooling, not formally elegant in isolation.
protocols/ccdp/composite-cognition-dispatch-protocol.md:258:The distinction between cognitive dispatch and data routing — the reason CCDP cannot be a thin layer over an existing RPC framework — lies in three properties unique to cognitive output:
protocols/ccdp/composite-cognition-dispatch-protocol.md:260:**Cognitive outputs are claims, not data.** A database query returns a fact. A theorem prover returns a proof. An LLM returns a plausible completion. These are structurally different kinds of evidence, and a protocol that treats them identically forces the consumer to reconstruct epistemic status from scratch at every boundary. CCDP makes provenance a first-class protocol field: every response carries a grade indicating the evidence strength behind it, with defined composition rules for multi-service operations (Section 10).
protocols/ccdp/composite-cognition-dispatch-protocol.md:262:**Provenance-grade insufficiency is a routing event, not an error.** When a cognitive service cannot produce output at the requested provenance grade, this is not a failure — it is information. "I can generate candidate solutions but cannot verify them" is a legitimate, structured response that the dispatcher should route to a verification service or escalate to a human. CCDP defines escalation as a protocol message type with structured routing semantics (Section 13).
protocols/ccdp/composite-cognition-dispatch-protocol.md:264:**The specification-recursion problem.** Formal verification relocates error rather than eliminating it: "did we build it right?" becomes "did we specify the right thing?" [Vericoding; Goodhart 1975] — a compound citation per Section 2's convention, resolving to the separately defined [Vericoding] and [Goodhart 1975] entries. An LLM that games a weak specification into a vacuous proof is not a verification failure — it is a Goodhart failure. CCDP's provenance system is designed with this recursion in mind: a grade of FORMALLY_VERIFIED carries a scope field binding it to a specific specification, and the specification's own provenance is separately tracked (Section 10).
protocols/ccdp/composite-cognition-dispatch-protocol.md:269:CCDP is governed by eight principles, each grounded in the research base:
protocols/ccdp/composite-cognition-dispatch-protocol.md:273:2. **The end-to-end principle applies.** The dispatcher verifies *protocol* correctness (well-formed envelopes, valid routing, schema compliance, timeout enforcement) — structural validation, not semantic interpretation (Section 4). *Content* correctness is the service's responsibility. (From: Saltzer, Reed & Clark 1984.)
protocols/ccdp/composite-cognition-dispatch-protocol.md:275:3. **Audit is mandatory, not optional.** Every message passing through the dispatcher gets structured audit metadata. This is core protocol behavior, not an extension. (From: the general security principle that implementation-discipline-dependent security fails unpredictably across deployments — see Section 3.2.1's analysis of MCP.)
protocols/ccdp/composite-cognition-dispatch-protocol.md:277:4. **Provenance grades are first-class.** Every response carries an evidence-strength field. This is the protocol's novel contribution. (From: the Spence signaling theory — quality signals work only when expensive to fake.)
protocols/ccdp/composite-cognition-dispatch-protocol.md:279:5. **Escalation is a protocol operation, not an error.** Services can return "I cannot handle this at the required provenance grade" as a structured escalation that the dispatcher routes upward. (From: OTP supervision — escalation through the supervision tree is normal operation.)
protocols/ccdp/composite-cognition-dispatch-protocol.md:283:7. **Extensibility without breakage.** Unknown metadata fields are preserved and forwarded. New capabilities are added as metadata keys without protocol version bumps. (From: TCP options field, HTTP headers, protobuf unknown-field forwarding.)
protocols/ccdp/composite-cognition-dispatch-protocol.md:285:8. **Security by default.** Mutual authentication and bearer-token scoping are required at all conformance levels. Message signing is required for Full conformance when producing high-grade provenance (FORMALLY_VERIFIED, HUMAN_ATTESTED) and for all cross-administrative-domain deployments. See Section 15 for the complete signing policy. (From: NSA/CISA findings — security as a protocol guarantee, not an implementation choice.)
protocols/ccdp/composite-cognition-dispatch-protocol.md:290:CCDP specifies the message format, routing semantics, registry interface, provenance system, audit requirements, and security baseline for communication between a dispatcher and cognitive services. It does not specify:
protocols/ccdp/composite-cognition-dispatch-protocol.md:296:- The specific set of capability types (which are registry-managed, not protocol-defined; the protocol defines a set of well-known Capability Types as extension points in Section 8.3; the registry manages the open-ended set of deployment-specific types)
protocols/ccdp/composite-cognition-dispatch-protocol.md:298:CCDP is transport-layer agnostic in principle but specifies HTTP as the REQUIRED base transport and JSON-RPC 2.0 as the REQUIRED wire format. Future specifications MAY define bindings for other transports.
protocols/ccdp/composite-cognition-dispatch-protocol.md:305:**Artifact Reference.** The `artifact_ref` object within an Evidence entry (see Evidence Entry below), pointing to a verifiable artifact (proof object, test result, signed attestation) stored outside the CCDP message. Artifact References appear in Evidence entries. The `uri` form, dereference authority, and access-control requirements are deployment-defined. Artifact References SHOULD include an `integrity` hash for content verification (REQUIRED at grades VALIDATED and above — see Evidence Entry). Artifact References that appear in audit records are subject to the audit retention policy (Section 11.5). Deployments MUST ensure that referenced artifacts remain resolvable for the configured audit retention period. Decomposition Plan result references (Section 14.3.3) are typed JSON Pointer `$ref` objects and are not Artifact References.
protocols/ccdp/composite-cognition-dispatch-protocol.md:307:**Audit Record.** A structured log entry created by the Dispatcher for each message processed. Audit Records are defined in Section 11. Each Audit Record carries an `audit_schema_version` field independent of the CCDP document and wire versions.
protocols/ccdp/composite-cognition-dispatch-protocol.md:317:**Capability Type.** A string identifier for a class of cognitive function, using reverse-domain notation (e.g., `org.ccdp.deduction`, `org.ccdp.planning`). The protocol defines a set of well-known Capability Types (Section 8.3); implementations MAY define additional types through the Registry.
protocols/ccdp/composite-cognition-dispatch-protocol.md:319:**Conformance Level.** The level of protocol compliance claimed by an implementation: Core (all Core requirements) or Full (all Core requirements plus all Full requirements). Conformance is defined by explicit, stably-identified requirement tables (e.g. `DISP-CORE-NNN`, `DISP-FULL-NNN` for the Dispatcher), not by the sum of all SHOULD statements in the specification. Service and Registry stable IDs are an open item (Section 18). See Section 16.4.
protocols/ccdp/composite-cognition-dispatch-protocol.md:321:**Content.** The payload of a CCDP message — the actual cognitive input or output. Content is opaque to the Dispatcher: the Dispatcher MUST NOT interpret, transform, or make routing decisions based on Content. The Dispatcher MAY perform structural validation of Content against JSON Schemas and MAY resolve typed references within Content wrappers during decomposition plan execution (Section 14). These are structural operations, not semantic interpretation (see Structural Validation vs Semantic Interpretation below). Content structure is governed by the Capability Record's input and output schemas.
protocols/ccdp/composite-cognition-dispatch-protocol.md:325:**Decomposition.** The process of breaking a complex request into a set of typed sub-requests, each routable to a different Service. Decomposition is performed by a Decomposition Service — a Service whose Capability Type is `org.ccdp.decomposition` — and produces a Decomposition Plan (Section 14).
protocols/ccdp/composite-cognition-dispatch-protocol.md:329:**Dispatcher.** The central routing component of a CCDP system. The Dispatcher receives messages, reads their Envelopes, makes routing decisions based on Envelope metadata and Registry lookups, forwards messages to Services, and manages audit logging, health monitoring, and escalation routing. The Dispatcher is a constrained protocol enforcement and execution coordinator. It performs structural operations — routing, schema validation, decomposition plan execution, typed-reference resolution, structural result assembly, audit logging, health monitoring, and deadline/budget enforcement — but has no cognitive capability. It never reasons about what content means. A conforming Dispatcher MUST NOT reason about message Content (Section 16.1).
protocols/ccdp/composite-cognition-dispatch-protocol.md:331:**Envelope.** The structured metadata portion of a CCDP message, containing all information the Dispatcher needs for routing, audit, and protocol enforcement. The Envelope includes message type, identity and tracing fields, routing fields, constraint fields, provenance fields, audit fields, and extensible metadata. The Dispatcher reads the Envelope and selected structural Content fields (schema shapes, typed references) to route and process messages (Section 7).
protocols/ccdp/composite-cognition-dispatch-protocol.md:333:**Escalation.** A structured protocol response indicating that the originating actor (a Service or the Dispatcher) determined that a request cannot be fulfilled at the required provenance grade or capability level. An Escalation is a first-class message type (not an error), carrying the reason for escalation, the achieved provenance grade (if any), a partial result (if available), and a suggested routing target. The Dispatcher routes Escalations according to the Escalation Chain (Section 13). The `escalation_origin` field (Section 7.3.4) distinguishes Service-generated from Dispatcher-generated Escalations.
protocols/ccdp/composite-cognition-dispatch-protocol.md:345:    "uri": "urn:ccdp:artifact:abc123",
protocols/ccdp/composite-cognition-dispatch-protocol.md:373:**Message.** The atomic unit of CCDP communication. A Message consists of an Envelope and a Content payload, encoded as a JSON-RPC 2.0 request or response. Messages are typed by the `envelope.type` field (Section 7).
protocols/ccdp/composite-cognition-dispatch-protocol.md:375:**Message Type.** The classification of a CCDP Message by its protocol function. The defined Message Types are: REQUEST, RESPONSE, NOTIFICATION, ESCALATION, HEALTH_REQUEST, HEALTH_RESPONSE, and DECOMPOSITION_RESULT (Section 7.2).
protocols/ccdp/composite-cognition-dispatch-protocol.md:385:**Registry.** The Capability Registry — a service that stores Capability Records, enforces schema versioning and compatibility, and answers routing queries from the Dispatcher. The Registry is a CCDP infrastructure component, not a cognitive Service. Its interface is defined in Section 8; its implementation is not specified.
protocols/ccdp/composite-cognition-dispatch-protocol.md:387:**Request.** A CCDP Message of type REQUEST, carrying a cognitive task from a requester (human or Service) to the Dispatcher for routing to an appropriate Service.
protocols/ccdp/composite-cognition-dispatch-protocol.md:389:**Requester.** The human, application, or Service that originates a CCDP Request. The Requester is identified by the `source_id` envelope field and authenticated via bearer tokens (for external requesters) or mTLS identity (for Service-to-Service sub-requests).
protocols/ccdp/composite-cognition-dispatch-protocol.md:391:**Response.** A CCDP Message of type RESPONSE, carrying the result of a cognitive task from a Service back through the Dispatcher to the requester. Every Response MUST carry Provenance metadata (Section 10).
protocols/ccdp/composite-cognition-dispatch-protocol.md:393:**Service.** Any component that implements one or more Capabilities and communicates with the Dispatcher using the CCDP protocol. Services are heterogeneous: they may be LLM endpoints, theorem provers, classical planners, databases, human review queues, composite LLM+service hybrids, or Decomposition Services. From the Dispatcher's perspective, all Services expose the same typed interface — the Dispatcher does not know or care what implementation backs a Service (Section 5.3).
protocols/ccdp/composite-cognition-dispatch-protocol.md:403:**Trace Context.** The W3C Trace Context fields (`traceparent` and `tracestate`) propagated through CCDP messages to enable distributed tracing across the system. CCDP maps these to Envelope fields (Section 11.3).
protocols/ccdp/composite-cognition-dispatch-protocol.md:411:A CCDP system has a star topology with the Dispatcher at the center. All communication between Services passes through the Dispatcher. Services do not communicate directly with each other.
protocols/ccdp/composite-cognition-dispatch-protocol.md:443:This topology is a deliberate design choice, not a scaling constraint. The Dispatcher is the single point of protocol enforcement — authentication, routing, audit logging, health monitoring, and deadline enforcement all happen at the Dispatcher. A Service that bypasses the Dispatcher bypasses all of these guarantees.
protocols/ccdp/composite-cognition-dispatch-protocol.md:445:The star topology avoids the O(N²) communication explosion that full-mesh agent architectures face [arXiv-Agent-Comms]. With N services, CCDP requires N links (Dispatcher ↔ Service), not N(N-1)/2. The cost is that the Dispatcher is a single point of failure; high-availability deployment is an infrastructure concern outside this specification's scope, but the protocol's self-contained message design simplifies Dispatcher replication, though production deployments must address shared state for replay caches, circuit-breaker state, health tables, and audit-store consistency (see Section 15.5 and Section 13.6).
protocols/ccdp/composite-cognition-dispatch-protocol.md:450:A CCDP system comprises four kinds of components. Each has a defined role and a defined boundary of responsibility.
protocols/ccdp/composite-cognition-dispatch-protocol.md:455:The Dispatcher is the protocol's routing and enforcement engine. It is a constrained protocol enforcement and execution coordinator — not a reasoner. Its duties include structural operations that go beyond simple routing, but it has no cognitive capability and never reasons about message content. Its responsibilities are:
protocols/ccdp/composite-cognition-dispatch-protocol.md:479:A Service implements one or more Capabilities and communicates with the Dispatcher using CCDP messages. A Service's responsibilities are:
protocols/ccdp/composite-cognition-dispatch-protocol.md:488:**Idempotency scope.** The idempotency requirement is scoped by service type. Mode 2 (deterministic) Services MUST return byte-identical responses for the same `request_id`. Mode 1 (LLM) and Mode 3 (composite) Services MUST return semantically equivalent responses — the same conclusion at the same or higher provenance grade — but MAY differ in surface form (wording, formatting). For protocol conformance purposes, idempotency is tested by requiring Services to cache and return the original Response for duplicate `request_id` values within the idempotency window. Semantic equivalence is a service-quality property, not a protocol-testable conformance requirement. Mode 4 (human queue) Services MUST return the same response if the human has already submitted a result for the `request_id`; if the human has not yet responded, a retry with the same `request_id` is a no-op (the request remains in the queue). Implementations SHOULD maintain a response cache keyed by `request_id` with a configurable TTL (RECOMMENDED: 24 hours).
protocols/ccdp/composite-cognition-dispatch-protocol.md:511:The Human Supervisor occupies the top of the supervision tree. The Human Supervisor is not a CCDP component in the protocol sense — the protocol does not specify the human interface — but the protocol is designed to support human supervision:
protocols/ccdp/composite-cognition-dispatch-protocol.md:542:The most architecturally significant mode. An LLM sits in front of a deterministic service as a translator: it converts a natural-language request into the service's formal input language, passes it through, and converts the formal output back to natural language. From the Dispatcher's perspective, this is a single Service with a single typed interface — the internal LLM translation layer is not visible in the protocol.
protocols/ccdp/composite-cognition-dispatch-protocol.md:564:Mode substitution has multiple protocol-visible effects: Provenance Grade, cost (different modes have different cost profiles), latency, evidence entries (different methods produce different evidence), signing (some modes may not support signing), and isolation properties. For example, a Mode 2 replacement will report FORMALLY_VERIFIED where the Mode 4 predecessor reported HUMAN_ATTESTED. Consumers of the output can use the Provenance Grade to calibrate their trust — the protocol ensures the change in backing implementation is transparent through the epistemic metadata. Mode substitution is transparent only when the replacement mode satisfies the consumer's full `provenance_requirement` — including `min_policy_grade`, `required_methods`, `required_evidence_types`, and any signing or isolation requirements. A consumer requiring `HUMAN_ATTESTED` cannot be served by a Mode 2 replacement reporting `FORMALLY_VERIFIED` unless the consumer's policy accepts formal verification as equivalent for that claim type. Deployments SHOULD configure mode-substitution policies per capability type that account for all these effects, not just provenance.
protocols/ccdp/composite-cognition-dispatch-protocol.md:569:Decomposition — breaking a complex request into typed sub-requests — is itself a cognitive act. Rather than requiring the Dispatcher to perform decomposition (which would violate the constrained-coordinator principle) or requiring the human to pre-decompose all requests (which does not scale), CCDP treats decomposition as a first-class Service with Capability Type `org.ccdp.decomposition`.
protocols/ccdp/composite-cognition-dispatch-protocol.md:575:Result composition is a structural operation: the Dispatcher assembles typed sub-results according to the plan's composition specification (template assembly, concatenation, or selection) without reasoning about what the sub-results mean. When composition requires cognitive judgment (e.g., synthesizing sub-results into a coherent narrative), the plan routes composition to a dedicated Composition Service with capability type `org.ccdp.composition` (Section 14.3.4).
protocols/ccdp/composite-cognition-dispatch-protocol.md:586:CCDP's architecture maps to the classic supervision-tree model:
protocols/ccdp/composite-cognition-dispatch-protocol.md:590:- **Services are worker processes.** Each supervised, each with a typed protocol on its wire. They crash loudly (return structured errors or Escalations) rather than silently emitting corrupt output.
protocols/ccdp/composite-cognition-dispatch-protocol.md:592:- **Typed protocols on the wires.** Every link between Dispatcher and Service is a typed contract enforced by the Registry. Malformed messages are rejected at the boundary. This is the supervision tree's process isolation principle expressed as protocol enforcement.
protocols/ccdp/composite-cognition-dispatch-protocol.md:602:CCDP follows the TCP/IP tradition of layered protocol design: each layer provides a specific abstraction, relies only on the layer below, and can evolve independently. The layering is deliberate — it separates transport concerns (how bytes move) from routing concerns (where messages go) from epistemic concerns (how much to trust the result).
protocols/ccdp/composite-cognition-dispatch-protocol.md:604:Unlike TCP/IP, where the application layer is unspecified by the transport, CCDP's upper layers carry load-bearing protocol semantics. Provenance grades, audit metadata, and escalation semantics are not application concerns delegated to the endpoints — they are protocol-layer features enforced at the Dispatcher. This is the sense in which "the protocol is smart": the layers above transport carry intelligence that a Dispatcher with no cognitive capability of its own can enforce mechanically.
protocols/ccdp/composite-cognition-dispatch-protocol.md:609:CCDP defines four layers, mapped to their TCP/IP analogs:
protocols/ccdp/composite-cognition-dispatch-protocol.md:620:│  (CCDP's novel contribution — no TCP/IP analog)      │
protocols/ccdp/composite-cognition-dispatch-protocol.md:649:**HTTP is REQUIRED** as the base transport protocol. CCDP messages are HTTP POST requests to defined endpoints. HTTP was chosen for ubiquity: it works with all Service types (LLM endpoints, web services, queue systems), is supported by all programming languages, and composes with existing infrastructure (load balancers, proxies, monitoring).
protocols/ccdp/composite-cognition-dispatch-protocol.md:651:**JSON-RPC 2.0 is REQUIRED** as the wire format. Every CCDP message is a JSON-RPC 2.0 request or response, with CCDP-specific method names and parameter structures. JSON-RPC was chosen for simplicity: its specification fits on one page, it is transport-agnostic, and it imposes minimal parsing overhead on a Dispatcher with no cognitive capability of its own. Both MCP and A2A chose JSON-RPC 2.0 for the same reasons.
protocols/ccdp/composite-cognition-dispatch-protocol.md:655:**Authentication** is performed at this layer. The REQUIRED mechanism is mutual TLS (mTLS) for Dispatcher-to-Service authentication. Bearer tokens with scoped permissions MAY be used as an additional authorization mechanism (Section 15). Bearer-token validation is logically an application-level concern (it checks CCDP-scoped authorization, not transport-level connectivity). It is placed in the Transport Layer for implementation convenience: tokens are typically carried in HTTP headers and checked before message parsing begins. This is a pragmatic layer crossing, not a design error.
protocols/ccdp/composite-cognition-dispatch-protocol.md:657:Implementations MAY support additional transports (e.g., QUIC for latency-critical paths, WebSocket for long-lived connections) as protocol extensions, provided they satisfy the same reliability, encryption, and authentication guarantees. The Transport Layer is the most substitutable layer in the stack.
protocols/ccdp/composite-cognition-dispatch-protocol.md:685:Layer 2 carries protocol-enforcement fields. The required set varies by message type — see the per-message required-field matrix in Section 7.3. Fields such as `capability_type` and `destination_id` are required on REQUEST messages but absent or optional on RESPONSE, HEALTH, and NOTIFICATION messages. The Dispatcher reads Layer 2 fields for routing and protocol enforcement. It also reads selected Layer 3 fields (provenance requirements, cost budgets, deadlines) for routing policy and reads Layer 4 Content structurally for schema validation and typed-reference resolution. These cross-layer reads are enumerated in Section 6.5. The Dispatcher MUST NOT perform semantic interpretation of any layer's content.
protocols/ccdp/composite-cognition-dispatch-protocol.md:690:The Epistemic Layer carries the information that makes CCDP different from a generic RPC protocol. It has no TCP/IP analog — this is CCDP's novel contribution.
protocols/ccdp/composite-cognition-dispatch-protocol.md:733:- **Epistemic evolution**: Add new Evidence types or new composition rules without changing transport or routing. Adding new Provenance Grades is a protocol extension that requires a document version increment. New grades affect the ordering used for routing, conformance checking, and policy evaluation. The grade set is versioned through the Registry; deployments MUST ensure that all components recognize the grade vocabulary before grades are used in production. New epistemic fields are added as metadata extensions. Existing implementations that do not understand the new fields MUST preserve and forward them (Section 7.7).
protocols/ccdp/composite-cognition-dispatch-protocol.md:736:This independence is a direct application of the end-to-end principle: each layer does only what it must, and correctness guarantees that belong to a higher layer are not duplicated at a lower layer. The Dispatcher enforces protocol correctness (Layer 2); Services enforce content correctness (Layer 4); the Epistemic Layer (Layer 3) carries the metadata that connects them.
protocols/ccdp/composite-cognition-dispatch-protocol.md:741:Several sources describe a layered agent protocol stack forming: MCP for tool integration, A2A for agent coordination, WebMCP for browser interaction. CCDP's layering differs in three respects:
protocols/ccdp/composite-cognition-dispatch-protocol.md:743:**The Epistemic Layer has no counterpart.** The emerging stack has no protocol-level concept of provenance, evidence strength, or epistemic status. This is the gap CCDP fills — the recognition that cognitive outputs are claims with pedigree, not data with types.
protocols/ccdp/composite-cognition-dispatch-protocol.md:745:**The Dispatcher is a protocol enforcer, not a capable agent.** In the emerging stack, the "client" or "orchestrator" is assumed to be an intelligent agent. CCDP's Dispatcher is closer to a policy-enforcing message coordinator: it reads headers and structural metadata, applies routing policies, executes decomposition plans, validates schemas, and forwards processed messages — all without cognitive capability. The protocol carries the intelligence; the Dispatcher enforces it.
protocols/ccdp/composite-cognition-dispatch-protocol.md:747:**Audit is a layer concern, not an extension.** In the emerging stack, observability comes from bolting on OpenTelemetry or similar frameworks. In CCDP, audit fields are mandatory Layer 2 elements — the Dispatcher writes them as part of its core function, not as an opt-in integration.
protocols/ccdp/composite-cognition-dispatch-protocol.md:760:Every CCDP message is encoded as a JSON-RPC 2.0 [JSON-RPC] request or response, transported over HTTP POST. The JSON-RPC `method` field identifies the CCDP message type. Method-bearing messages (REQUEST, ESCALATION, NOTIFICATION, HEALTH_REQUEST, DECOMPOSITION_RESULT) carry CCDP data in the JSON-RPC `params` object. Response messages (RESPONSE, HEALTH_RESPONSE) carry CCDP data in the JSON-RPC `result` object.
protocols/ccdp/composite-cognition-dispatch-protocol.md:762:A CCDP Request encoded as JSON-RPC 2.0:
protocols/ccdp/composite-cognition-dispatch-protocol.md:767:  "method": "ccdp/request",
protocols/ccdp/composite-cognition-dispatch-protocol.md:776:A CCDP Response encoded as JSON-RPC 2.0:
protocols/ccdp/composite-cognition-dispatch-protocol.md:789:The JSON-RPC `id` field MUST match the CCDP `envelope.request_id`. This enables correlation at both the JSON-RPC layer and the CCDP layer.
protocols/ccdp/composite-cognition-dispatch-protocol.md:794:The following CCDP message types are defined:
protocols/ccdp/composite-cognition-dispatch-protocol.md:798:| `ccdp/request` | REQUEST | Requester → Dispatcher → Service | JSON-RPC request (`method`, `id`, `params`) |
protocols/ccdp/composite-cognition-dispatch-protocol.md:799:| `ccdp/escalation` | ESCALATION | Service → Dispatcher → Escalation target | JSON-RPC request (`method`, `id`, `params`) |
protocols/ccdp/composite-cognition-dispatch-protocol.md:800:| `ccdp/health.request` | HEALTH_REQUEST | Dispatcher → Service | JSON-RPC request (`method`, `id`, `params`) |
protocols/ccdp/composite-cognition-dispatch-protocol.md:801:| `ccdp/decomposition.result` | DECOMPOSITION_RESULT | Decomposition Service → Dispatcher | JSON-RPC request (`method`, `id`, `params`) |
protocols/ccdp/composite-cognition-dispatch-protocol.md:802:| `ccdp/notification` | NOTIFICATION | Any → Dispatcher → Any | JSON-RPC notification (`method`, `params`, no `id`) |
protocols/ccdp/composite-cognition-dispatch-protocol.md:806:REQUEST, ESCALATION, HEALTH_REQUEST, and DECOMPOSITION_RESULT are encoded as JSON-RPC requests — they carry a `method` field, an `id`, and `params`. NOTIFICATION is encoded as a JSON-RPC notification — it carries a `method` and `params` but no `id`, and no response is expected. RESPONSE and HEALTH_RESPONSE are encoded as JSON-RPC responses — they carry an `id` correlating to a prior request and a `result` object containing the CCDP envelope and content. JSON-RPC responses do not carry a `method` field; the CCDP message type is identified by the `envelope.type` field within the `result` object.
protocols/ccdp/composite-cognition-dispatch-protocol.md:811:The Envelope is the structured metadata portion of every CCDP message. The Dispatcher reads the Envelope for routing and enforcement; Content is semantically opaque, though structurally validated and resolved where specified (Section 6).
protocols/ccdp/composite-cognition-dispatch-protocol.md:816:The following fields are REQUIRED on every CCDP message envelope:
protocols/ccdp/composite-cognition-dispatch-protocol.md:821:    "ccdp_version": "1.0",
protocols/ccdp/composite-cognition-dispatch-protocol.md:833:**`ccdp_version`** (string, REQUIRED): The CCDP protocol version. MUST be `"1.0"` for this specification. Implementations MUST reject messages with an unrecognized version. The wire protocol version is independent of the document version (Section 2.1). This specification document is version 0.2.0; the wire protocol version remains `"1.0"` because the on-the-wire message format has not changed.
protocols/ccdp/composite-cognition-dispatch-protocol.md:835:**`type`** (string, REQUIRED): The message type. One of: `"REQUEST"`, `"RESPONSE"`, `"NOTIFICATION"`, `"ESCALATION"`, `"HEALTH_REQUEST"`, `"HEALTH_RESPONSE"`, `"DECOMPOSITION_RESULT"`. For method-bearing messages (REQUEST, ESCALATION, HEALTH_REQUEST, DECOMPOSITION_RESULT, NOTIFICATION), the `envelope.type` value MUST correspond to the JSON-RPC `method` field. For response messages (RESPONSE, HEALTH_RESPONSE), the `envelope.type` is carried inside the `result` object and is the sole identifier of the CCDP message type — JSON-RPC responses have no `method` field.
protocols/ccdp/composite-cognition-dispatch-protocol.md:845:**`source_id`** (string, REQUIRED): The identifier of the component that originated this message. For Requests from external clients, this is the client identifier. For Responses, this is the Service identifier. For forwarded messages, this is the originator, not the Dispatcher. When the Dispatcher forwards a message, it does not overwrite `source_id`. The Dispatcher's identity is recorded in the `audit.dispatcher_id` field (Section 7.5), not in `source_id`. This means `source_id` always identifies the originator, and `audit.dispatcher_id` identifies the intermediary. If future extensions require explicit sender/forwarder identification, they SHOULD use metadata keys (e.g., `org.ccdp.forwarder_id`) rather than overloading `source_id`. For signature verification and authorization, implementations MUST use the Authenticated Sender identity established by the transport layer (Section 4, Section 15.2), not the `source_id` payload field. The `source_id` field is an unauthenticated originator claim; the Authenticated Sender is cryptographically verified.
protocols/ccdp/composite-cognition-dispatch-protocol.md:847:**`metadata`** (object, REQUIRED but MAY be empty): Extensible key-value metadata. Unknown keys MUST be preserved and forwarded by all intermediaries, including the Dispatcher. Keys use reverse-domain notation for namespacing (e.g., `"com.example.custom_field": "value"`). Keys in the `org.ccdp.*` namespace are reserved for protocol-defined extensions.
protocols/ccdp/composite-cognition-dispatch-protocol.md:859:    "capability_type": "org.ccdp.deduction",
protocols/ccdp/composite-cognition-dispatch-protocol.md:925:            "uri": "urn:ccdp:artifact:test-results/run-2026-08-03-001.json",
protocols/ccdp/composite-cognition-dispatch-protocol.md:1002:When `partial_result_available` is true, the Content contains whatever the Service was able to produce before escalating. The Dispatcher MUST include this partial result when forwarding the escalation. This is the canonical location for partial results. Partial results MUST be carried in the ESCALATION message's Content, not in metadata. When the Dispatcher forwards the original Request through the escalation chain, it accumulates partial results from prior Services in the Request's metadata under the key `org.ccdp.partial_results` (Section 13.4.1) for downstream Services' reference. The Content of the forwarded Request remains the original requester's Content. For Dispatcher-generated post-receipt mismatch Escalations (`provenance_mismatch_policy`, Section 9.2), the partial result is the original Service Response content that failed to meet the provenance requirement.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1014:    "capability_type": "org.ccdp.notification",
protocols/ccdp/composite-cognition-dispatch-protocol.md:1052:        "org.ccdp.deduction": {
protocols/ccdp/composite-cognition-dispatch-protocol.md:1102:| `ccdp_version` | R | R | R | R | R | R | R |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1124:The Content is the opaque payload of a CCDP message. Its structure is governed by the Capability Record's input schema (for Requests) or output schema (for Responses).
protocols/ccdp/composite-cognition-dispatch-protocol.md:1130:    "schema_ref": "org.ccdp.deduction/input/v2",
protocols/ccdp/composite-cognition-dispatch-protocol.md:1215:Implementations MUST support messages of at least 16 MiB. Implementations SHOULD support messages of at least 64 MiB. Messages exceeding the implementation's size limit MUST be rejected with HTTP status 413 (Payload Too Large). If the message is small enough to parse but exceeds the CCDP implementation's processing limit, it MUST be rejected with CCDP error code `-32602` (Invalid params) with a `data.reason` of `"message_too_large"`.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1222:The `metadata` field on every envelope provides the extension point for protocol evolution:
protocols/ccdp/composite-cognition-dispatch-protocol.md:1226:Metadata keys in the `org.ccdp.*` namespace that begin with `org.ccdp.request.*` are request-directional: they are meaningful on REQUEST and ESCALATION messages and SHOULD NOT be blindly copied to RESPONSE messages. Metadata keys beginning with `org.ccdp.response.*` are response-directional. Keys without a directional prefix are bidirectional and MUST be preserved in both directions. This prevents request-only control metadata (e.g., escalation history, routing hints) from leaking into responses.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1228:2. New protocol features SHOULD be introduced as metadata keys in the `org.ccdp.*` namespace before being promoted to top-level envelope fields in a subsequent protocol version.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1234:This approach follows the TCP/IP tradition of extensible headers: existing implementations continue to work as new fields are added, and the protocol evolves without version bumps for non-breaking changes.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1254:When CCDP messages are transported over HTTP, the following mapping applies:
protocols/ccdp/composite-cognition-dispatch-protocol.md:1256:| Scenario | HTTP Status | CCDP Behavior |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1258:| Successful response | 200 OK | JSON-RPC result with CCDP envelope |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1267:HTTP status codes in the 4xx range indicate client-side issues; 5xx indicate Dispatcher-side issues. The JSON-RPC error body, when present, carries the structured CCDP error with trace context for audit correlation.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1291:    "capability_type": "org.ccdp.deduction",
protocols/ccdp/composite-cognition-dispatch-protocol.md:1293:    "endpoint": "https://z3-prover-01.internal:8443/ccdp",
protocols/ccdp/composite-cognition-dispatch-protocol.md:1333:      "endpoint": "https://z3-prover-01.internal:8443/ccdp/health",
protocols/ccdp/composite-cognition-dispatch-protocol.md:1347:      {"kind": "capability_type", "value": "org.ccdp.human_review"}
protocols/ccdp/composite-cognition-dispatch-protocol.md:1372:**`endpoint`** (string, REQUIRED): The HTTPS endpoint where the Service accepts CCDP messages.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1403:**`escalation_chain`** (array of typed entries, REQUIRED but MAY be empty): Ordered list of fallback targets to try if this Service returns an Escalation. The Dispatcher processes the chain in order. An empty chain means escalation goes directly to the Human Supervisor's queue. Escalation chain entries are typed objects: `{"kind": "service_id", "value": "human-review-queue-01"}` or `{"kind": "capability_type", "value": "org.ccdp.human_review"}`. The `kind` field disambiguates the entry; string-only entries are ambiguous and MUST NOT be used.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1428:| `org.ccdp.deduction` | Logical deduction, theorem proving, SMT solving | Mode 2 or 3 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1429:| `org.ccdp.planning` | Task planning, decomposition into action sequences | Mode 2 or 3 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1430:| `org.ccdp.language.generation` | Natural language generation, drafting, summarization | Mode 1 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1431:| `org.ccdp.language.translation` | Translation between natural languages or representations | Mode 1 or 3 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1432:| `org.ccdp.language.analysis` | Natural language understanding, classification, extraction | Mode 1 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1433:| `org.ccdp.verification` | Verification of code, proofs, plans against specifications | Mode 2 or 3 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1434:| `org.ccdp.selection` | Ranking, scoring, best-of-N selection | Mode 1, 2, or 3 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1435:| `org.ccdp.retrieval` | Information retrieval, database query, knowledge lookup | Mode 2 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1436:| `org.ccdp.decomposition` | Request decomposition into typed sub-requests | Mode 1 or 3 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1437:| `org.ccdp.human_review` | Human review, judgment, attestation | Mode 4 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1438:| `org.ccdp.code.generation` | Source code generation | Mode 1 or 3 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1439:| `org.ccdp.code.execution` | Code execution in a sandboxed environment | Mode 2 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1440:| `org.ccdp.composition` | Custom composition of Decomposition sub-results requiring judgment (Section 14.3.4) | Mode 1 or 3 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:1494:This specification defines the Registry's logical operations and data model. It does not mandate a specific wire protocol for Registry access. Implementations MAY expose the Registry as a REST API, a gRPC service, an in-process library, or any other mechanism that satisfies the operation contracts above. If two CCDP deployments need their Registries to interoperate (e.g., federated routing), they MUST agree on a common Registry API binding outside this specification. A future CCDP extension MAY define a standard Registry wire protocol.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1506:Schema evolution is the chronic wound of typed protocols. CCDP addresses it through the Registry, which enforces compatibility rules at registration time — not at the Dispatcher, which should not need to understand schema evolution.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1560:The specific availability strategy is an implementation concern, not a protocol requirement. The protocol requires only that routing decisions are logged (including whether they were made from live Registry data, cached data, or static fallback), so that the audit trail reflects the data freshness of each routing decision.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1568:Routing is the Dispatcher's core function: given a Request with a `capability_type`, select the Service best suited to handle it. CCDP routing is *envelope-based* — the Dispatcher makes routing decisions from envelope metadata and Registry data, never from Content.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1585:If `envelope.destination_id` is non-null, route to the specified Service. If the Service is not registered, not ACTIVE, or not healthy, return error `-32001` (service unavailable). Do not fall through to capability-based routing. Explicit destinations MUST also pass authorization, provenance feasibility, deadline, budget, and isolation checks — the same policy filters applied to normal routing (Steps 2–6). An explicit destination is a routing hint, not a bypass of protocol enforcement. If any check fails, the Dispatcher MUST return the appropriate error, not silently fall through to normal routing.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1627:- `"escalate"`: Treat as implicit escalation with reason `PROVENANCE_BELOW_REQUIREMENT` and `envelope.escalation.escalation_origin: "dispatcher"` (Section 7.3.4). Because no candidate passed the provenance filter, there is no originating Service whose `escalation_chain` to walk — the implicit escalation routes directly to `org.ccdp.human_review` as the terminal target, subject to the same authorization and data-class checks as Section 13.4 step 6.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1672:4. **Human queue.** If no automated Service can handle the request, route to a Service of type `org.ccdp.human_review`. This is the terminal escalation target.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1710:│ org.ccdp.deduction   │ z3-prover  │ HEALTHY  │ CLOSED      │ 0.35     │
protocols/ccdp/composite-cognition-dispatch-protocol.md:1711:│ org.ccdp.deduction   │ isabelle   │ DEGRADED │ CLOSED      │ 0.80     │
protocols/ccdp/composite-cognition-dispatch-protocol.md:1712:│ org.ccdp.planning    │ fd-planner │ HEALTHY  │ CLOSED      │ 0.10     │
protocols/ccdp/composite-cognition-dispatch-protocol.md:1713:│ org.ccdp.language.*  │ llm-pool   │ HEALTHY  │ CLOSED      │ 0.55     │
protocols/ccdp/composite-cognition-dispatch-protocol.md:1714:│ org.ccdp.human_review│ review-q   │ HEALTHY  │ CLOSED      │ 0.20     │
protocols/ccdp/composite-cognition-dispatch-protocol.md:1715:│ org.ccdp.verification│ verifier   │ UNHEALTHY│ OPEN        │ —        │
protocols/ccdp/composite-cognition-dispatch-protocol.md:1719:The Routing Table is refreshed from the Registry at a configurable interval (RECOMMENDED: every 30 seconds) and updated in real-time by health check responses. It is an internal Dispatcher structure, not a protocol element — its format is implementation-defined.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1721:**Wildcard capability matching.** Capability type patterns ending in `.*` match any capability type that shares the prefix up to the wildcard. `org.ccdp.language.*` matches `org.ccdp.language.generation`, `org.ccdp.language.translation`, etc., but not `org.ccdp.language` itself (the wildcard requires at least one additional segment). Wildcard patterns are valid only in token scopes (Section 15.3.2) and routing configuration; they MUST NOT appear in Capability Records or Registry lookups. The routing table format is implementation-defined and MAY support pattern matching (e.g., glob or prefix wildcards). Pattern syntax, if supported, is not part of the CCDP wire protocol — it is a deployment-local routing configuration concern. The `org.ccdp.language.*` entry in the example above illustrates implementation flexibility, not a normative capability identifier.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1729:Provenance grades are CCDP's novel contribution — the feature that distinguishes it from every existing protocol for service communication. The core insight: cognitive outputs are not data; they are *claims with epistemic status*. A database query returns a fact. A theorem prover returns a proof. An LLM returns a plausible completion. These are structurally different kinds of evidence, and a protocol that treats them identically forces every consumer to reconstruct epistemic status from scratch.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1735:**The specification-recursion problem [Vericoding; Goodhart 1975]:** Formal verification relocates error rather than eliminating it. A proof guarantees code-meets-spec but is silent on whether the spec captures intent. Empirically, LLMs game weak specifications into vacuous proofs (~9% of "verified" specs in the Vericoding benchmark were too weak). CCDP addresses this by requiring the `scope` field on FORMALLY_VERIFIED grades — binding the grade to a specific specification whose own provenance is separately trackable.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1740:Eight provenance grades are defined, ordered from weakest to strongest epistemic standing. The grades are numbered 0–7. For routing and conformance purposes, a higher-numbered grade satisfies any request requiring a lower-numbered grade (the policy-order property defined in Section 10.2.1). This ordering is a protocol convention, not a claim about universal epistemic subsumption — see the caveat below.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1745:This ordering is a *policy order* for routing and conformance, not a universal epistemic truth. The ordering holds for the protocol's primary use case — selecting services and evaluating whether a response meets a requester's quality threshold. It does not hold in all epistemic contexts: HUMAN_ATTESTED does not imply deterministic COMPUTED (a human reviewer may not have performed the computation); CROSS_CHECKED does not imply VALIDATED against an external criterion unless validation was part of each independent process. The ordering reflects the protocol's design judgment that grades higher on the ladder are harder to achieve and more expensive to fake (the Spence signaling criterion), not that every higher grade subsumes every lower grade's specific method. Consumers with domain-specific epistemic requirements SHOULD inspect the `evidence` entries rather than relying solely on grade comparison.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1837:This does not mean human judgment is infallible. It means that within the CCDP architecture, human attestation is the terminal verification step — the point where epistemic responsibility is explicitly assigned to a named individual. The provenance chain makes this assignment visible and auditable rather than implicit.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1956:**Example 2: When FORMALLY_VERIFIED is required despite human attestation.** A cryptographic protocol implementation requires machine-checkable correctness proofs. A human expert's review (HUMAN_ATTESTED) provides confidence but not the reproducible, automated verification the deployment requires. The requester sets `required_methods: ["formal_verification"]` — HUMAN_ATTESTED alone, despite ranking higher in the policy order, would not satisfy this requirement because it lacks a machine-checkable proof.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1980:CCDP mitigates this through three mechanisms:
protocols/ccdp/composite-cognition-dispatch-protocol.md:1986:3. **Provenance auditing service.** Deployments SHOULD include a provenance auditing service — a Service whose capability type is `org.ccdp.verification` — that spot-checks provenance claims by re-verifying evidence. This is the supply-chain inspection model applied to cognitive provenance.
protocols/ccdp/composite-cognition-dispatch-protocol.md:1996:Audit records carry an `audit_schema_version` field (string, REQUIRED) independent of the CCDP document version and wire protocol version. The current audit schema version is `"1.0"`. Changes to audit record structure increment this version. Audit consumers MUST check `audit_schema_version` and handle unknown versions gracefully (log a warning and preserve the record without interpretation).
protocols/ccdp/composite-cognition-dispatch-protocol.md:1998:Audit is not an extension, an integration, or a best practice. It is a REQUIRED protocol behavior. Every Message that passes through the Dispatcher MUST generate a structured audit record. This requirement is grounded in a practical lesson: general NSA/CISA AI deployment guidance [NSA-CISA-2024] and CCDP's analysis of MCP's audit limitations (Section 3) both point to the risk of leaving audit to implementation discipline — protocols without mandatory audit leave security and reliability to "implementation discipline," which fails unpredictably across deployments.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2023:      "capability_type": "org.ccdp.deduction",
protocols/ccdp/composite-cognition-dispatch-protocol.md:2065:    "ccdp_version": "1.0"
protocols/ccdp/composite-cognition-dispatch-protocol.md:2123:    "ccdp_version": "1.0"
protocols/ccdp/composite-cognition-dispatch-protocol.md:2239:      "capability_type": "org.ccdp.language.translation"
protocols/ccdp/composite-cognition-dispatch-protocol.md:2263:CCDP uses W3C Trace Context [W3C-TC] for distributed tracing. The `trace_id` and `span_id` fields in the CCDP envelope map directly to the W3C `traceparent` header fields:
protocols/ccdp/composite-cognition-dispatch-protocol.md:2275:5. Preserve the `tracestate` header if present, appending a CCDP-specific entry: `ccdp=dispatcher_id`. The Dispatcher appends `ccdp=<dispatcher_id>` to the `tracestate` header. If the `tracestate` header would exceed 512 bytes after appending, the Dispatcher MUST truncate the oldest entries (leftmost) to make room, per W3C Trace Context [W3C-TC] §3.3.2.1. W3C Trace Context §3.3.2.1 specifies that the rightmost entries are the most recently added. Truncating leftmost entries removes the oldest vendor data, which is the least likely to be needed by downstream components. However, some vendors may depend on ordering semantics. If the Dispatcher cannot safely truncate, it SHOULD omit its own `ccdp=<dispatcher_id>` entry rather than truncating other vendors' entries, and log the omission. The Dispatcher MUST sanitize `tracestate` values: strip control characters and validate against the W3C tracestate grammar before forwarding.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2277:This ensures that CCDP traces are compatible with standard distributed tracing infrastructure (OpenTelemetry, Jaeger, Zipkin). Services that use tracing internally can link their internal spans to the CCDP trace.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2290:| `ccdp_version` | string | CCDP wire protocol version from the processed message envelope |
protocols/ccdp/composite-cognition-dispatch-protocol.md:2293:| `message_summary.type` | string | CCDP message type that triggered this record |
protocols/ccdp/composite-cognition-dispatch-protocol.md:2310:| Dispatcher | `dispatcher_id`, `ccdp_version` | Most message types |
protocols/ccdp/composite-cognition-dispatch-protocol.md:2364:4. **Integrity.** Audit records MUST be written to a tamper-evident store for CCDP Full conformance. CCDP Core conformance REQUIRES structured audit records but permits deployment-configured integrity mechanisms. For Full conformance, tamper evidence means either cryptographic chaining (each record includes a hash of the previous record), append-only storage with integrity verification, or a write-once medium. The specific mechanism is deployment-defined.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2373:- **`degrade`**: The Dispatcher continues processing messages without audit. The `degrade` policy is non-conformant — a Dispatcher operating in `degrade` mode does not meet CCDP Core conformance requirements. It exists solely as a development/debugging mode. Implementations MUST NOT enable `degrade` in any deployment that claims CCDP conformance. If used, the Dispatcher MUST set a metadata flag `org.ccdp.audit_gap: true` on all messages processed during the gap, and MUST log the gap duration and message count when the audit store recovers.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2398:Without resource signals, the Dispatcher cannot make resource-rational decisions. TCP solved this problem with congestion control — window advertisements, ECN, slow start, AIMD. CCDP solves it with explicit resource signals at the protocol level: cost budgets on requests, resource consumption on responses, and capacity advertisements from services.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2499:    "org.ccdp.deduction": {
protocols/ccdp/composite-cognition-dispatch-protocol.md:2505:    "org.ccdp.verification": {
protocols/ccdp/composite-cognition-dispatch-protocol.md:2560:When a Service is overloaded, it needs a way to signal the Dispatcher to slow down. CCDP supports back-pressure through three mechanisms:
protocols/ccdp/composite-cognition-dispatch-protocol.md:2572:A Service MAY respond to a CCDP Request with HTTP 429 instead of a CCDP Response. The `Retry-After` header (in seconds) tells the Dispatcher when to retry. The Dispatcher:
protocols/ccdp/composite-cognition-dispatch-protocol.md:2579:More precisely: when a Service returns HTTP 429 (Too Many Requests) instead of a CCDP Response, the Dispatcher MUST: (a) log the 429 in the audit trail as a rate-limit event with the Service's `Retry-After` header value if present, (b) treat the Service as temporarily at capacity (equivalent to health status DEGRADED for the requested capability), (c) follow the retry/reroute strategy in Section 13.5.1, and (d) if the Dispatcher itself rate-limits a requester, it MUST return a CCDP error response with code `-32014` (rate limited) and include the `Retry-After` value in the error `data` field. Error `-32003` is reserved for all-services-unhealthy conditions (Section 9.2 Step 3); it is distinct from Dispatcher-side rate limiting. HTTP 429 MUST NOT be passed through to the requester as a raw HTTP response.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2605:In CCDP, the analog is error amplification across serial cognitive operations. A small uncertainty in Decomposition can produce large errors in downstream sub-results, which compound when composed. The protocol does not solve this problem directly — it is a content-level concern, not a protocol-level one — but it provides the tools for detecting it:
protocols/ccdp/composite-cognition-dispatch-protocol.md:2619:CCDP distinguishes three categories of failure, each with different protocol behavior:
protocols/ccdp/composite-cognition-dispatch-protocol.md:2625:3. **Epistemic insufficiency** — the Service operates correctly but cannot meet the Request's epistemic requirements: the achieved provenance grade is below threshold, capability is exceeded, or the search space is exhausted without a determination. These are *not errors*. They are Escalations — structured routing events that the Dispatcher handles as normal protocol operations.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2627:The distinction between service errors and epistemic insufficiency is load-bearing. An HTTP 500 means something broke. An Escalation with reason `PROVENANCE_BELOW_REQUIREMENT` means the originating actor — a Service or the Dispatcher — determined that the response does not or cannot meet the requested provenance standard. The protocol handles these differently: errors trigger retries and circuit breakers; escalations trigger the Escalation Chain.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2634:Protocol errors are returned as JSON-RPC 2.0 error responses. CCDP defines the following error codes in addition to the standard JSON-RPC error codes:
protocols/ccdp/composite-cognition-dispatch-protocol.md:2639:| `-32600` | Invalid request | Not a valid JSON-RPC request or unrecognized CCDP message type |
protocols/ccdp/composite-cognition-dispatch-protocol.md:2640:| `-32601` | Method not found | Unrecognized CCDP method |
protocols/ccdp/composite-cognition-dispatch-protocol.md:2641:| `-32602` | Invalid params | Malformed CCDP envelope (missing required fields, invalid types) |
protocols/ccdp/composite-cognition-dispatch-protocol.md:2657:For every CCDP error code, the `data` object MUST include at minimum: `trace_id` (string), `request_id` (string), and `timestamp` (ISO 8601). Error-specific fields are defined per code in the table above. This ensures that errors are audit-correlatable even when the full envelope is unavailable.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2673:          "schema_ref": "org.ccdp.deduction/input/v2"
protocols/ccdp/composite-cognition-dispatch-protocol.md:2685:All protocol errors MUST be logged in the audit trail.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2744:6. If all chain targets are exhausted, route to `org.ccdp.human_review` as the terminal target. The human-review fallback MUST still pass authorization and data-class checks. If the requester's token does not authorize `org.ccdp.human_review`, or if the request's data-class/isolation requirements cannot be met by the human queue, the Dispatcher MUST return error `-32006` (escalation chain exhausted) rather than silently routing to an unauthorized or non-compliant target.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2747:The Dispatcher MUST forward the original Request (not the Escalation) to the next target in the chain. The Dispatcher accumulates partial results from prior escalation targets in the forwarded Request's metadata under `org.ccdp.partial_results`. The most recent escalating Service's partial result is in that Service's ESCALATION message Content (the canonical location per Section 7.3.4). The metadata accumulation provides downstream Services and human reviewers with the full escalation history.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2752:- `provenance_unavailable_policy="escalate"`: no candidate Service could satisfy the provenance requirement at routing time. No originating Service record is available, so the implicit escalation routes directly to `org.ccdp.human_review` (bypassing chain walk).
protocols/ccdp/composite-cognition-dispatch-protocol.md:2764:    "org.ccdp.escalation_history": [
protocols/ccdp/composite-cognition-dispatch-protocol.md:2788:    "org.ccdp.partial_results": [
protocols/ccdp/composite-cognition-dispatch-protocol.md:2831:A Service that returns a CCDP error response (a JSON-RPC error with a CCDP error code) is treated as a permanent failure for this Request:
protocols/ccdp/composite-cognition-dispatch-protocol.md:2834:2. Reroute to an alternative Service if the error suggests it (e.g., `-32010` schema validation failed may succeed with a different Service version). Rerouting after schema validation failure is appropriate only when the alternative Service supports a compatible schema version. The Dispatcher SHOULD check schema version compatibility in the Registry before rerouting, rather than blindly forwarding to another Service. Automatic rerouting to a different service version after a schema validation failure is permitted only when the request includes `org.ccdp.allow_schema_version_fallback: true` in the request metadata. This flag defaults to `false`. When absent or `false`, schema validation failures are permanent errors — the Dispatcher MUST return the error to the requester rather than attempting version fallback.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2840:If a Service returns a response that is valid JSON-RPC but invalid CCDP (missing required envelope fields, missing provenance, content does not match output schema), the Dispatcher:
protocols/ccdp/composite-cognition-dispatch-protocol.md:2853:The Dispatcher probes each Service's health at the interval specified in the Service's Capability Record (`health_check.interval_seconds`). Health checks use the `ccdp/health.request` method. The response is a HEALTH_RESPONSE — a JSON-RPC result (not a method-bearing message) whose `envelope.type` is `"HEALTH_RESPONSE"`. The Dispatcher interprets the structured result to update its health table. (Section 7.3.6)
protocols/ccdp/composite-cognition-dispatch-protocol.md:2925:      "org.ccdp.deduction": { "available": true, "current_load": 0.95 },
protocols/ccdp/composite-cognition-dispatch-protocol.md:2926:      "org.ccdp.verification": { "available": false }
protocols/ccdp/composite-cognition-dispatch-protocol.md:2945:CCDP resolves this by treating decomposition as a first-class Service: a dedicated Decomposition Service with Capability Type `org.ccdp.decomposition` that receives complex requests and emits structured Decomposition Plans. The Dispatcher routes to the Decomposition Service first, then routes each sub-request from the plan independently. The Dispatcher performs only structural operations — routing, dependency resolution, typed result-reference substitution; the decomposition intelligence lives in a dedicated, auditable Service behind a typed interface.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2952:**Explicit decomposition request.** The requester sets `capability_type` to `org.ccdp.decomposition`, indicating that the request should be decomposed rather than handled directly. The Dispatcher routes to the Decomposition Service, receives a plan, and executes it.
protocols/ccdp/composite-cognition-dispatch-protocol.md:2972:    "schema_ref": "org.ccdp.decomposition/output/v1",
protocols/ccdp/composite-cognition-dispatch-protocol.md:2980:          "capability_type": "org.ccdp.language.translation",
protocols/ccdp/composite-cognition-dispatch-protocol.md:2999:          "capability_type": "org.ccdp.deduction",
protocols/ccdp/composite-cognition-dispatch-protocol.md:3017:          "capability_type": "org.ccdp.language.translation",
protocols/ccdp/composite-cognition-dispatch-protocol.md:3126:- `"selection"`: Select the best sub-result by a criterion (useful for cross-checking). Allowed `selection` criteria are: `"highest_provenance"` (select the sub-result with the highest provenance grade in policy order), `"lowest_cost"` (select the sub-result with the lowest reported cost), `"first_completed"` (select the first sub-result to arrive — for latency-optimized redundant dispatch). Other selection criteria require routing to an `org.ccdp.composition` Service, as they may involve semantic judgment.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3129:When `method` is `"custom"`, the composition payload is routed to a Service with capability type `org.ccdp.composition`. This is a well-known capability type (added to Section 8.3's well-known types). The Composition Service receives the sub-results and the composition specification and returns the composed result. The Dispatcher does not perform custom composition itself — it delegates to the Composition Service and forwards the result.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3173:3. **Dispatch independent sub-requests.** For each sub-request with no dependencies (or all dependencies satisfied), create a CCDP Request with:
protocols/ccdp/composite-cognition-dispatch-protocol.md:3187:**Composition boundary.** For `template`, `concatenation`, and `selection` composition methods, the Dispatcher performs structural assembly: it places sub-results into the template slots, concatenates them in order, or selects by a typed criterion (highest provenance grade, lowest cost). These are mechanical operations on typed wrappers, consistent with the Coordinator Dispatcher model. The Dispatcher MUST NOT perform composition that requires reasoning about content meaning — such composition MUST be routed to an `org.ccdp.composition` Service.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3198:The Decomposition Service implements Capability Type `org.ccdp.decomposition` with:
protocols/ccdp/composite-cognition-dispatch-protocol.md:3200:**Input schema:** A CCDP Request (the request to be decomposed). The Decomposition Service receives the original Content and must emit a valid Decomposition Plan.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3213:A sub-request in a Decomposition Plan MAY itself have `capability_type: "org.ccdp.decomposition"`, producing a nested decomposition. The Dispatcher handles this recursively: the sub-decomposition produces its own plan, which the Dispatcher executes as a nested sub-tree of the parent plan.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3227:CCDP defines security requirements at the protocol level — authentication, authorization, token scoping, message signing, and replay protection. Some requirements (mutual TLS, bearer-token validation) are enforced by the protocol infrastructure. Others (isolation, workload attestation) are protocol requirements that depend on deployment infrastructure for enforcement. The protocol specifies what must be true; the deployment ensures it is true.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3229:This design choice is a direct response to the MCP release-candidate retrospective [MCP-2026-07-28], which found that a security posture "highly dependent on implementation discipline rather than protocol guarantees" fails unpredictably across deployments.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3231:Every CCDP deployment MUST implement the security requirements in this section. There are no "development mode" exceptions in the specification — while individual deployments MAY relax requirements in non-production environments, the protocol defines a security floor that conforming implementations MUST meet in production.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3253:- Tokens MUST be scoped to specific Capability Types. A token authorized for `org.ccdp.language.generation` MUST NOT be accepted for `org.ccdp.deduction`.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3255:- Tokens SHOULD be issued by an OAuth 2.0 authorization server using Pushed Authorization Requests [RFC 9126] with PKCE [RFC 7636]. Implementations requiring issuer validation SHOULD follow [RFC 9207]. PAR and PKCE apply to the authorization-code issuance flow — how tokens are obtained from the authorization server. They are best practices for token issuance, not requirements for token validation. The Dispatcher, acting as a resource server, validates tokens by checking signature/introspection, expiration, audience, and scope (Section 15.3). The issuance-flow recommendations apply to authorization-server deployments, not to CCDP Dispatchers.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3290:  "scope": ["org.ccdp.deduction", "org.ccdp.language.*"],
protocols/ccdp/composite-cognition-dispatch-protocol.md:3302:Wildcard scopes (e.g., `org.ccdp.language.*`) match all subtypes. Wildcard scope matching uses the following grammar: a scope pattern ending in `.*` matches any scope string that begins with the prefix (everything before `.*`) followed by a dot and one or more additional segments. `org.ccdp.language.*` matches `org.ccdp.language.generation` but not `org.ccdp.language` or `org.ccdp.*`. Exact-match scopes take precedence over wildcard matches. A token with scope `["org.ccdp.language.generation"]` is authorized for that specific capability; a token with scope `["org.ccdp.language.*"]` is authorized for any capability under that prefix.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3315:For environments requiring end-to-end integrity (the requester must be able to verify that the Service's response was not modified by the Dispatcher or any intermediary), CCDP supports application-level message signing:
protocols/ccdp/composite-cognition-dispatch-protocol.md:3322:    "org.ccdp.signature": {
protocols/ccdp/composite-cognition-dispatch-protocol.md:3336:**Signed components.** The `signed_fields` array identifies top-level components of the CCDP message to include in the signature. Valid values are `"envelope"` and `"content"`. The signature input is the JCS [RFC 8785] canonical form of a JSON object containing exactly those components:
protocols/ccdp/composite-cognition-dispatch-protocol.md:3344:Message signing is OPTIONAL for CCDP Core conformance. For CCDP Full conformance, message signing is REQUIRED for Services that produce responses at grade FORMALLY_VERIFIED or HUMAN_ATTESTED, and RECOMMENDED for all other Services. For deployments spanning untrusted administrative domains (different organizations, different cloud regions), message signing is REQUIRED regardless of conformance level.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3356:CCDP defines two signing profiles with different mutable-field sets:
protocols/ccdp/composite-cognition-dispatch-protocol.md:3358:**Requester-outbound profile.** The requester signs before the Dispatcher processes the message. Mutable fields excluded from `envelope` before signing: `audit` (added by the Dispatcher on receipt; absent on the requester's outbound envelope), `remaining_budget_ms` (decremented by the Dispatcher at each subsequent hop, per Section 12.4), `destination_id` (excluded only when null or absent at signing time; a non-null requester-specified `destination_id` is part of the requester's routing intent and MUST remain in the signed envelope — see Section 7.3.2 and Section 9.2 Step 1), and metadata keys in `org.ccdp.dispatcher.*`. All other envelope fields are immutable after signing. The Dispatcher verifies the Requester's signature — computed over the envelope as the requester sent it — before processing.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3360:**Service-response profile.** The service signs its response. Mutable fields excluded from `envelope` before signing: `audit` (added or updated by the Dispatcher when forwarding the response, per Section 7.5) and metadata keys in `org.ccdp.dispatcher.*`. The `remaining_budget_ms` field is not present on response messages. All other envelope fields, and the entire `content` object, are immutable after signing. The `signed_fields` array MUST include both `"envelope"` and `"content"`. The Dispatcher verifies the Service's signature before forwarding to the Requester.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3398:Isolation requirements declared in Registry metadata are *policy inputs*, not protocol-enforceable guarantees. The Dispatcher trusts that the deployment infrastructure enforces isolation as declared. For deployments requiring stronger assurance, implementations SHOULD support workload attestation — the Service provides a signed attestation (e.g., via a Trusted Platform Module or confidential-computing attestation service) that its runtime environment matches the declared isolation requirements. Attestation verification is an optional Full-conformance feature.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3415:The MCP fault taxonomy study identified tool naming collisions as an attack vector — malicious entries in public registries with names that shadow legitimate tools. CCDP mitigates this through:
protocols/ccdp/composite-cognition-dispatch-protocol.md:3425:Services that require credentials (API keys, database passwords, etc.) MUST NOT receive them through the CCDP protocol. Credentials are provisioned through out-of-band mechanisms (environment variables, secret managers, key vaults). The CCDP protocol carries authentication tokens for *protocol-level* identity, not application-level credentials.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3440:Rate limiting parameters are deployment-configured, not protocol-specified.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3453:A CCDP Core Dispatcher MUST satisfy all requirements in this table.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3457:| DISP-CORE-001 | Parse all CCDP message types. | 7.2 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:3459:| DISP-CORE-003 | Validate `ccdp_version`: reject messages with unrecognized versions. | 7.3.1 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:3461:| DISP-CORE-005 | Respect metadata directionality: `org.ccdp.request.*` keys are request-directional, `org.ccdp.response.*` keys are response-directional, keys without a directional prefix are bidirectional. Strip sensitivity-labeled keys at administrative domain boundaries. | 7.7 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:3494:A CCDP Full Dispatcher MUST satisfy all Core requirements plus all requirements in this table.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3500:| DISP-FULL-003 | Execute Decomposition Plans: dispatch sub-requests, resolve typed result references, compose results using structural methods (template, concatenation, selection). Route custom composition to an `org.ccdp.composition` Service. | 14.4 |
protocols/ccdp/composite-cognition-dispatch-protocol.md:3529:1. Accept CCDP Request messages and return CCDP Response, Escalation, or Error messages (Section 7).
protocols/ccdp/composite-cognition-dispatch-protocol.md:3530:2. Include the `ccdp_version` field on all messages.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3532:4. Preserve and forward all unknown `metadata` fields from the Request to the Response. Services MUST respect metadata directionality: keys in `org.ccdp.request.*` are request-directional and SHOULD NOT be copied to Response messages. Keys in `org.ccdp.response.*` are response-directional. Keys without a directional prefix are bidirectional and MUST be preserved.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3539:7. Reject malformed Requests with a CCDP error response rather than attempting to interpret them.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3622:**CCDP Core:** Implements all MUST requirements applicable to the relevant component type (Dispatcher, Service, or Registry) throughout this specification. For the Dispatcher, the Core requirements table (Section 16.1.1) is the authoritative checklist. For Services and Registries, Core conformance is assessed against all normative MUST requirements applicable to their component type — particularly the explicit lists in Sections 16.2 and 16.3, but also applicable requirements in Sections 7, 8, 10, 12, 13, and 15. This is the minimum for interoperability.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3624:**CCDP Full:** Full conformance requires all Core requirements plus all Full requirements listed in Section 16.1.2. Full conformance tables with stable requirement IDs are defined for the Dispatcher (Section 16.1) only. Service and Registry Full conformance tables are an open item — see Section 18 (Open Questions). Until those tables are defined, Services and Registries conform to all normative MUST requirements applicable to their component type, without a Core/Full distinction. SHOULD-level recommendations throughout the specification are best practices, not Full conformance obligations. Full conformance is defined by the explicit Full requirements tables, not by the sum of all SHOULD statements.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3626:Implementations MUST declare their conformance level in their documentation and in the Registry (for Services) via a `metadata` field: `"org.ccdp.conformance_level": "core"` or `"org.ccdp.conformance_level": "full"`.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3631:A CCDP Core Dispatcher MUST be able to communicate with any CCDP Core Service. A CCDP Full Dispatcher MUST be able to communicate with both CCDP Core and CCDP Full Services. Differences in conformance level MUST NOT cause protocol errors — they MAY result in reduced functionality. A CCDP Core Dispatcher MUST validate Decomposition Plans but is not required to execute them. A Core Dispatcher MAY route decomposition requests to a human queue, an external orchestrator, or return error `-32002` (no service for capability) if no decomposition executor is available. A CCDP Full Dispatcher MUST execute valid Decomposition Plans.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3640:A future companion document will define a conformance test suite for CCDP Core and Full implementations. Each Dispatcher requirement identifier in Section 16.1, and each prose-numbered Service and Registry requirement in Sections 16.2 and 16.3, is intended to correspond to one or more testable assertions. Implementations claiming conformance SHOULD publish their test results against the conformance suite when it becomes available.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3658:CCDP operates in an environment where:
protocols/ccdp/composite-cognition-dispatch-protocol.md:3670:3. Attackers may attempt to exploit protocol features (escalation chains, decomposition, provenance) for unauthorized access or quality degradation.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3675:1. Multiple colluding compromised components (this is a single-fault model). While multi-component collusion is out of scope for protocol-level defense, it is a high-risk scenario for large-scale deployments. Deployments handling sensitive data or operating across organizational boundaries SHOULD implement defense-in-depth measures beyond the protocol: independent audit verification, cross-domain provenance spot-checking, and organizational access controls that limit the blast radius of a single compromised identity.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3687:**CCDP mitigations:**
protocols/ccdp/composite-cognition-dispatch-protocol.md:3689:- Input schema validation (Section 8.2.2) constrains the structural shape of Content, reducing the protocol-level attack surface. Schema validation does not mitigate semantic prompt-injection attacks — a structurally valid string can still be a prompt injection. Content-level injection defense is the Service's responsibility (see Section 17.4).
protocols/ccdp/composite-cognition-dispatch-protocol.md:3692:**Residual risk:** Content injection is fundamentally a Service-level concern. CCDP's contribution is ensuring that injection cannot affect protocol behavior (routing, audit, provenance) — only the Service's content processing.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3699:**CCDP mitigations:**
protocols/ccdp/composite-cognition-dispatch-protocol.md:3700:- Evidence entries must substantiate grades above ASSERTED (Section 10.3). A grade of FORMALLY_VERIFIED without an Evidence entry containing `method: "formal_verification"` and an `artifact_ref` with `artifact_type: "proof_certificate"` (or equivalent verifiable proof artifact) is a protocol violation (Section 10.3).
protocols/ccdp/composite-cognition-dispatch-protocol.md:3705:**Residual risk:** A sufficiently sophisticated attacker could forge evidence entries (e.g., generate a fake proof object). Full mitigation requires independent proof checking — the protocol makes evidence available for checking but does not perform the check itself.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3712:**CCDP mitigations:**
protocols/ccdp/composite-cognition-dispatch-protocol.md:3728:**CCDP mitigations:**
protocols/ccdp/composite-cognition-dispatch-protocol.md:3741:**CCDP mitigations:**
protocols/ccdp/composite-cognition-dispatch-protocol.md:3753:**CCDP mitigations:**
protocols/ccdp/composite-cognition-dispatch-protocol.md:3765:**CCDP mitigations:**
protocols/ccdp/composite-cognition-dispatch-protocol.md:3766:- Evidence `artifact_ref` fields are references (URIs), not inline data. Access to the referenced artifacts is governed by the artifact storage system's access controls, not by CCDP.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3770:**Residual risk:** Free-text fields (`evidence.description`, `escalation.detail`) can carry arbitrary text. Deployments processing sensitive data SHOULD implement content scanning on these fields. The same exfiltration risk applies to all free-text and URI fields in the protocol: `escalation.detail`, `routing_decision.reason`, `schema_validation` error messages, notification content, and audit record annotations. Deployments processing sensitive data SHOULD implement content scanning or field-length limits on all free-text fields, and SHOULD classify protocol fields by sensitivity level (public, internal, restricted) with access controls matching the classification.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3777:**CCDP mitigations:** None at the protocol level. This is an inherent property of any system that exposes latency data.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3784:The NSA/CISA AI deployment security guidance [NSA-CISA-2024] makes specific recommendations for securely deploying AI systems generally, not specific to any single protocol. This table maps those general recommendations to CCDP features — it is not a claim that CCDP was independently assessed against them.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3786:| NSA/CISA Recommendation | CCDP Response | Requirement Level |
protocols/ccdp/composite-cognition-dispatch-protocol.md:3799:Three security concerns that CCDP does not fully address, stated without softening because the credibility of the security design depends on not overclaiming:
protocols/ccdp/composite-cognition-dispatch-protocol.md:3801:**Content-level attacks are the Service's problem.** CCDP protects the protocol layer — routing, audit, provenance. It does not protect the content layer. A prompt injection that causes an LLM to produce wrong output is invisible to CCDP unless the output fails schema validation or provenance verification. The protocol provides the infrastructure for detecting such failures (provenance grades, cross-checking, auditing) but does not perform the detection.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3803:**The trust model is single-fault.** CCDP is designed to detect and contain compromise of a single component. If both a Service and the Registry are compromised, the attacker can redirect traffic and falsify provenance without detection. Multi-component compromise requires organizational security measures beyond the protocol's scope.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3805:**Provenance grades are claims, not proofs.** A provenance grade is a structured assertion by the Service about its own output's epistemic status. While evidence entries make some grades independently verifiable (proof objects can be checked, test results can be re-run), the protocol fundamentally trusts that Services honestly report their grades. Systematic dishonesty requires organizational and auditing countermeasures, not just protocol design.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3810:This section documents design questions that have been identified during the v0.2 review process but are intentionally deferred. They are recorded here so that future revisions can address them with full context. None of the questions listed in this section, by themselves, represent known contradictions. Each represents a point where a different design choice could improve the protocol.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3828:**Trade-offs.** Renaming grades has wire-compatibility implications. The numeric grade values (0–7) are the protocol-level identifiers for routing and conformance; names are human-readable labels. The current mixed naming is well-understood by the intended audience. A rename would be a breaking change for any pre-1.0 implementations but is feasible before the first stable release.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3835:**Question.** Should CCDP define capacity-reservation tokens, admission-control handshakes, or lease-based capacity semantics?
protocols/ccdp/composite-cognition-dispatch-protocol.md:3837:**Trade-offs.** Reservation and admission control would improve routing accuracy under load but add significant protocol complexity (reservation lifecycle, timeout, cancellation, partial-use accounting). The current snapshot model with staleness guidance is adequate for moderate-scale deployments. High-scale deployments can add infrastructure-level admission control without protocol changes.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3844:**Question.** Should the protocol add explicit identity wire fields (e.g., `originator_id`, `sender_id`, `forwarder_id`) to the message envelope, or is the current lighter approach (a defined term plus transport-layer verification) sufficient?
protocols/ccdp/composite-cognition-dispatch-protocol.md:3853:**Question.** Should CCDP define a normative Registry wire binding for multi-implementation interoperability, or should Registry interoperability remain deployment-defined?
protocols/ccdp/composite-cognition-dispatch-protocol.md:3855:**Trade-offs.** A normative binding enables Registry interoperability across implementations but constrains deployment flexibility. Many deployments will embed the Registry in the Dispatcher process, making a wire binding unnecessary. A binding could be defined as an optional companion specification rather than a core protocol requirement.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3887:(Note: RFC 8785 is Informational, not Standards Track. CCDP takes a normative dependency on this Informational RFC for message-signing canonicalization. If a Standards Track canonicalization scheme becomes available, future CCDP versions may adopt it.)
protocols/ccdp/composite-cognition-dispatch-protocol.md:3906:The foundational paper for CCDP's principle that the Dispatcher verifies protocol correctness while content correctness is the Service's responsibility.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3912:**[Kleppmann 2012]** Kleppmann, M., "Schema evolution in Avro, Protocol Buffers and Thrift," 2012. https://martin.kleppmann.com/2012/12/05/schema-evolution-in-avro-protocol-buffers-thrift.html
protocols/ccdp/composite-cognition-dispatch-protocol.md:3914:The basis for CCDP's schema versioning and compatibility rules in the Capability Registry.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3918:Demonstrated that typed contracts and code generation are achievable without the full gRPC operational overhead. CCDP's HTTP-native approach is informed by Connect's design.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3922:gRPC's deadline propagation model — a per-call deadline that decrements at each hop. Grounds CCDP's `deadline` and `remaining_budget_ms` flow-control fields (Section 12.4).
protocols/ccdp/composite-cognition-dispatch-protocol.md:3926:**[MCP-2026-07-28]** Model Context Protocol, "Release Candidate: The next generation of MCP," July 2026. https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/ (Blog post published May 21, 2026, announcing the July 28, 2026 release candidate.) (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:3930:The general AI-deployment security findings that drove CCDP's "security by default" principle. See also [MCP-2026-07-28] for MCP-specific security posture findings.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3934:Analysis of 407 MCP-specific issues from 385 repositories documenting the consequences of loose protocol contracts.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3936:**[A2A]** Linux Foundation / Google, "Agent2Agent (A2A) Protocol Specification," 2025. https://a2a-protocol.org/latest/specification (accessed 2026-08-04; the specification has moved from its original google.github.io/A2A/ location) — For background: Galileo AI, "Google Agent2Agent A2A Protocol Guide." https://galileo.ai/blog/google-agent2agent-a2a-protocol-guide (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:3938:**[Zylos-Interop]** Zylos Research, "Agent Interoperability Protocols 2026." https://zylos.ai/research/2026-03-26-agent-interoperability-protocols-mcp-a2a-acp-convergence/ (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:3940:**[Zylos-A2A]** Zylos Research, "Agent-to-Agent Communication Protocols." https://zylos.ai/research/2026-02-15-agent-to-agent-communication-protocols/ (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:3942:**[FIPA-ACL]** SmythOS, "FIPA Agent Communication Language." https://smythos.com/developers/agent-development/fipa-agent-communication-language/ — See also: SmythOS, "Agent Communication Languages Comparison." https://smythos.com/developers/agent-development/agent-communication-languages-and-protocols-comparison/
protocols/ccdp/composite-cognition-dispatch-protocol.md:3946:The most substantive academic survey of the agent communication landscape. CCDP drew from its FIPA-ACL historical analysis and two-layer standardization strategy while addressing a different problem space (supervised specialists under a dumb router, not autonomous peers at internet scale).
protocols/ccdp/composite-cognition-dispatch-protocol.md:3948:**[Mitra-Stack]** Mitra, S., "The Agent Protocol Stack: MCP + A2A + A2UI as TCP/IP Moment," 2026. https://subhadipmitra.com/blog/2026/agent-protocol-stack/ (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:3950:**[DEV-Standards]** "The State of Agentic AI Standards in 2026," DEV Community. https://dev.to/alexmercedcoder/the-state-of-agentic-ai-standards-in-2026-mcp-a2a-webmcp-osi-and-the-protocol-stack-taking-3o2l (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:3977:The supervision-tree architecture. Build reliable systems from unreliable components through strong isolation, message-passing-only interaction, supervision, and "let it crash." Grounds CCDP's overall architectural model.
protocols/ccdp/composite-cognition-dispatch-protocol.md:3981:The actor model: isolated actors interacting only by asynchronous messages. Grounds the "typed protocols on the wires" between Dispatcher and Services.
protocols/ccdp/composite-cognition-dispatch-protocol.md:4044:The references in this section are secondary or journalistic sources used for context and historical framing. Protocol-level claims in this specification are grounded in the primary references (Sections 19.1–19.3). Secondary sources provide useful context but are not evidence for protocol design decisions.
protocols/ccdp/composite-cognition-dispatch-protocol.md:4046:**[MCP-Roadmap]** Model Context Protocol, "The 2026 MCP Roadmap." https://blog.modelcontextprotocol.io/posts/2026-mcp-roadmap/ (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:4048:**[Scalifi-Flaws]** Scalifi AI, "Six Fatal Flaws of MCP," 2025. https://www.scalifiai.com/blog/model-context-protocol-flaws-2025 (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:4050:**[Sivaro-MCP]** Sivaro, "Is MCP Outdated? A 2026 Reality Check." https://sivaro.in/articles/is-model-context-protocol-outdated-a-2026-reality-check/ (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:4052:**[A2A-Adoption]** Glukhov, "A2A Protocol 2026 Adoption and Reality." https://www.glukhov.org/ai-systems/comparisons/a2a-protocol-2026-adoption/ (accessed 2026-08-03)
protocols/ccdp/composite-cognition-dispatch-protocol.md:4075:- [v0.1](https://github.com/billosys/ai-engineering/blob/ccdp-0.1/protocols/ccdp/composite-cognition-dispatch-protocol.md)
protocols/ccdp/composite-cognition-dispatch-protocol.md:4087:The wire protocol version remains `"1.0"` during this draft cycle.
protocols/ccdp/composite-cognition-dispatch-protocol.md:4092:Distinguished document version (`0.2.0`) from wire protocol version (`"1.0"`).
protocols/ccdp/composite-cognition-dispatch-protocol.md:4107:protocol enforcement and execution coordinator, not a cognitive reasoner.
protocols/ccdp/composite-cognition-dispatch-protocol.md:4167:no-candidate unavailability routes directly to `org.ccdp.human_review`.
protocols/ccdp/composite-cognition-dispatch-protocol.md:4174:Added `org.ccdp.composition` capability for semantic composition. Added
protocols/ccdp/src/13-error-handling.md:5:CCDP distinguishes three categories of failure, each with different protocol behavior:
protocols/ccdp/src/13-error-handling.md:11:3. **Epistemic insufficiency** — the Service operates correctly but cannot meet the Request's epistemic requirements: the achieved provenance grade is below threshold, capability is exceeded, or the search space is exhausted without a determination. These are *not errors*. They are Escalations — structured routing events that the Dispatcher handles as normal protocol operations.
protocols/ccdp/src/13-error-handling.md:13:The distinction between service errors and epistemic insufficiency is load-bearing. An HTTP 500 means something broke. An Escalation with reason `PROVENANCE_BELOW_REQUIREMENT` means the originating actor — a Service or the Dispatcher — determined that the response does not or cannot meet the requested provenance standard. The protocol handles these differently: errors trigger retries and circuit breakers; escalations trigger the Escalation Chain.
protocols/ccdp/src/13-error-handling.md:19:Protocol errors are returned as JSON-RPC 2.0 error responses. CCDP defines the following error codes in addition to the standard JSON-RPC error codes:
protocols/ccdp/src/13-error-handling.md:24:| `-32600` | Invalid request | Not a valid JSON-RPC request or unrecognized CCDP message type |
protocols/ccdp/src/13-error-handling.md:25:| `-32601` | Method not found | Unrecognized CCDP method |
protocols/ccdp/src/13-error-handling.md:26:| `-32602` | Invalid params | Malformed CCDP envelope (missing required fields, invalid types) |
protocols/ccdp/src/13-error-handling.md:42:For every CCDP error code, the `data` object MUST include at minimum: `trace_id` (string), `request_id` (string), and `timestamp` (ISO 8601). Error-specific fields are defined per code in the table above. This ensures that errors are audit-correlatable even when the full envelope is unavailable.
protocols/ccdp/src/13-error-handling.md:58:          "schema_ref": "org.ccdp.deduction/input/v2"
protocols/ccdp/src/13-error-handling.md:70:All protocol errors MUST be logged in the audit trail.
protocols/ccdp/src/13-error-handling.md:127:6. If all chain targets are exhausted, route to `org.ccdp.human_review` as the terminal target. The human-review fallback MUST still pass authorization and data-class checks. If the requester's token does not authorize `org.ccdp.human_review`, or if the request's data-class/isolation requirements cannot be met by the human queue, the Dispatcher MUST return error `-32006` (escalation chain exhausted) rather than silently routing to an unauthorized or non-compliant target.
protocols/ccdp/src/13-error-handling.md:130:The Dispatcher MUST forward the original Request (not the Escalation) to the next target in the chain. The Dispatcher accumulates partial results from prior escalation targets in the forwarded Request's metadata under `org.ccdp.partial_results`. The most recent escalating Service's partial result is in that Service's ESCALATION message Content (the canonical location per Section 7.3.4). The metadata accumulation provides downstream Services and human reviewers with the full escalation history.
protocols/ccdp/src/13-error-handling.md:135:- `provenance_unavailable_policy="escalate"`: no candidate Service could satisfy the provenance requirement at routing time. No originating Service record is available, so the implicit escalation routes directly to `org.ccdp.human_review` (bypassing chain walk).
protocols/ccdp/src/13-error-handling.md:146:    "org.ccdp.escalation_history": [
protocols/ccdp/src/13-error-handling.md:170:    "org.ccdp.partial_results": [
protocols/ccdp/src/13-error-handling.md:210:A Service that returns a CCDP error response (a JSON-RPC error with a CCDP error code) is treated as a permanent failure for this Request:
protocols/ccdp/src/13-error-handling.md:213:2. Reroute to an alternative Service if the error suggests it (e.g., `-32010` schema validation failed may succeed with a different Service version). Rerouting after schema validation failure is appropriate only when the alternative Service supports a compatible schema version. The Dispatcher SHOULD check schema version compatibility in the Registry before rerouting, rather than blindly forwarding to another Service. Automatic rerouting to a different service version after a schema validation failure is permitted only when the request includes `org.ccdp.allow_schema_version_fallback: true` in the request metadata. This flag defaults to `false`. When absent or `false`, schema validation failures are permanent errors — the Dispatcher MUST return the error to the requester rather than attempting version fallback.
protocols/ccdp/src/13-error-handling.md:218:If a Service returns a response that is valid JSON-RPC but invalid CCDP (missing required envelope fields, missing provenance, content does not match output schema), the Dispatcher:
protocols/ccdp/src/13-error-handling.md:229:The Dispatcher probes each Service's health at the interval specified in the Service's Capability Record (`health_check.interval_seconds`). Health checks use the `ccdp/health.request` method. The response is a HEALTH_RESPONSE — a JSON-RPC result (not a method-bearing message) whose `envelope.type` is `"HEALTH_RESPONSE"`. The Dispatcher interprets the structured result to update its health table. (Section 7.3.6)
protocols/ccdp/src/13-error-handling.md:298:      "org.ccdp.deduction": { "available": true, "current_load": 0.95 },
protocols/ccdp/src/13-error-handling.md:299:      "org.ccdp.verification": { "available": false }
protocols/ccdp/src/18-open-questions.md:3:This section documents design questions that have been identified during the v0.2 review process but are intentionally deferred. They are recorded here so that future revisions can address them with full context. None of the questions listed in this section, by themselves, represent known contradictions. Each represents a point where a different design choice could improve the protocol.
protocols/ccdp/src/18-open-questions.md:19:**Trade-offs.** Renaming grades has wire-compatibility implications. The numeric grade values (0–7) are the protocol-level identifiers for routing and conformance; names are human-readable labels. The current mixed naming is well-understood by the intended audience. A rename would be a breaking change for any pre-1.0 implementations but is feasible before the first stable release.
protocols/ccdp/src/18-open-questions.md:25:**Question.** Should CCDP define capacity-reservation tokens, admission-control handshakes, or lease-based capacity semantics?
protocols/ccdp/src/18-open-questions.md:27:**Trade-offs.** Reservation and admission control would improve routing accuracy under load but add significant protocol complexity (reservation lifecycle, timeout, cancellation, partial-use accounting). The current snapshot model with staleness guidance is adequate for moderate-scale deployments. High-scale deployments can add infrastructure-level admission control without protocol changes.
protocols/ccdp/src/18-open-questions.md:33:**Question.** Should the protocol add explicit identity wire fields (e.g., `originator_id`, `sender_id`, `forwarder_id`) to the message envelope, or is the current lighter approach (a defined term plus transport-layer verification) sufficient?
protocols/ccdp/src/18-open-questions.md:41:**Question.** Should CCDP define a normative Registry wire binding for multi-implementation interoperability, or should Registry interoperability remain deployment-defined?
protocols/ccdp/src/18-open-questions.md:43:**Trade-offs.** A normative binding enables Registry interoperability across implementations but constrains deployment flexibility. Many deployments will embed the Registry in the Dispatcher process, making a wire binding unnecessary. A binding could be defined as an optional companion specification rather than a core protocol requirement.
protocols/ccdp/json/inventory/fields.md:1:# CCDP Field Census
protocols/ccdp/json/inventory/fields.md:4:defining section. Extracted from CCDP draft v0.2 (document version 0.2.0).
protocols/ccdp/json/inventory/fields.md:22:| `method` | string (`ccdp/request`, `ccdp/escalation`, `ccdp/health.request`, `ccdp/decomposition.result`, `ccdp/notification`) | R on method-bearing messages; absent on RESPONSE/HEALTH_RESPONSE | §7.1, §7.2 |
protocols/ccdp/json/inventory/fields.md:32:| `ccdp_version` | string (`"1.0"`) | R | R | R | R | R | R | R | §7.3.1, §2.1 |
protocols/ccdp/json/inventory/fields.md:234:| `trace_id` | string | R (every CCDP error) | §13.2 |
protocols/ccdp/json/inventory/fields.md:243:## 15. Protocol-defined metadata keys (`org.ccdp.*`)
protocols/ccdp/json/inventory/fields.md:247:| `org.ccdp.escalation_history` | array of history entries | §13.4.1 |
protocols/ccdp/json/inventory/fields.md:255:| `org.ccdp.partial_results` | array of `{service_id, provenance, content}` | §13.4.1, §7.3.4 |
protocols/ccdp/json/inventory/fields.md:256:| `org.ccdp.signature` | object `{algorithm, key_id, profile, signed_fields, value, timestamp}` | §15.4.2, §15.4.4 |
protocols/ccdp/json/inventory/fields.md:257:| `org.ccdp.allow_schema_version_fallback` | boolean, default false | §13.5.2 |
protocols/ccdp/json/inventory/fields.md:258:| `org.ccdp.audit_gap` | boolean (`degrade` mode only — non-conformant) | §11.6 |
protocols/ccdp/json/inventory/fields.md:259:| `org.ccdp.conformance_level` | `"core"` / `"full"` (Registry metadata) | §16.4 |
protocols/ccdp/json/inventory/fields.md:260:| `org.ccdp.request.*` / `org.ccdp.response.*` prefixes | directional key classes | §7.7 |
protocols/ccdp/json/inventory/fields.md:261:| `org.ccdp.dispatcher.*` prefix | Dispatcher-mutable keys excluded from signing | §15.4.4 |
protocols/ccdp/json/inventory/fields.md:262:| `org.ccdp.forwarder_id` | named only as a hypothetical future extension | §7.3.1 **[not a defined field]** |
protocols/ccdp/json/canonical/message-request.notes.md:12:| `method` | `"ccdp/request"` | §7.1, §7.2 |
protocols/ccdp/json/canonical/message-request.notes.md:14:| `params.envelope`, `params.content` | carrier of CCDP data for method-bearing messages | §7.1 |
protocols/ccdp/json/canonical/message-request.notes.md:20:| `ccdp_version` | R | §7.3.1 (value `"1.0"` per §2.1) |
protocols/ccdp/json/canonical/message-request.notes.md:50:| `org.ccdp.allow_schema_version_fallback` | §13.5.2 (default false) |
protocols/ccdp/json/canonical/message-request.notes.md:51:| `org.ccdp.signature` (object: `algorithm`, `key_id`, `profile`, `signed_fields`, `value`, `timestamp`) | §15.4.2 (structure), §15.4.4 (`"requester-outbound"` profile). **Placement of the requester-outbound signature in metadata is inferred from §15.4.2's service-response example — the spec never shows a requester-signed message; F-17** |
protocols/ccdp/json/canonical/message-request.notes.md:52:| `org.ccdp.escalation_history[]` (`service_id` / `dispatcher_id` + `responding_service_id`, `reason`, `escalation_origin`, `achieved_grade`, `detail`, `timestamp`) | §13.4.1 |
protocols/ccdp/json/canonical/message-request.notes.md:53:| `org.ccdp.partial_results[]` (`service_id`, `provenance`, `content`) | §13.4.1, §7.3.4 |
protocols/ccdp/json/canonical/message-request.notes.md:57:`org.ccdp.dispatcher.*` keys are excluded, §15.4.4), yet §13.4.1 has the
protocols/ccdp/json/canonical/message-request.notes.md:58:Dispatcher append `org.ccdp.escalation_history` / `org.ccdp.partial_results`
protocols/ccdp/json/canonical/message-request.notes.md:73:Body fields follow the §8.2.1 example `input_schema` for `org.ccdp.deduction`.
protocols/ccdp/src/16-conformance.md:9:A CCDP Core Dispatcher MUST satisfy all requirements in this table.
protocols/ccdp/src/16-conformance.md:13:| DISP-CORE-001 | Parse all CCDP message types. | 7.2 |
protocols/ccdp/src/16-conformance.md:15:| DISP-CORE-003 | Validate `ccdp_version`: reject messages with unrecognized versions. | 7.3.1 |
protocols/ccdp/src/16-conformance.md:17:| DISP-CORE-005 | Respect metadata directionality: `org.ccdp.request.*` keys are request-directional, `org.ccdp.response.*` keys are response-directional, keys without a directional prefix are bidirectional. Strip sensitivity-labeled keys at administrative domain boundaries. | 7.7 |
protocols/ccdp/src/16-conformance.md:49:A CCDP Full Dispatcher MUST satisfy all Core requirements plus all requirements in this table.
protocols/ccdp/src/16-conformance.md:55:| DISP-FULL-003 | Execute Decomposition Plans: dispatch sub-requests, resolve typed result references, compose results using structural methods (template, concatenation, selection). Route custom composition to an `org.ccdp.composition` Service. | 14.4 |
protocols/ccdp/src/16-conformance.md:81:1. Accept CCDP Request messages and return CCDP Response, Escalation, or Error messages (Section 7).
protocols/ccdp/src/16-conformance.md:82:2. Include the `ccdp_version` field on all messages.
protocols/ccdp/src/16-conformance.md:84:4. Preserve and forward all unknown `metadata` fields from the Request to the Response. Services MUST respect metadata directionality: keys in `org.ccdp.request.*` are request-directional and SHOULD NOT be copied to Response messages. Keys in `org.ccdp.response.*` are response-directional. Keys without a directional prefix are bidirectional and MUST be preserved.
protocols/ccdp/src/16-conformance.md:90:7. Reject malformed Requests with a CCDP error response rather than attempting to interpret them.
protocols/ccdp/src/16-conformance.md:161:**CCDP Core:** Implements all MUST requirements applicable to the relevant component type (Dispatcher, Service, or Registry) throughout this specification. For the Dispatcher, the Core requirements table (Section 16.1.1) is the authoritative checklist. For Services and Registries, Core conformance is assessed against all normative MUST requirements applicable to their component type — particularly the explicit lists in Sections 16.2 and 16.3, but also applicable requirements in Sections 7, 8, 10, 12, 13, and 15. This is the minimum for interoperability.
protocols/ccdp/src/16-conformance.md:163:**CCDP Full:** Full conformance requires all Core requirements plus all Full requirements listed in Section 16.1.2. Full conformance tables with stable requirement IDs are defined for the Dispatcher (Section 16.1) only. Service and Registry Full conformance tables are an open item — see Section 18 (Open Questions). Until those tables are defined, Services and Registries conform to all normative MUST requirements applicable to their component type, without a Core/Full distinction. SHOULD-level recommendations throughout the specification are best practices, not Full conformance obligations. Full conformance is defined by the explicit Full requirements tables, not by the sum of all SHOULD statements.
protocols/ccdp/src/16-conformance.md:165:Implementations MUST declare their conformance level in their documentation and in the Registry (for Services) via a `metadata` field: `"org.ccdp.conformance_level": "core"` or `"org.ccdp.conformance_level": "full"`.
protocols/ccdp/src/16-conformance.md:169:A CCDP Core Dispatcher MUST be able to communicate with any CCDP Core Service. A CCDP Full Dispatcher MUST be able to communicate with both CCDP Core and CCDP Full Services. Differences in conformance level MUST NOT cause protocol errors — they MAY result in reduced functionality. A CCDP Core Dispatcher MUST validate Decomposition Plans but is not required to execute them. A Core Dispatcher MAY route decomposition requests to a human queue, an external orchestrator, or return error `-32002` (no service for capability) if no decomposition executor is available. A CCDP Full Dispatcher MUST execute valid Decomposition Plans.
protocols/ccdp/src/16-conformance.md:177:A future companion document will define a conformance test suite for CCDP Core and Full implementations. Each Dispatcher requirement identifier in Section 16.1, and each prose-numbered Service and Registry requirement in Sections 16.2 and 16.3, is intended to correspond to one or more testable assertions. Implementations claiming conformance SHOULD publish their test results against the conformance suite when it becomes available.
protocols/ccdp/json/inventory/enums.md:1:# CCDP Closed Vocabularies (Enums)
protocols/ccdp/json/inventory/enums.md:3:Every closed (or nominally closed) value set in CCDP draft v0.2 (document
protocols/ccdp/json/inventory/enums.md:17:`ccdp/request`, `ccdp/escalation`, `ccdp/health.request`,
protocols/ccdp/json/inventory/enums.md:18:`ccdp/decomposition.result`, `ccdp/notification`. (RESPONSE and
protocols/ccdp/json/inventory/enums.md:83:`-32013` is skipped without comment (F-24). Every CCDP error's `data` MUST
protocols/ccdp/json/inventory/enums.md:109:`org.ccdp.deduction`, `org.ccdp.planning`, `org.ccdp.language.generation`,
protocols/ccdp/json/inventory/enums.md:110:`org.ccdp.language.translation`, `org.ccdp.language.analysis`,
protocols/ccdp/json/inventory/enums.md:111:`org.ccdp.verification`, `org.ccdp.selection`, `org.ccdp.retrieval`,
protocols/ccdp/json/inventory/enums.md:112:`org.ccdp.decomposition`, `org.ccdp.human_review`, `org.ccdp.code.generation`,
protocols/ccdp/json/inventory/enums.md:113:`org.ccdp.code.execution`, `org.ccdp.composition`.
protocols/ccdp/json/inventory/enums.md:114:**`org.ccdp.notification` is used in the §7.3.5 example but is not in this
protocols/ccdp/json/inventory/enums.md:115:table (F-12).** Wildcards (`org.ccdp.language.*`) are valid only in token
protocols/ccdp/json/inventory/enums.md:165:| `selection` criteria | `highest_provenance`, `lowest_cost`, `first_completed` | closed (3); others require `org.ccdp.composition` routing |
protocols/ccdp/json/inventory/enums.md:189:`core`, `full` (lowercase, in `org.ccdp.conformance_level`). Requirement ID
protocols/ccdp/json/inventory/enums.md:219:| `ccdp_version` | `"1.0"` | §2.1, §7.3.1 |
protocols/ccdp/json/inventory/enums.md:223:| `tracestate` entry | `ccdp=<dispatcher_id>`; 512-byte truncation rule | §11.3 |
protocols/ccdp/json/canonical/message-health-request.notes.md:4:"ccdp/health.request"` — §7.2, §13.6.1). A HEALTH_REQUEST always targets a
protocols/ccdp/src/03-introduction.md:9:This architecture requires a protocol. Requests must flow from humans (and from services making sub-requests) through a central point — the dispatcher — to the appropriate service, with responses flowing back. The dispatcher must route correctly, and every link in the chain must be auditable. The protocol must carry enough structure for routing decisions to be classification rather than reasoning — the dispatcher reads envelopes, not content.
protocols/ccdp/src/03-introduction.md:11:No existing protocol jointly satisfies these requirements: typed envelope routing through a non-cognitive coordinator, mandatory structured audit, provenance grades with composition rules, first-class escalation, cost and deadline signals, and a capability registry with schema versioning.
protocols/ccdp/src/03-introduction.md:17:MCP is the closest existing protocol to CCDP's problem space — it connects language models to external capabilities. Its core abstraction (tools, resources, prompts) covers most LLM-to-service interactions.
protocols/ccdp/src/03-introduction.md:23:**Designed for smart consumers, not dumb dispatchers.** MCP assumes the client (the LLM/host) is the intelligent party — it interprets natural-language tool descriptions, decides which tools to invoke, and manages conversation flow. Tool descriptions are free-text strings meant for LLM consumption. For CCDP's dispatcher — a protocol enforcement and execution coordinator that operates on envelope metadata — MCP carries insufficient routing structure. The protocol intelligence lives in the consumer, not the envelope.
protocols/ccdp/src/03-introduction.md:25:**No mandatory audit.** MCP does not mandate structured logging of tool invocations. Audit trails are implementation concerns. For a supervision-tree architecture where every link must be inspectable, audit metadata must be a core protocol field, not an afterthought.
protocols/ccdp/src/03-introduction.md:27:**No cost or resource signals.** MCP provides no protocol-native required cost budget, consumption reporting, or deadline enforcement fields comparable to CCDP's flow-control layer (Section 12). A dispatcher cannot make resource-rational routing decisions without this information. TCP has congestion signals (ECN, window advertisements); a cognitive dispatch protocol needs cognitive-resource signals.
protocols/ccdp/src/03-introduction.md:29:**Security by implementation discipline.** MCP's security posture is highly dependent on implementation discipline rather than protocol guarantees: no mandated authentication, tool parameter injection enabling arbitrary code execution, tool naming collisions exploitable from public registries. The 2026-07-28 spec adds OAuth 2.0 authorization with Pushed Authorization Requests [RFC 9126] and PKCE [RFC 7636], but the "security by convention" orientation persists.
protocols/ccdp/src/03-introduction.md:31:**No epistemic dimension.** Most fundamentally, MCP treats service outputs as data. CCDP treats them as *claims with epistemic status*. A prover's output and an LLM's output are structurally different kinds of evidence, and the protocol must carry that distinction. MCP has no concept of provenance grades, evidence strength, or provenance-grade-below-threshold escalation (CCDP's `PROVENANCE_BELOW_REQUIREMENT` escalation reason).
protocols/ccdp/src/03-introduction.md:37:A2A's limitation for CCDP is that it assumes both sides of a link are *agents* — capable, autonomous entities that negotiate and decide. CCDP's dispatcher is deliberately not an agent. It is a protocol enforcement and execution coordinator. A2A's complexity (Agent Card infrastructure, multi-transport support, autonomous negotiation) is overkill for a system where one side is a constrained coordinator, not an autonomous agent, and its peer-to-peer topology does not match CCDP's star topology.
protocols/ccdp/src/03-introduction.md:43:gRPC's *implementation complexity* works against the constrained-coordinator principle. Schema version management is a chronic operational wound — the discipline of never reusing field numbers, managing `.proto` file distribution, and coordinating the protoc toolchain across heterogeneous services creates friction that compounds. A dispatcher should not need a protoc toolchain. CCDP adopts gRPC's design principles (typed contracts, deadline propagation, interceptor-style audit) without its implementation weight, using JSON Schema for runtime-validatable contracts and JSON-RPC 2.0 for the wire format.
protocols/ccdp/src/03-introduction.md:49:FIPA-ACL never escaped the lab. It lacked verifiable identity, governance frameworks, runtime tooling, and practical deployment paths. It was described in a comprehensive survey as having limited practical deployment despite its formal elegance [arXiv-Agent-Comms]. CCDP inherits FIPA's insight — speech acts as message types — while designing explicitly against its failure modes: every protocol feature must be practically deployable with minimal tooling, not formally elegant in isolation.
protocols/ccdp/src/03-introduction.md:53:The distinction between cognitive dispatch and data routing — the reason CCDP cannot be a thin layer over an existing RPC framework — lies in three properties unique to cognitive output:
protocols/ccdp/src/03-introduction.md:55:**Cognitive outputs are claims, not data.** A database query returns a fact. A theorem prover returns a proof. An LLM returns a plausible completion. These are structurally different kinds of evidence, and a protocol that treats them identically forces the consumer to reconstruct epistemic status from scratch at every boundary. CCDP makes provenance a first-class protocol field: every response carries a grade indicating the evidence strength behind it, with defined composition rules for multi-service operations (Section 10).
protocols/ccdp/src/03-introduction.md:57:**Provenance-grade insufficiency is a routing event, not an error.** When a cognitive service cannot produce output at the requested provenance grade, this is not a failure — it is information. "I can generate candidate solutions but cannot verify them" is a legitimate, structured response that the dispatcher should route to a verification service or escalate to a human. CCDP defines escalation as a protocol message type with structured routing semantics (Section 13).
protocols/ccdp/src/03-introduction.md:59:**The specification-recursion problem.** Formal verification relocates error rather than eliminating it: "did we build it right?" becomes "did we specify the right thing?" [Vericoding; Goodhart 1975] — a compound citation per Section 2's convention, resolving to the separately defined [Vericoding] and [Goodhart 1975] entries. An LLM that games a weak specification into a vacuous proof is not a verification failure — it is a Goodhart failure. CCDP's provenance system is designed with this recursion in mind: a grade of FORMALLY_VERIFIED carries a scope field binding it to a specific specification, and the specification's own provenance is separately tracked (Section 10).
protocols/ccdp/src/03-introduction.md:63:CCDP is governed by eight principles, each grounded in the research base:
protocols/ccdp/src/03-introduction.md:67:2. **The end-to-end principle applies.** The dispatcher verifies *protocol* correctness (well-formed envelopes, valid routing, schema compliance, timeout enforcement) — structural validation, not semantic interpretation (Section 4). *Content* correctness is the service's responsibility. (From: Saltzer, Reed & Clark 1984.)
protocols/ccdp/src/03-introduction.md:69:3. **Audit is mandatory, not optional.** Every message passing through the dispatcher gets structured audit metadata. This is core protocol behavior, not an extension. (From: the general security principle that implementation-discipline-dependent security fails unpredictably across deployments — see Section 3.2.1's analysis of MCP.)
protocols/ccdp/src/03-introduction.md:71:4. **Provenance grades are first-class.** Every response carries an evidence-strength field. This is the protocol's novel contribution. (From: the Spence signaling theory — quality signals work only when expensive to fake.)
protocols/ccdp/src/03-introduction.md:73:5. **Escalation is a protocol operation, not an error.** Services can return "I cannot handle this at the required provenance grade" as a structured escalation that the dispatcher routes upward. (From: OTP supervision — escalation through the supervision tree is normal operation.)
protocols/ccdp/src/03-introduction.md:77:7. **Extensibility without breakage.** Unknown metadata fields are preserved and forwarded. New capabilities are added as metadata keys without protocol version bumps. (From: TCP options field, HTTP headers, protobuf unknown-field forwarding.)
protocols/ccdp/src/03-introduction.md:79:8. **Security by default.** Mutual authentication and bearer-token scoping are required at all conformance levels. Message signing is required for Full conformance when producing high-grade provenance (FORMALLY_VERIFIED, HUMAN_ATTESTED) and for all cross-administrative-domain deployments. See Section 15 for the complete signing policy. (From: NSA/CISA findings — security as a protocol guarantee, not an implementation choice.)
protocols/ccdp/src/03-introduction.md:83:CCDP specifies the message format, routing semantics, registry interface, provenance system, audit requirements, and security baseline for communication between a dispatcher and cognitive services. It does not specify:
protocols/ccdp/src/03-introduction.md:89:- The specific set of capability types (which are registry-managed, not protocol-defined; the protocol defines a set of well-known Capability Types as extension points in Section 8.3; the registry manages the open-ended set of deployment-specific types)
protocols/ccdp/src/03-introduction.md:91:CCDP is transport-layer agnostic in principle but specifies HTTP as the REQUIRED base transport and JSON-RPC 2.0 as the REQUIRED wire format. Future specifications MAY define bindings for other transports.
protocols/ccdp/src/02-conventions.md:5:This document specifies a protocol for composite cognition dispatch. It is published as an initial specification for examination, implementation feedback, and iterative refinement. Distribution is unlimited.
protocols/ccdp/src/02-conventions.md:9:This specification uses two independent version identifiers. The **document version** (currently 0.2.0) tracks the maturity of the specification text — its completeness, internal consistency, and review status. The **wire protocol version** (currently `"1.0"`, carried in every message's `envelope.ccdp_version` field) tracks the on-the-wire format. A document revision that clarifies prose, tightens conformance requirements, or adds non-breaking features increments the document version without changing the wire protocol version. A document revision that changes message structure, adds required envelope fields, or alters wire semantics increments the wire protocol version. Implementations negotiate by wire protocol version; the document version is for human readers and specification governance.
protocols/ccdp/src/02-conventions.md:11:**Pre-1.0 stability note.** While the document version is below 1.0 and no conforming implementations have been declared, document revisions MAY change wire-visible semantics without incrementing the wire protocol version. The wire protocol version will be frozen at the first stable release (document version 1.0.0). Implementers building against pre-1.0 drafts should track the document version and expect wire-level changes between revisions.
protocols/ccdp/src/02-conventions.md:25:Where examples show JSON structures with `// ...` comments, the comments are an expository convenience and are not valid JSON. Implementations MUST NOT include comments in wire-format messages. The content type for all CCDP messages is `application/json` with charset UTF-8. JSON numbers follow IEEE 754 double-precision semantics; implementations requiring higher precision for monetary or cryptographic values SHOULD use string-encoded representations. Enumeration values in this specification use `UPPER_SNAKE_CASE` (e.g., `FORMALLY_VERIFIED`, `SEARCH_EXHAUSTED`). Reverse-domain identifiers (capability types, metadata namespace keys) use dot-separated segments (e.g., `org.ccdp.language.generation`). These conventions are normative for wire-format values.
protocols/ccdp/src/02-conventions.md:27:Monetary values in CCDP messages SHOULD be represented as strings (e.g., `"4.50"` not `4.50`) to avoid IEEE 754 floating-point precision loss. All monetary-value examples in this specification use string representations.
protocols/ccdp/src/02-conventions.md:39:All examples in this specification are informative unless explicitly marked as normative. Tables that list requirements use normative language (MUST, SHOULD, MAY) and carry the same force as prose requirements. Diagrams are informative aids. Design notes (marked as such) are informative rationale and do not create protocol obligations.
protocols/ccdp/json/canonical/error-object.notes.md:3:Maximal CCDP error as a JSON-RPC 2.0 error response, using code `-32010`
protocols/ccdp/json/canonical/error-object.notes.md:11:| `error.data.trace_id` | R for every CCDP error code | §13.2 |
protocols/ccdp/json/canonical/error-object.notes.md:26:The full error-code table (standard JSON-RPC `-32700`…`-32603` plus CCDP
protocols/ccdp/src/19-references.md:29:(Note: RFC 8785 is Informational, not Standards Track. CCDP takes a normative dependency on this Informational RFC for message-signing canonicalization. If a Standards Track canonicalization scheme becomes available, future CCDP versions may adopt it.)
protocols/ccdp/src/19-references.md:47:The foundational paper for CCDP's principle that the Dispatcher verifies protocol correctness while content correctness is the Service's responsibility.
protocols/ccdp/src/19-references.md:53:**[Kleppmann 2012]** Kleppmann, M., "Schema evolution in Avro, Protocol Buffers and Thrift," 2012. https://martin.kleppmann.com/2012/12/05/schema-evolution-in-avro-protocol-buffers-thrift.html
protocols/ccdp/src/19-references.md:55:The basis for CCDP's schema versioning and compatibility rules in the Capability Registry.
protocols/ccdp/src/19-references.md:59:Demonstrated that typed contracts and code generation are achievable without the full gRPC operational overhead. CCDP's HTTP-native approach is informed by Connect's design.
protocols/ccdp/src/19-references.md:63:gRPC's deadline propagation model — a per-call deadline that decrements at each hop. Grounds CCDP's `deadline` and `remaining_budget_ms` flow-control fields (Section 12.4).
protocols/ccdp/src/19-references.md:67:**[MCP-2026-07-28]** Model Context Protocol, "Release Candidate: The next generation of MCP," July 2026. https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/ (Blog post published May 21, 2026, announcing the July 28, 2026 release candidate.) (accessed 2026-08-03)
protocols/ccdp/src/19-references.md:71:The general AI-deployment security findings that drove CCDP's "security by default" principle. See also [MCP-2026-07-28] for MCP-specific security posture findings.
protocols/ccdp/src/19-references.md:75:Analysis of 407 MCP-specific issues from 385 repositories documenting the consequences of loose protocol contracts.
protocols/ccdp/src/19-references.md:77:**[A2A]** Linux Foundation / Google, "Agent2Agent (A2A) Protocol Specification," 2025. https://a2a-protocol.org/latest/specification (accessed 2026-08-04; the specification has moved from its original google.github.io/A2A/ location) — For background: Galileo AI, "Google Agent2Agent A2A Protocol Guide." https://galileo.ai/blog/google-agent2agent-a2a-protocol-guide (accessed 2026-08-03)
protocols/ccdp/src/19-references.md:79:**[Zylos-Interop]** Zylos Research, "Agent Interoperability Protocols 2026." https://zylos.ai/research/2026-03-26-agent-interoperability-protocols-mcp-a2a-acp-convergence/ (accessed 2026-08-03)
protocols/ccdp/src/19-references.md:81:**[Zylos-A2A]** Zylos Research, "Agent-to-Agent Communication Protocols." https://zylos.ai/research/2026-02-15-agent-to-agent-communication-protocols/ (accessed 2026-08-03)
protocols/ccdp/src/19-references.md:83:**[FIPA-ACL]** SmythOS, "FIPA Agent Communication Language." https://smythos.com/developers/agent-development/fipa-agent-communication-language/ — See also: SmythOS, "Agent Communication Languages Comparison." https://smythos.com/developers/agent-development/agent-communication-languages-and-protocols-comparison/
protocols/ccdp/src/19-references.md:87:The most substantive academic survey of the agent communication landscape. CCDP drew from its FIPA-ACL historical analysis and two-layer standardization strategy while addressing a different problem space (supervised specialists under a dumb router, not autonomous peers at internet scale).
protocols/ccdp/src/19-references.md:89:**[Mitra-Stack]** Mitra, S., "The Agent Protocol Stack: MCP + A2A + A2UI as TCP/IP Moment," 2026. https://subhadipmitra.com/blog/2026/agent-protocol-stack/ (accessed 2026-08-03)
protocols/ccdp/src/19-references.md:91:**[DEV-Standards]** "The State of Agentic AI Standards in 2026," DEV Community. https://dev.to/alexmercedcoder/the-state-of-agentic-ai-standards-in-2026-mcp-a2a-webmcp-osi-and-the-protocol-stack-taking-3o2l (accessed 2026-08-03)
protocols/ccdp/src/19-references.md:117:The supervision-tree architecture. Build reliable systems from unreliable components through strong isolation, message-passing-only interaction, supervision, and "let it crash." Grounds CCDP's overall architectural model.
protocols/ccdp/src/19-references.md:121:The actor model: isolated actors interacting only by asynchronous messages. Grounds the "typed protocols on the wires" between Dispatcher and Services.
protocols/ccdp/src/19-references.md:183:The references in this section are secondary or journalistic sources used for context and historical framing. Protocol-level claims in this specification are grounded in the primary references (Sections 19.1–19.3). Secondary sources provide useful context but are not evidence for protocol design decisions.
protocols/ccdp/src/19-references.md:185:**[MCP-Roadmap]** Model Context Protocol, "The 2026 MCP Roadmap." https://blog.modelcontextprotocol.io/posts/2026-mcp-roadmap/ (accessed 2026-08-03)
protocols/ccdp/src/19-references.md:187:**[Scalifi-Flaws]** Scalifi AI, "Six Fatal Flaws of MCP," 2025. https://www.scalifiai.com/blog/model-context-protocol-flaws-2025 (accessed 2026-08-03)
protocols/ccdp/src/19-references.md:189:**[Sivaro-MCP]** Sivaro, "Is MCP Outdated? A 2026 Reality Check." https://sivaro.in/articles/is-model-context-protocol-outdated-a-2026-reality-check/ (accessed 2026-08-03)
protocols/ccdp/src/19-references.md:191:**[A2A-Adoption]** Glukhov, "A2A Protocol 2026 Adoption and Reality." https://www.glukhov.org/ai-systems/comparisons/a2a-protocol-2026-adoption/ (accessed 2026-08-03)
protocols/ccdp/src/20-previous-versions.md:5:- [v0.2](https://github.com/billosys/ai-engineering/blob/ccdp-0.2/protocols/ccdp/composite-cognition-dispatch-protocol.md)
protocols/ccdp/src/20-previous-versions.md:6:- [v0.1](https://github.com/billosys/ai-engineering/blob/ccdp-0.1/protocols/ccdp/composite-cognition-dispatch-protocol.md)
protocols/ccdp/json/canonical/audit-record.notes.md:21:| `ccdp_version` | Table 11.1 |
protocols/ccdp/src/06-protocol-layers.md:5:CCDP follows the TCP/IP tradition of layered protocol design: each layer provides a specific abstraction, relies only on the layer below, and can evolve independently. The layering is deliberate — it separates transport concerns (how bytes move) from routing concerns (where messages go) from epistemic concerns (how much to trust the result).
protocols/ccdp/src/06-protocol-layers.md:7:Unlike TCP/IP, where the application layer is unspecified by the transport, CCDP's upper layers carry load-bearing protocol semantics. Provenance grades, audit metadata, and escalation semantics are not application concerns delegated to the endpoints — they are protocol-layer features enforced at the Dispatcher. This is the sense in which "the protocol is smart": the layers above transport carry intelligence that a Dispatcher with no cognitive capability of its own can enforce mechanically.
protocols/ccdp/src/06-protocol-layers.md:11:CCDP defines four layers, mapped to their TCP/IP analogs:
protocols/ccdp/src/06-protocol-layers.md:22:│  (CCDP's novel contribution — no TCP/IP analog)      │
protocols/ccdp/src/06-protocol-layers.md:50:**HTTP is REQUIRED** as the base transport protocol. CCDP messages are HTTP POST requests to defined endpoints. HTTP was chosen for ubiquity: it works with all Service types (LLM endpoints, web services, queue systems), is supported by all programming languages, and composes with existing infrastructure (load balancers, proxies, monitoring).
protocols/ccdp/src/06-protocol-layers.md:52:**JSON-RPC 2.0 is REQUIRED** as the wire format. Every CCDP message is a JSON-RPC 2.0 request or response, with CCDP-specific method names and parameter structures. JSON-RPC was chosen for simplicity: its specification fits on one page, it is transport-agnostic, and it imposes minimal parsing overhead on a Dispatcher with no cognitive capability of its own. Both MCP and A2A chose JSON-RPC 2.0 for the same reasons.
protocols/ccdp/src/06-protocol-layers.md:56:**Authentication** is performed at this layer. The REQUIRED mechanism is mutual TLS (mTLS) for Dispatcher-to-Service authentication. Bearer tokens with scoped permissions MAY be used as an additional authorization mechanism (Section 15). Bearer-token validation is logically an application-level concern (it checks CCDP-scoped authorization, not transport-level connectivity). It is placed in the Transport Layer for implementation convenience: tokens are typically carried in HTTP headers and checked before message parsing begins. This is a pragmatic layer crossing, not a design error.
protocols/ccdp/src/06-protocol-layers.md:58:Implementations MAY support additional transports (e.g., QUIC for latency-critical paths, WebSocket for long-lived connections) as protocol extensions, provided they satisfy the same reliability, encryption, and authentication guarantees. The Transport Layer is the most substitutable layer in the stack.
protocols/ccdp/src/06-protocol-layers.md:85:Layer 2 carries protocol-enforcement fields. The required set varies by message type — see the per-message required-field matrix in Section 7.3. Fields such as `capability_type` and `destination_id` are required on REQUEST messages but absent or optional on RESPONSE, HEALTH, and NOTIFICATION messages. The Dispatcher reads Layer 2 fields for routing and protocol enforcement. It also reads selected Layer 3 fields (provenance requirements, cost budgets, deadlines) for routing policy and reads Layer 4 Content structurally for schema validation and typed-reference resolution. These cross-layer reads are enumerated in Section 6.5. The Dispatcher MUST NOT perform semantic interpretation of any layer's content.
protocols/ccdp/src/06-protocol-layers.md:89:The Epistemic Layer carries the information that makes CCDP different from a generic RPC protocol. It has no TCP/IP analog — this is CCDP's novel contribution.
protocols/ccdp/src/06-protocol-layers.md:130:- **Epistemic evolution**: Add new Evidence types or new composition rules without changing transport or routing. Adding new Provenance Grades is a protocol extension that requires a document version increment. New grades affect the ordering used for routing, conformance checking, and policy evaluation. The grade set is versioned through the Registry; deployments MUST ensure that all components recognize the grade vocabulary before grades are used in production. New epistemic fields are added as metadata extensions. Existing implementations that do not understand the new fields MUST preserve and forward them (Section 7.7).
protocols/ccdp/src/06-protocol-layers.md:133:This independence is a direct application of the end-to-end principle: each layer does only what it must, and correctness guarantees that belong to a higher layer are not duplicated at a lower layer. The Dispatcher enforces protocol correctness (Layer 2); Services enforce content correctness (Layer 4); the Epistemic Layer (Layer 3) carries the metadata that connects them.
protocols/ccdp/src/06-protocol-layers.md:137:Several sources describe a layered agent protocol stack forming: MCP for tool integration, A2A for agent coordination, WebMCP for browser interaction. CCDP's layering differs in three respects:
protocols/ccdp/src/06-protocol-layers.md:139:**The Epistemic Layer has no counterpart.** The emerging stack has no protocol-level concept of provenance, evidence strength, or epistemic status. This is the gap CCDP fills — the recognition that cognitive outputs are claims with pedigree, not data with types.
protocols/ccdp/src/06-protocol-layers.md:141:**The Dispatcher is a protocol enforcer, not a capable agent.** In the emerging stack, the "client" or "orchestrator" is assumed to be an intelligent agent. CCDP's Dispatcher is closer to a policy-enforcing message coordinator: it reads headers and structural metadata, applies routing policies, executes decomposition plans, validates schemas, and forwards processed messages — all without cognitive capability. The protocol carries the intelligence; the Dispatcher enforces it.
protocols/ccdp/src/06-protocol-layers.md:143:**Audit is a layer concern, not an extension.** In the emerging stack, observability comes from bolting on OpenTelemetry or similar frameworks. In CCDP, audit fields are mandatory Layer 2 elements — the Dispatcher writes them as part of its core function, not as an opt-in integration.
protocols/ccdp/json/canonical/message-notification.notes.md:11:| `capability_type` | O per matrix §7.3.8; value `"org.ccdp.notification"` copied from the §7.3.5 example — **this capability type is not in the §8.3 well-known table; F-12** | §7.3.5 example |
protocols/ccdp/json/canonical/message-decomposition-result.notes.md:4:(`method: "ccdp/decomposition.result"` — §7.2), sent Decomposition Service →
protocols/ccdp/json/canonical/message-decomposition-result.notes.md:13:| `capability_type` | R per matrix (`org.ccdp.decomposition`) | §7.3.8, §8.3 |
protocols/ccdp/json/canonical/capability-record.notes.md:5:`org.ccdp.conformance_level` metadata key (§16.4).
protocols/ccdp/json/canonical/capability-record.notes.md:34:| `metadata["org.ccdp.conformance_level"]` | `"core"` / `"full"` — Services MUST declare | §16.4 |
protocols/ccdp/json/canonical/capability-record.notes.md:36:Derived values: only the `org.ccdp.conformance_level` value choice (`"full"`).
protocols/ccdp/json/canonical/message-escalation.notes.md:4:(`method: "ccdp/escalation"` — §7.2). Scenario: reason
protocols/ccdp/src/10-provenance-grades.md:5:Provenance grades are CCDP's novel contribution — the feature that distinguishes it from every existing protocol for service communication. The core insight: cognitive outputs are not data; they are *claims with epistemic status*. A database query returns a fact. A theorem prover returns a proof. An LLM returns a plausible completion. These are structurally different kinds of evidence, and a protocol that treats them identically forces every consumer to reconstruct epistemic status from scratch.
protocols/ccdp/src/10-provenance-grades.md:11:**The specification-recursion problem [Vericoding; Goodhart 1975]:** Formal verification relocates error rather than eliminating it. A proof guarantees code-meets-spec but is silent on whether the spec captures intent. Empirically, LLMs game weak specifications into vacuous proofs (~9% of "verified" specs in the Vericoding benchmark were too weak). CCDP addresses this by requiring the `scope` field on FORMALLY_VERIFIED grades — binding the grade to a specific specification whose own provenance is separately trackable.
protocols/ccdp/src/10-provenance-grades.md:15:Eight provenance grades are defined, ordered from weakest to strongest epistemic standing. The grades are numbered 0–7. For routing and conformance purposes, a higher-numbered grade satisfies any request requiring a lower-numbered grade (the policy-order property defined in Section 10.2.1). This ordering is a protocol convention, not a claim about universal epistemic subsumption — see the caveat below.
protocols/ccdp/src/10-provenance-grades.md:19:This ordering is a *policy order* for routing and conformance, not a universal epistemic truth. The ordering holds for the protocol's primary use case — selecting services and evaluating whether a response meets a requester's quality threshold. It does not hold in all epistemic contexts: HUMAN_ATTESTED does not imply deterministic COMPUTED (a human reviewer may not have performed the computation); CROSS_CHECKED does not imply VALIDATED against an external criterion unless validation was part of each independent process. The ordering reflects the protocol's design judgment that grades higher on the ladder are harder to achieve and more expensive to fake (the Spence signaling criterion), not that every higher grade subsumes every lower grade's specific method. Consumers with domain-specific epistemic requirements SHOULD inspect the `evidence` entries rather than relying solely on grade comparison.
protocols/ccdp/src/10-provenance-grades.md:111:This does not mean human judgment is infallible. It means that within the CCDP architecture, human attestation is the terminal verification step — the point where epistemic responsibility is explicitly assigned to a named individual. The provenance chain makes this assignment visible and auditable rather than implicit.
protocols/ccdp/src/10-provenance-grades.md:222:**Example 2: When FORMALLY_VERIFIED is required despite human attestation.** A cryptographic protocol implementation requires machine-checkable correctness proofs. A human expert's review (HUMAN_ATTESTED) provides confidence but not the reproducible, automated verification the deployment requires. The requester sets `required_methods: ["formal_verification"]` — HUMAN_ATTESTED alone, despite ranking higher in the policy order, would not satisfy this requirement because it lacks a machine-checkable proof.
protocols/ccdp/src/10-provenance-grades.md:244:CCDP mitigates this through three mechanisms:
protocols/ccdp/src/10-provenance-grades.md:250:3. **Provenance auditing service.** Deployments SHOULD include a provenance auditing service — a Service whose capability type is `org.ccdp.verification` — that spot-checks provenance claims by re-verifying evidence. This is the supply-chain inspection model applied to cognitive provenance.
protocols/ccdp/json/canonical/message-response.notes.md:4:the sole CCDP type identifier — §7.2). Scenario chosen for coherence: a Mode 3
protocols/ccdp/json/canonical/message-response.notes.md:57:| `org.ccdp.signature` (service-response profile; `signed_fields` MUST include both components at FORMALLY_VERIFIED/HUMAN_ATTESTED, shown here as the RECOMMENDED practice for other grades) | §15.4.2, §15.4.4 |
protocols/ccdp/json/canonical/message-health-response.notes.md:4:`envelope.type` identifies the CCDP type — §7.2, §13.6.1). Scenario: DEGRADED
protocols/ccdp/src/11-audit-trail.md:5:Audit records carry an `audit_schema_version` field (string, REQUIRED) independent of the CCDP document version and wire protocol version. The current audit schema version is `"1.0"`. Changes to audit record structure increment this version. Audit consumers MUST check `audit_schema_version` and handle unknown versions gracefully (log a warning and preserve the record without interpretation).
protocols/ccdp/src/11-audit-trail.md:7:Audit is not an extension, an integration, or a best practice. It is a REQUIRED protocol behavior. Every Message that passes through the Dispatcher MUST generate a structured audit record. This requirement is grounded in a practical lesson: general NSA/CISA AI deployment guidance [NSA-CISA-2024] and CCDP's analysis of MCP's audit limitations (Section 3) both point to the risk of leaving audit to implementation discipline — protocols without mandatory audit leave security and reliability to "implementation discipline," which fails unpredictably across deployments.
protocols/ccdp/src/11-audit-trail.md:31:      "capability_type": "org.ccdp.deduction",
protocols/ccdp/src/11-audit-trail.md:73:    "ccdp_version": "1.0"
protocols/ccdp/src/11-audit-trail.md:130:    "ccdp_version": "1.0"
protocols/ccdp/src/11-audit-trail.md:244:      "capability_type": "org.ccdp.language.translation"
protocols/ccdp/src/11-audit-trail.md:267:CCDP uses W3C Trace Context [W3C-TC] for distributed tracing. The `trace_id` and `span_id` fields in the CCDP envelope map directly to the W3C `traceparent` header fields:
protocols/ccdp/src/11-audit-trail.md:279:5. Preserve the `tracestate` header if present, appending a CCDP-specific entry: `ccdp=dispatcher_id`. The Dispatcher appends `ccdp=<dispatcher_id>` to the `tracestate` header. If the `tracestate` header would exceed 512 bytes after appending, the Dispatcher MUST truncate the oldest entries (leftmost) to make room, per W3C Trace Context [W3C-TC] §3.3.2.1. W3C Trace Context §3.3.2.1 specifies that the rightmost entries are the most recently added. Truncating leftmost entries removes the oldest vendor data, which is the least likely to be needed by downstream components. However, some vendors may depend on ordering semantics. If the Dispatcher cannot safely truncate, it SHOULD omit its own `ccdp=<dispatcher_id>` entry rather than truncating other vendors' entries, and log the omission. The Dispatcher MUST sanitize `tracestate` values: strip control characters and validate against the W3C tracestate grammar before forwarding.
protocols/ccdp/src/11-audit-trail.md:281:This ensures that CCDP traces are compatible with standard distributed tracing infrastructure (OpenTelemetry, Jaeger, Zipkin). Services that use tracing internally can link their internal spans to the CCDP trace.
protocols/ccdp/src/11-audit-trail.md:293:| `ccdp_version` | string | CCDP wire protocol version from the processed message envelope |
protocols/ccdp/src/11-audit-trail.md:296:| `message_summary.type` | string | CCDP message type that triggered this record |
protocols/ccdp/src/11-audit-trail.md:313:| Dispatcher | `dispatcher_id`, `ccdp_version` | Most message types |
protocols/ccdp/src/11-audit-trail.md:366:4. **Integrity.** Audit records MUST be written to a tamper-evident store for CCDP Full conformance. CCDP Core conformance REQUIRES structured audit records but permits deployment-configured integrity mechanisms. For Full conformance, tamper evidence means either cryptographic chaining (each record includes a hash of the previous record), append-only storage with integrity verification, or a write-once medium. The specific mechanism is deployment-defined.
protocols/ccdp/src/11-audit-trail.md:374:- **`degrade`**: The Dispatcher continues processing messages without audit. The `degrade` policy is non-conformant — a Dispatcher operating in `degrade` mode does not meet CCDP Core conformance requirements. It exists solely as a development/debugging mode. Implementations MUST NOT enable `degrade` in any deployment that claims CCDP conformance. If used, the Dispatcher MUST set a metadata flag `org.ccdp.audit_gap: true` on all messages processed during the gap, and MUST log the gap duration and message count when the audit store recovers.
protocols/ccdp/json/canonical/registry-ops.md:6:standard Registry wire protocol to a future CCDP extension, and §18.5 tracks
protocols/ccdp/src/07-message-format.md:5:Every CCDP message is encoded as a JSON-RPC 2.0 [JSON-RPC] request or response, transported over HTTP POST. The JSON-RPC `method` field identifies the CCDP message type. Method-bearing messages (REQUEST, ESCALATION, NOTIFICATION, HEALTH_REQUEST, DECOMPOSITION_RESULT) carry CCDP data in the JSON-RPC `params` object. Response messages (RESPONSE, HEALTH_RESPONSE) carry CCDP data in the JSON-RPC `result` object.
protocols/ccdp/src/07-message-format.md:7:A CCDP Request encoded as JSON-RPC 2.0:
protocols/ccdp/src/07-message-format.md:12:  "method": "ccdp/request",
protocols/ccdp/src/07-message-format.md:21:A CCDP Response encoded as JSON-RPC 2.0:
protocols/ccdp/src/07-message-format.md:34:The JSON-RPC `id` field MUST match the CCDP `envelope.request_id`. This enables correlation at both the JSON-RPC layer and the CCDP layer.
protocols/ccdp/src/07-message-format.md:38:The following CCDP message types are defined:
protocols/ccdp/src/07-message-format.md:42:| `ccdp/request` | REQUEST | Requester → Dispatcher → Service | JSON-RPC request (`method`, `id`, `params`) |
protocols/ccdp/src/07-message-format.md:43:| `ccdp/escalation` | ESCALATION | Service → Dispatcher → Escalation target | JSON-RPC request (`method`, `id`, `params`) |
protocols/ccdp/src/07-message-format.md:44:| `ccdp/health.request` | HEALTH_REQUEST | Dispatcher → Service | JSON-RPC request (`method`, `id`, `params`) |
protocols/ccdp/src/07-message-format.md:45:| `ccdp/decomposition.result` | DECOMPOSITION_RESULT | Decomposition Service → Dispatcher | JSON-RPC request (`method`, `id`, `params`) |
protocols/ccdp/src/07-message-format.md:46:| `ccdp/notification` | NOTIFICATION | Any → Dispatcher → Any | JSON-RPC notification (`method`, `params`, no `id`) |
protocols/ccdp/src/07-message-format.md:50:REQUEST, ESCALATION, HEALTH_REQUEST, and DECOMPOSITION_RESULT are encoded as JSON-RPC requests — they carry a `method` field, an `id`, and `params`. NOTIFICATION is encoded as a JSON-RPC notification — it carries a `method` and `params` but no `id`, and no response is expected. RESPONSE and HEALTH_RESPONSE are encoded as JSON-RPC responses — they carry an `id` correlating to a prior request and a `result` object containing the CCDP envelope and content. JSON-RPC responses do not carry a `method` field; the CCDP message type is identified by the `envelope.type` field within the `result` object.
protocols/ccdp/src/07-message-format.md:54:The Envelope is the structured metadata portion of every CCDP message. The Dispatcher reads the Envelope for routing and enforcement; Content is semantically opaque, though structurally validated and resolved where specified (Section 6).
protocols/ccdp/src/07-message-format.md:58:The following fields are REQUIRED on every CCDP message envelope:
protocols/ccdp/src/07-message-format.md:63:    "ccdp_version": "1.0",
protocols/ccdp/src/07-message-format.md:75:**`ccdp_version`** (string, REQUIRED): The CCDP protocol version. MUST be `"1.0"` for this specification. Implementations MUST reject messages with an unrecognized version. The wire protocol version is independent of the document version (Section 2.1). This specification document is version 0.2.0; the wire protocol version remains `"1.0"` because the on-the-wire message format has not changed.
protocols/ccdp/src/07-message-format.md:77:**`type`** (string, REQUIRED): The message type. One of: `"REQUEST"`, `"RESPONSE"`, `"NOTIFICATION"`, `"ESCALATION"`, `"HEALTH_REQUEST"`, `"HEALTH_RESPONSE"`, `"DECOMPOSITION_RESULT"`. For method-bearing messages (REQUEST, ESCALATION, HEALTH_REQUEST, DECOMPOSITION_RESULT, NOTIFICATION), the `envelope.type` value MUST correspond to the JSON-RPC `method` field. For response messages (RESPONSE, HEALTH_RESPONSE), the `envelope.type` is carried inside the `result` object and is the sole identifier of the CCDP message type — JSON-RPC responses have no `method` field.
protocols/ccdp/src/07-message-format.md:87:**`source_id`** (string, REQUIRED): The identifier of the component that originated this message. For Requests from external clients, this is the client identifier. For Responses, this is the Service identifier. For forwarded messages, this is the originator, not the Dispatcher. When the Dispatcher forwards a message, it does not overwrite `source_id`. The Dispatcher's identity is recorded in the `audit.dispatcher_id` field (Section 7.5), not in `source_id`. This means `source_id` always identifies the originator, and `audit.dispatcher_id` identifies the intermediary. If future extensions require explicit sender/forwarder identification, they SHOULD use metadata keys (e.g., `org.ccdp.forwarder_id`) rather than overloading `source_id`. For signature verification and authorization, implementations MUST use the Authenticated Sender identity established by the transport layer (Section 4, Section 15.2), not the `source_id` payload field. The `source_id` field is an unauthenticated originator claim; the Authenticated Sender is cryptographically verified.
protocols/ccdp/src/07-message-format.md:89:**`metadata`** (object, REQUIRED but MAY be empty): Extensible key-value metadata. Unknown keys MUST be preserved and forwarded by all intermediaries, including the Dispatcher. Keys use reverse-domain notation for namespacing (e.g., `"com.example.custom_field": "value"`). Keys in the `org.ccdp.*` namespace are reserved for protocol-defined extensions.
protocols/ccdp/src/07-message-format.md:100:    "capability_type": "org.ccdp.deduction",
protocols/ccdp/src/07-message-format.md:165:            "uri": "urn:ccdp:artifact:test-results/run-2026-08-03-001.json",
protocols/ccdp/src/07-message-format.md:241:When `partial_result_available` is true, the Content contains whatever the Service was able to produce before escalating. The Dispatcher MUST include this partial result when forwarding the escalation. This is the canonical location for partial results. Partial results MUST be carried in the ESCALATION message's Content, not in metadata. When the Dispatcher forwards the original Request through the escalation chain, it accumulates partial results from prior Services in the Request's metadata under the key `org.ccdp.partial_results` (Section 13.4.1) for downstream Services' reference. The Content of the forwarded Request remains the original requester's Content. For Dispatcher-generated post-receipt mismatch Escalations (`provenance_mismatch_policy`, Section 9.2), the partial result is the original Service Response content that failed to meet the provenance requirement.
protocols/ccdp/src/07-message-format.md:252:    "capability_type": "org.ccdp.notification",
protocols/ccdp/src/07-message-format.md:289:        "org.ccdp.deduction": {
protocols/ccdp/src/07-message-format.md:337:| `ccdp_version` | R | R | R | R | R | R | R |
protocols/ccdp/src/07-message-format.md:358:The Content is the opaque payload of a CCDP message. Its structure is governed by the Capability Record's input schema (for Requests) or output schema (for Responses).
protocols/ccdp/src/07-message-format.md:364:    "schema_ref": "org.ccdp.deduction/input/v2",
protocols/ccdp/src/07-message-format.md:446:Implementations MUST support messages of at least 16 MiB. Implementations SHOULD support messages of at least 64 MiB. Messages exceeding the implementation's size limit MUST be rejected with HTTP status 413 (Payload Too Large). If the message is small enough to parse but exceeds the CCDP implementation's processing limit, it MUST be rejected with CCDP error code `-32602` (Invalid params) with a `data.reason` of `"message_too_large"`.
protocols/ccdp/src/07-message-format.md:452:The `metadata` field on every envelope provides the extension point for protocol evolution:
protocols/ccdp/src/07-message-format.md:456:Metadata keys in the `org.ccdp.*` namespace that begin with `org.ccdp.request.*` are request-directional: they are meaningful on REQUEST and ESCALATION messages and SHOULD NOT be blindly copied to RESPONSE messages. Metadata keys beginning with `org.ccdp.response.*` are response-directional. Keys without a directional prefix are bidirectional and MUST be preserved in both directions. This prevents request-only control metadata (e.g., escalation history, routing hints) from leaking into responses.
protocols/ccdp/src/07-message-format.md:458:2. New protocol features SHOULD be introduced as metadata keys in the `org.ccdp.*` namespace before being promoted to top-level envelope fields in a subsequent protocol version.
protocols/ccdp/src/07-message-format.md:464:This approach follows the TCP/IP tradition of extensible headers: existing implementations continue to work as new fields are added, and the protocol evolves without version bumps for non-breaking changes.
protocols/ccdp/src/07-message-format.md:482:When CCDP messages are transported over HTTP, the following mapping applies:
protocols/ccdp/src/07-message-format.md:484:| Scenario | HTTP Status | CCDP Behavior |
protocols/ccdp/src/07-message-format.md:486:| Successful response | 200 OK | JSON-RPC result with CCDP envelope |
protocols/ccdp/src/07-message-format.md:495:HTTP status codes in the 4xx range indicate client-side issues; 5xx indicate Dispatcher-side issues. The JSON-RPC error body, when present, carries the structured CCDP error with trace context for audit correlation.
