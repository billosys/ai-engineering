# Slice 03: CCDP Package Implementation Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | Generated assembled-spec freshness is enforced before packaging. | Run `make ccdp-package` or the package precheck and inspect evidence that temporary assembly output is compared with `protocols/ccdp/composite-cognition-dispatch-protocol.md`; if drift exists, inspect the named generated-spec refresh and rerun. | serious | slice02 bubble-up | pending |  | This row is the explicit disposition of Slice 01's generated-output drift. |
| F-2 | Root Makefile exposes CCDP package build and check targets without folding CCDP into skill-bundle `all`/`INSTALL_ZIPS` semantics. | Run `make help`; inspect `Makefile`; run `make ccdp-package` and `make check-ccdp-package`. | correctness | slice02 design | pending |  | CCDP is a protocol package, not a skill bundle. |
| F-3 | `ccdp.zip` exists and contains exactly one `ccdp/` archive root. | Run `make ccdp-package && unzip -l ccdp.zip`; validator must assert all entries are under `ccdp/`. | serious | slice02 design | pending |  | |
| F-4 | Required package contents are present and excluded material is absent. | Run `make check-ccdp-package`; inspect validator output or artifact manifest for assembled spec, `src/`, `json/`, `visual-guide/`, template, assembler Cargo/source files, package `Makefile`, generated README, and absence of workbench/prompts/Cargo target/root README/root Makefile. | serious | slice02 design | pending |  | |
| F-5 | The generated `ccdp/README.md` is package-local and usable as the unzipped entrypoint. | Inspect generated README inside `ccdp.zip` or extracted package; run package validator link checks. | serious | slice02 design | pending |  | Keep this generated/staged rather than copying root README unchanged. |
| F-6 | `scripts/check-ccdp-package` implements CCDP-specific validation. | Inspect the script and run `make check-ccdp-package`; if a self-test mode exists, run it too. | serious | slice02 design | pending |  | It may share helper ideas with `check-package-paths` but must validate CCDP semantics. |
| F-7 | Package-local Markdown path validation passes without treating protocol syntax as filesystem paths. | Run `make check-ccdp-package`; inspect checks/fixtures or code paths for JSON Pointer, slash-prefixed protocol paths, elision comments, and field-path filtering. | serious | slice02 design | pending |  | Avoid both false hard failures and blind suppression. |
| F-8 | An extracted CCDP package can run a non-mutating assembly check. | Extract `ccdp.zip` to a temp dir and run `make -C <tmp>/ccdp ccdp-rfc OUTPUT=/private/tmp/<name>.md`, or inspect `make check-ccdp-package` evidence for the same operation. | serious | slice02 design | pending |  | The check must not dirty the source checkout or package artifact. |
| F-9 | Existing source-clone CCDP assembly remains available. | Run `make ccdp` or `make -C protocols/ccdp ccdp-rfc OUTPUT=/private/tmp/<name>.md`; record any generated-file drift separately. | correctness | arc-plan | pending |  | |
| F-10 | Existing skill-bundle packaging and path gates still pass. | Run `make check-package-paths` and `make all`. | serious | project DoD | pending |  | Builds many zips; run serially. |
| F-11 | Implementation scope stays within Arc 03 packaging. | Inspect `git diff --stat`, `git diff --check`, and changed file list for no CCDP runtime behavior, no workbench/prompts inclusion, and no unrelated planning/source changes. | correctness | slice-plan | pending |  | Generated assembled-spec refresh is allowed only if required by F-1. |
| F-12 | The close report walks every ledger row, inventories artifacts, names implementation state, and bubbles findings up to Arc 03. | From planning worktree: `test -f project01-harmonise-paths/arc03-ccdp-distribution-package/slice03-ccdp-package-implementation/closing-report.md && rg -n "F-1|F-2|F-3|F-4|F-5|F-6|F-7|F-8|F-9|F-10|F-11|F-12|Artifacts|Bubble-up to Arc 03" project01-harmonise-paths/arc03-ccdp-distribution-package/slice03-ccdp-package-implementation/closing-report.md`. | correctness | slice-plan | pending |  | Proposed-done remains pending CDC verification. |
